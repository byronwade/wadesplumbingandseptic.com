# Eve SEO Agent Implementation Status

## Phase 76: Task 7 Local Falcon Live Probe Path (2026-08-03)

- State: READY_FOR_HUMAN_REVIEW / offline `MOCK_VERIFIED`; Task 6 GA4 explicitly skipped with recorded blocker so Task 7 may proceed. Live Local Falcon proof blocked until owner sets `LOCAL_FALCON_API_KEY`.
- Branch/PR: `cursor/eve-local-falcon-live-aab8` (Task 7 from `INTEGRATION_ROLLOUT.md`).
- Offline: focused probe `probeLocalFalconLive`; CRON route `GET /_internal/eve/api/live-probe/local-falcon`; CLI `npm run live:probe:local-falcon`; adapter soft-fails upstream errors to `FAILED` evidence; Cron allowlisted.
- Docs: `MANUAL_SETUP.md` Next: Local Falcon; Task 6 moved to Deferred.
- Next exact action: merge/deploy Task 7; arm and Cron-prove if `LOCAL_FALCON_API_KEY` is present, else record `BLOCKED_MISSING_CREDENTIALS`; then trigger standing propose Cron for a draft blog PR test.

## Phase 75: Task 6 GA4 Production Path Deployed; Live Proof BLOCKED (2026-08-03)

- State: Path on `main` / Production; Cron exercised; live result `BLOCKED_MISSING_CREDENTIALS` (no `GA4_PROPERTY_ID`). Not `LIVE_VERIFIED`.
- Branch/PR: `cursor/eve-ga4-live-aab8` / `#119` squash-merged to `main` (`5bf9ef1`).
- Production: code deploy `dpl_526tjDnzit1Nyf4rzaaU8p9gyufR`; armed deploy `dpl_7jioeRN4hSwyzfe3gsFmaciA2gzm` with `SEO_AGENT_ENABLE_GA4=true`, `SEO_AGENT_LIVE_READS_APPROVED=true`, `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID=ga4-2026-08-03`.
- Cron evidence (redacted): three invocations on armed deploy returned HTTP `503` (handler returns `200` only for `LIVE_VERIFIED`):
  - `2026-08-03T16:57:41.970Z` request `7p76k-1785776261970-aaa8d15e9c8b` → `503`
  - `2026-08-03T16:57:46.316Z` request `26b49-1785776266316-0405072ab4ca` → `503`
  - `2026-08-03T16:57:50.659Z` request `2gzmb-1785776270659-9e9dc22c6a7a` → `503`
- Cause: Production has Google service account + `SEO_AGENT_ENABLE_GA4` arming path, but no `GA4_PROPERTY_ID` (and no `GA4_ACCESS_TOKEN`). Owner must complete Manual Setup Next: Google Analytics Data (GA4).
- Cleanup: live-reads approved false, approved run ID removed, `SEO_AGENT_ENABLE_GA4=false`, Production redeploy `dpl_3uPKVQtU6NdtxZgfZbPXyhJQS1qb` after reset.
- Branches/PRs: `#119` merged; no open PRs; no leftover `cursor/*` feature branches after cleanup.
- Skip decision: Task 6 remains blocked on missing `GA4_PROPERTY_ID`; rollout continues to Task 7 with this recorded skip. Owner can still complete Deferred GA4 setup later.
- Next exact action: SUPERSEDED by Phase 76 Task 7 Local Falcon path.

## Phase 74: Task 6 GA4 Live Probe Path (2026-08-03)

- State: READY_FOR_HUMAN_REVIEW / offline `MOCK_VERIFIED`; live proof blocked until owner completes GA4 property access and sets `GA4_PROPERTY_ID`.
- Branch/PR: `cursor/eve-ga4-live-aab8` (Task 6 from `INTEGRATION_ROLLOUT.md`).
- Offline: `createGa4Adapter` accepts service-account token provider (`analytics.readonly`) or `GA4_ACCESS_TOKEN`; normalizes `properties/<id>`; returns aggregate `sessions_total` / `row_count` only; upstream errors → `FAILED` evidence. Focused probe `probeGa4Live`; CRON route `GET /_internal/eve/api/live-probe/ga4`; CLI `npm run live:probe:ga4`; Cron allowlisted.
- Docs: `MANUAL_SETUP.md` adds owner steps for Analytics Data API, GA4 Viewer grant on the existing Eve service account, and Production `GA4_PROPERTY_ID`.
- Commands (sidecar): `npm run format` exit `0`; `npm run typecheck` exit `0`; `npm test` → `179/179`; `npm run verify` exit `0`.
- Live arming (next): after owner sets `GA4_PROPERTY_ID` and grants Viewer, set `SEO_AGENT_ENABLE_GA4=true`, `SEO_AGENT_LIVE_READS_APPROVED=true`, `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID=ga4-2026-08-03`, deploy, Cron-run `/_internal/eve/api/live-probe/ga4` three times (`200` = `LIVE_VERIFIED`), then reset flags.
- Next exact action: SUPERSEDED by Phase 75 Production Cron exercise (blocked on missing `GA4_PROPERTY_ID`).

## Phase 73: Task 6 GA4 BLOCKED_MISSING_CREDENTIALS (2026-08-03)

- State: SUPERSEDED by Phase 74 probe path. Previously blocked: Production had `SEO_AGENT_ENABLE_GA4` but no `GA4_PROPERTY_ID` / access token. No live GA4 request was made at that checkpoint.

## Phase 72: Task 5 Browser Research LIVE_VERIFIED (2026-08-03)

- State: COMPLETE for Task 5 HTTP browser-research live proof (`LIVE_VERIFIED`).
- Branch/PR: `cursor/eve-browser-research-live-aab8` / `#116`.
- Method: Armed Production for run ID `browser-research-2026-08-03` (`SEO_AGENT_BROWSER_RESEARCH_ENABLED=true`, Search Console/PageSpeed/Browserbase flags false, `SEO_AGENT_LIVE_READS_APPROVED=true`, matching approved run ID), Production-deployed commit `3be489d` as `dpl_FVcXgzZ2AD5ysz6aXAJtvhA2fLea`, then triggered Production Cron `GET /_internal/eve/api/live-probe/browser-research` three times. Handler returns HTTP `200` only for `classification: LIVE_VERIFIED`.
- Evidence (redacted): three successful Production responses on deployment `dpl_FVcXgzZ2AD5ysz6aXAJtvhA2fLea`:
  - `2026-08-03T16:11:19.604Z` request `pvg9f-1785773479604-875726474167` → `200`
  - `2026-08-03T16:11:24.010Z` request `jxqqx-1785773484010-820345f8e44f` → `200`
  - `2026-08-03T16:11:28.231Z` request `4klwv-1785773488231-7505d212de28` → `200`
- Cleanup: `SEO_AGENT_LIVE_READS_APPROVED=false`, approved run ID removed, `SEO_AGENT_BROWSER_RESEARCH_ENABLED=false`, Production redeploy `dpl_7AotW1u2guL5ZJKEAc5TjiTyipm1` after reset. Standing propose may still auto-enable research for Cron proposal runs.
- Browserbase: still `BLOCKED_MISSING_CREDENTIALS` (no Production API key/project id).
- Next exact action: start Task 6 (GA4) only if GA4 credentials/property access are reviewed; otherwise record `BLOCKED_MISSING_CREDENTIALS` and skip to the next owned task.

## Phase 71: Task 5 Browser Research Path + Past-Phase Hardening (2026-08-03)

- State: SUPERSEDED by Phase 72 live proof. Offline path + past-phase hardening shipped on `cursor/eve-browser-research-live-aab8` (`MOCK_VERIFIED`; `175/175` tests).
- Confirmation on `main` before this branch: sidecar `npm run format:check`, `typecheck`, `test` → `170/170`, `verify` exit `0`. Production health `GET /_internal/eve/api/healthz` → `200`, `ready: true`, `mode: propose`, `mutation_kill_switch: false`, live-read flags off.
- Past-phase improvements: HTTP handler tests for Task 3 topics and Task 4 PageSpeed QA routes; `INTEGRATION_ROLLOUT.md` Tasks 5 to 14 stubs; `MANUAL_SETUP.md` Tasks 1 to 4 marked complete; `pageSpeed` flag naming note.

## Phase 70: Task 4 PageSpeed Draft/Preview QA LIVE_VERIFIED (2026-08-03)

- State: COMPLETE for Task 4 live proof (`LIVE_VERIFIED`).
- Branch/PR: `cursor/eve-pagespeed-qa-wiring-aab8` / `#114`.
- Method: Armed Production for run ID `pagespeed-qa-2026-08-03` (`SEO_AGENT_ENABLE_PAGESPEED=true`, `SEO_AGENT_ENABLE_SEARCH_CONSOLE=false`, `SEO_AGENT_LIVE_READS_APPROVED=true`, matching approved run ID), Production-deployed commit `32b6538` as `dpl_7KfPa57Q6RVkaKPQi5et4AnDDvjU`, then triggered Production Cron `GET /_internal/eve/api/live-probe/pagespeed-qa` three times. Handler returns HTTP `200` only for `classification: LIVE_VERIFIED`.
- Evidence (redacted): three successful Production responses on deployment `dpl_7KfPa57Q6RVkaKPQi5et4AnDDvjU`:
  - `2026-08-03T15:42:21.357Z` request `65vkz-1785771741357-0f9f97fa4377` → `200`
  - `2026-08-03T15:42:27.705Z` request `jbv4p-1785771747705-fe677252cc12` → `200`
  - `2026-08-03T15:42:34.002Z` request `2frhj-1785771754002-d21d8f77df25` → `200`
- Cleanup: `SEO_AGENT_LIVE_READS_APPROVED=false`, approved run ID removed, `SEO_AGENT_ENABLE_PAGESPEED=false`, Production redeploy `dpl_8jafayWyxzcBp1KYnbs2Zge3USx2` after reset.
- Soft-budget rule: PageSpeed QA annotates human review only. It does not fail-close draft publication.
- Next exact action: start Task 5 (browser research / Browserbase) on its own PR.

## Phase 69: Task 4 PageSpeed Draft/Preview QA Path (2026-08-03)

- State: SUPERSEDED by Phase 70 live proof. Offline QA wiring path shipped on `cursor/eve-pagespeed-qa-wiring-aab8` (`MOCK_VERIFIED` fixtures; `170/170` tests).
- Offline: `pagespeed-qa.mjs` extracts performance score + core vitals, grades soft PASS/WARN/FAIL budgets, redacts measured URLs, and omits raw Lighthouse audit dumps from durable evidence; `executeDraftProposal` soft-fails when PageSpeed is off and adds a Preview / performance QA section to the Connect draft PR brief; focused probe `probePageSpeedQaLive`; CRON-authenticated route `GET /_internal/eve/api/live-probe/pagespeed-qa`; CLI `npm run live:probe:pagespeed-qa`; annual manual Cron entry allowlisted in deployment-config verifier.

## Phase 68: Task 3 Search Console Topic Wiring LIVE_VERIFIED (2026-08-03)

- State: COMPLETE for Task 3 live proof (`LIVE_VERIFIED`).
- Branch/PR: `cursor/eve-gsc-topic-wiring-aab8` / `#113`.
- Method: Armed Production for run ID `search-console-topics-2026-08-03` (`SEO_AGENT_ENABLE_SEARCH_CONSOLE=true`, `SEO_AGENT_ENABLE_PAGESPEED=false`, `SEO_AGENT_LIVE_READS_APPROVED=true`, matching approved run ID), Production-deployed commit `06647e5` as `dpl_7gtrEWvmx6zmyvRKXkmQ4cBPcMgS`, then triggered Production Cron `GET /_internal/eve/api/live-probe/search-console-topics` three times. Handler returns HTTP `200` only for `classification: LIVE_VERIFIED`.
- Evidence (redacted): three successful Production responses on deployment `dpl_7gtrEWvmx6zmyvRKXkmQ4cBPcMgS`:
  - `2026-08-03T15:32:10.232Z` request `mcxf9-1785771130232-a771f1f49e53` → `200`
  - `2026-08-03T15:32:28.908Z` request `nfpqh-1785771148908-0fa253aae282` → `200`
  - `2026-08-03T15:32:32.137Z` request `bnksh-1785771152137-ff6f49e55f1c` → `200`
- Cleanup: `SEO_AGENT_LIVE_READS_APPROVED=false`, approved run ID removed, `SEO_AGENT_ENABLE_SEARCH_CONSOLE=false`, Production redeploy `dpl_CK9coF1KUP446G6rd8AuD41SufgP` after reset.
- Next exact action: start Task 4 (PageSpeed QA wiring) on its own PR.

## Phase 67: Task 3 Search Console Topic Wiring Path (2026-08-03)

- State: SUPERSEDED by Phase 68 live proof. Offline wiring path shipped on `cursor/eve-gsc-topic-wiring-aab8` (`MOCK_VERIFIED` fixtures; `163/163` tests).
- Offline: `search-console-topics.mjs` matching + capped `score_bonus`; adapter `queryTopicSignals` with redacted evidence; `executeDraftProposal` soft-fails when Search Console is off and applies `gsc_signal` when signals match; focused probe `probeSearchConsoleTopicsLive`; CRON-authenticated route `GET /_internal/eve/api/live-probe/search-console-topics`; CLI `npm run live:probe:search-console-topics`; annual manual Cron entry allowlisted in deployment-config verifier.

## Phase 66: Task 2 PageSpeed LIVE_VERIFIED (2026-08-03)

- State: COMPLETE for Task 2 live proof (`LIVE_VERIFIED`).
- Branch/PR: `cursor/eve-pagespeed-live-aab8` / `#110` merged to `main`.
- Method: Production Cron invocation of `GET /_internal/eve/api/live-probe/pagespeed` with Vercel-injected `CRON_SECRET`. Handler returns HTTP `200` only for `classification: LIVE_VERIFIED`. First armed attempt on the initial probe path returned `403` in ~300ms (upstream/request-bound failure before the longer PageSpeed policy landed); after raising the PageSpeed timeout/byte budget and returning `FAILED` evidence for upstream errors, three Cron probes succeeded.
- Evidence (redacted): three successful Production responses on deployment `dpl_8jiVBefvXrezjkZsYUbNQk9rZtvy`:
  - `2026-08-03T15:02:34.624Z` request `jlmdf-1785769354624-edf5f2e49d8b` → `200`
  - `2026-08-03T15:04:18.215Z` request `89kdm-1785769458215-e741a6a75eb2` → `200`
  - `2026-08-03T15:05:54.588Z` request `x288r-1785769554588-9e473083c699` → `200`
- Cleanup: `SEO_AGENT_LIVE_READS_APPROVED=false`, approved run ID removed, `SEO_AGENT_ENABLE_PAGESPEED=false`, Production redeploy `dpl_4CyZxrkZM3p3pkxBbiGJfx5VRkXr`. Health remains `mode: propose`, `mutation_kill_switch: false`.
- Next exact action: start Task 3 (Search Console topic wiring) on its own PR.

## Phase 65: Task 2 PageSpeed Live Probe Path (2026-08-03)

- State: SUPERSEDED by Phase 66 live proof. Offline probe path shipped on `cursor/eve-pagespeed-live-aab8` (`MOCK_VERIFIED` fixtures; `127/127` tests).
- Offline: focused PageSpeed probe helpers, CRON-authenticated Production route `GET /_internal/eve/api/live-probe/pagespeed`, CLI `npm run live:probe:pagespeed`, annual manual-trigger Cron entry, and deterministic fixtures. Deployment-config verifier allowlists only the Search Console and PageSpeed live-probe crons. Sidecar `npm test` → `127/127` passing; format/typecheck/verify passed. This is `MOCK_VERIFIED` for the probe path.

## Phase 64: Search Console live retest (2026-08-03)

- State: COMPLETE retest. Search Console remains `LIVE_VERIFIED`.
- Method: Re-armed Production for run ID `search-console-retest-2026-08-03` (`SEO_AGENT_ENABLE_SEARCH_CONSOLE=true`, `SEO_AGENT_LIVE_READS_APPROVED=true`, matching approved run ID), redeployed `dpl_5q8pyVTiDp3Fra7eFwbW5myquTvq`, then triggered Production Cron `GET /_internal/eve/api/live-probe/search-console` three times. Handler returns HTTP `200` only for `classification: LIVE_VERIFIED`.
- Evidence (redacted):
  - `2026-08-03T14:46:43.279Z` request `6fxnq-1785768403279-e0b72c0b4d9d` → `200`
  - `2026-08-03T14:46:59.797Z` request `7vhmb-1785768419797-cf14e9d7523f` → `200`
  - `2026-08-03T14:47:07.183Z` request `78tfs-1785768427183-b28b62851423` → `200`
- Cleanup: `SEO_AGENT_LIVE_READS_APPROVED=false`, approved run ID removed, `SEO_AGENT_ENABLE_SEARCH_CONSOLE=false`, Production redeploy `dpl_DapyXdqEZRGun8cRrzdSA2sJT1Qy`. Health again `mode: propose`, `mutation_kill_switch: false`.


## Phase 63: Task 1 Search Console LIVE_VERIFIED (2026-08-03)

- State: COMPLETE for Task 1 live proof (`LIVE_VERIFIED`).
- Branch/PRs: `#106` (probe path) and `#107` (Cron trigger) merged to `main`.
- Method: Production Cron invocation of `GET /_internal/eve/api/live-probe/search-console` with Vercel-injected `CRON_SECRET` (local CLI cannot read Sensitive secrets). Handler returns HTTP `200` only for `classification: LIVE_VERIFIED`.
- Evidence (redacted): three successful Production responses on deployment `dpl_DyNQf96UdyXcA9pCfwCxYtx16DGe`:
  - `2026-08-03T14:35:36.041Z` request `jsh2f-1785767736041-0b69afa834f2` → `200`
  - `2026-08-03T14:36:04.615Z` request `b7m42-1785767764615-502f16a8929e` → `200`
  - `2026-08-03T14:36:25.128Z` request `ml9pv-1785767785128-14ab8d8d6262` → `200`
- Cleanup: `SEO_AGENT_LIVE_READS_APPROVED=false`, approved run ID removed, `SEO_AGENT_ENABLE_SEARCH_CONSOLE=false`, `SEO_AGENT_FORCE_OBSERVE=false` (redeploy restores propose Cron). No content write, draft PR, or merge of site content.
- Note: Eve audit Cron `audit-2026-08-03` (`wrun_41KZ40H1GC0GPMHDHHFFWJGNMW`) returned `BLOCKED_MISSING_CREDENTIALS` for missing Git-backed repository snapshot before adapters ran; the focused live-probe path is the authoritative Search Console proof.
- Next exact action: start Task 2 (PageSpeed live verification) on its own PR. Do not wire GSC into topic selection until Task 2 exits.

## Phase 62: Task 1 Search Console Live Probe Path (2026-08-03)

- State: READY_FOR_HUMAN_REVIEW / blocked on Production deploy for final `LIVE_VERIFIED`. Task 1 from `INTEGRATION_ROLLOUT.md` started on dedicated branch `cursor/eve-search-console-live-aab8`.
- Offline: focused Search Console probe helpers, CRON-authenticated Production route `GET /_internal/eve/api/live-probe/search-console?run_id=...`, CLI `npm run live:probe:search-console`, and deterministic fixtures. Sidecar `npm test` → `122/122` passing; verify/format/typecheck passed. This is `MOCK_VERIFIED` for the probe path.
- Production config set for the exact run ID `search-console-live-2026-08-03`: `SEO_AGENT_ENABLE_SEARCH_CONSOLE=true`, `SEO_AGENT_ENABLE_PAGESPEED=false`, `SEO_AGENT_LIVE_READS_APPROVED=true`, matching run ID, `SEO_AGENT_ENV=production` (boolean/run flags stored non-sensitive so CLI can see them).
- Live blocker: `vercel env run -e production` does **not** inject Sensitive Production secrets (`GOOGLE_SERVICE_ACCOUNT_*`, `CRON_SECRET`, `PAGESPEED_API_KEY` absent in the local process). Local execute therefore cannot mint a Search Console token. Live proof must run inside the deployed Production Eve runtime after this PR is human-merged and deployed, via:
  - `GET /_internal/eve/api/live-probe/search-console?run_id=search-console-live-2026-08-03` with `Authorization: Bearer $CRON_SECRET`
- After a `LIVE_VERIFIED` response: reset `SEO_AGENT_LIVE_READS_APPROVED=false` and remove the approved run ID. Do not start Task 2 until that proof exists.
- Out of scope (Task 3): wiring GSC into topic selection.
- Next exact action: human merge/deploy this PR, then run the Production live-probe URL once and record the redacted classification.

## Phase 60: Proposal Generation Token Safeguards (2026-08-03)

- State: MERGED via `#104` / READY_FOR_HUMAN_REVIEW was. Owner asked to confirm the generation pipeline is careful with tokens and to add safeguards so Eve cannot accidentally burn the session budget on wasteful generation. Follow-up: quality remains more important than thrift.
- Branch: `cursor/eve-blog-quality-aab8`.
- Changed scope: added `src/generation-guards.mjs` with proposal-stage waste ceilings. Quality-first budgets: write **6,500**, expand **4,000**, expand-prompt draft ceiling **28,000** chars, reserved output **20,000**, max **5** model calls. Safeguards that remain: skip obvious topic-model re-ranks, no-progress expand stop, structure-preserving truncation only for runaway input, and fallback calls billed to the same guard. Expand prompts explicitly prioritize completeness over brevity. PR briefs report generation spend.
- Verification: recorded after the quality-first rebalance test run. This remains `MOCK_VERIFIED`; AI Gateway is not called by these fixtures.
- Next exact action: Task 3 Search Console topic wiring after `#104` merges.

## Phase 59: Expand-Existing Peer Review Instead of Full Rewrites (2026-08-03)

- State: MERGED via `#104` / READY_FOR_HUMAN_REVIEW was. Owner asked Eve to peer-review and expand good drafts for SEO depth instead of rewriting whole posts when they are only too thin or missing sections.
- Correction: `src/draft-revision.mjs` classifies expandable quality gaps (`TOO_THIN`, FAQ/section/link/must-cover gaps, local/meta gaps) versus fatal claim failures. Proposal runs now peer-review the existing draft and run up to two expand/revise rounds with a smaller token budget and preserve-first prompts. Fatal claim failures still reject immediately with no rewrite loop.
- PR briefs record expansion rounds so reviewers can see that Eve added depth instead of starting over.
- Verification: with Node `v24.14.0`, sidecar `npm test` exited `0` (`135/135` passing), including expand-existing and fatal-claim non-expand coverage.
- Next exact action: merge/deploy with prior brief/autonomy work, then confirm a live draft that needed depth shows expansion rounds in the PR body.

## Phase 58: Detailed Draft PR Briefs (2026-08-03)

- State: MERGED via `#104` / READY_FOR_HUMAN_REVIEW was. Owner asked for detailed PR briefs that explain what Eve did, why it chose the topic over alternatives, why the post matters, and how it helps SEO.
- Correction: Connect draft PR bodies now use `buildDraftPrBrief` with reviewer sections for run actions, timing importance, alternatives considered/skipped, SEO mechanics, expected observation window, and execution evidence, while keeping the required packet markers.
- Topic decisions also capture `why_over_others` and `seo_value` for the brief.
- Verification: with Node `v24.14.0`, sidecar `npm test` exited `0` (`130/130` passing), including detailed brief packet marker coverage.
- Next exact action: merge/deploy with prior autonomy work, then confirm the next live draft PR body contains the detailed brief sections.

## Phase 57: Fully Autonomous Research and Topic Decisions (2026-08-03)

- State: MERGED via `#104` / READY_FOR_HUMAN_REVIEW was. Owner direction: nothing in the proposal path should be an owner option or unavailable toggle. Eve must research and decide automatically.
- Correction: standing Production propose auto-enables community browser research with versioned hosts (`COMMUNITY_RESEARCH_DOMAINS`). Proposal runs attach the browser research adapter by default (fixtures may pass `null` only for offline tests). Eve also runs an autonomous model topic decision over viable candidates; score ranking remains fallback only when the model cannot return a valid id. Writer prompts receive decision rationale and research notes.
- Remaining human boundary: draft PR merge/release only. Eve still cannot write `main`, merge, or deploy.
- Verification: with Node `v24.14.0`, sidecar `npm test` exited `0` (`129/129` passing). Classification for automatic research/decision wiring is `MOCK_VERIFIED` until the next live Cron draft is reviewed.
- Next exact action: merge and deploy, run Eve Cron, confirm the draft PR packet shows model topic decision plus automatic demand research without owner env toggles.

## Phase 56: Automatic Local Demand and Community Timing (2026-08-03)

- State: MERGED via `#104` / READY_FOR_HUMAN_REVIEW was / extended by Phase 57. Owner asked for a more advanced automatic research path: holidays, trending concepts, and Santa Cruz County community events (including Capitola Art & Wine) so neighbors find and click the site.
- Correction: Eve proposal selection now merges a versioned local demand calendar (`seo/manifests/local-demand-calendar.json`) and trending local concepts (`seo/manifests/trending-local-concepts.json`) ahead of the evergreen catalog. Active windows cover Independence Day, Labor Day, Thanksgiving, New Year, Watsonville Strawberry Festival, Capitola Art & Wine, Santa Cruz County Fair, Open Studios, rainy season, and summer visitor hosting, plus hard-water, vacation-rental, ADU, and storm-week trend concepts.
- Runtime: demand timing is automatic from versioned manifests. Phase 57 removes the owner browser-research toggle requirement for standing propose.
- Quality: demand-timed drafts must name the community event/holiday/trend, keep people-first depth gates, and reject fake event affiliation claims.
- Verification: with Node `v24.14.0`, sidecar `npm test` exited `0` (`127/127` passing before Phase 57).
- Next exact action: complete Phase 57 merge/deploy proof.

## Phase 55: Blog Generation Quality Rebuild (2026-08-03)

- State: MERGED via `#104` / READY_FOR_HUMAN_REVIEW was / extended by Phase 56. Owner closed the thin Connect draft (#102) and asked for people-first SEO content that can compete: more unique pages, stronger click appeal, and enough depth to satisfy helpful-content expectations.
- Live proof context: Cron → Connect previously opened draft PR [#102](https://github.com/byronwade/wadesplumbingandseptic.com/pull/102) via `gitwadesplumbingandseptic-com[bot]` (Agent Run `wrun_41KZ3TS9TB0GQPT2K1YEX9SMHQ`). That post was generic, non-local, thinly linked, and not worth keeping. PRs `#102` and `#101` are closed; the colliding `eve/seo/...hosting-checklist...` branch was deleted.
- Root cause: the live Cron path used a hardcoded hosting-checklist topic, a generic anti-local writer prompt, a single home link, and fallback publishing that could open a draft PR from junk Markdown.
- Correction: `src/blog-opportunity.mjs` now holds a 10-topic Santa Cruz County catalog with click titles, CTR meta hooks, unique-value briefs, must-cover points, and people-also-ask questions. Selection stays inventory-aware. The writer brief targets people-first helpful content. Publish gates require about `1,400`+ words, `7`+ H2s, Quick Answer, unique-value section, `5`+ FAQ answers, `4`+ planned internal links, local meta description, and reject generic filler or unsupported claims. Writer max output tokens raised to `6,500`. Junk drafts still return `REJECTED_DRAFT_QUALITY` with no Connect PR.
- Verification: with Node `v24.14.0`, sidecar `npm test` exited `0` (`122/122` passing originally; Phase 56 raises the suite to `127/127`).
- Next exact action: complete Phase 56 merge/deploy proof.

## Phase 54: Live Connect Draft PR Proof (2026-08-03)

- State: LIVE_VERIFIED for Cron → Vercel Connect → draft PR. Not a fixture result. Publisher reuses an existing draft branch or open draft PR on GitHub create `422` (`#103` merged).
- Connect: team `wades-web-dev` connector `github/wadesplumbingandseptic-com` (`scl_FVfVjQbQtfeqYXRbfvxCrA`), GitHub App `gitwadesplumbingandseptic-com`, installation `150943643` on `byronwade`, attached to project `wadesplumbingandseptic-com` for Production and Preview.
- Production health before proof: `mode: propose`, `ready: true`, `mutation_kill_switch: false`.
- Commands: `vercel crons run "/eve/v1/cron/mztlWjGEvDDzWJTu64vs_D4R0wSVHIOJ1Cxo9o6r_VI" --project wadesplumbingandseptic-com --scope wades-web-dev` at `2026-08-03T12:50:08.094Z`. Agent Run `wrun_41KZ3TS9TB0GQPT2K1YEX9SMHQ` completed in `30.7s` on deployment `dpl_3ZTdJevhxmAQwp7xv1vqW5zfTVF9`.
- Draft PR: [#102](https://github.com/byronwade/wadesplumbingandseptic.com/pull/102) opened by `gitwadesplumbingandseptic-com[bot]` on `eve/seo/2026-08-03-home-plumbing-hosting-checklist-2026-08-03` with `content/posts/home-plumbing-hosting-checklist-2026-08-03.md`. Draft only; no merge and no `main` write.
- Earlier same-day failure: Agent Run `wrun_41KZ3TN9860GSFRYR8Q3EV14EK` hit GitHub HTTP `422` because the interim manual `eve/seo/...` branch already existed. After that branch was removed, the second Cron succeeded.

## Phase 53: Remove Draft-PR Safety Blocks (2026-08-03)

- State: READY_FOR_HUMAN_REVIEW. Owner asked to remove safety guards so Eve can just open a draft PR.
- Changed scope: proposal authorization only checks the job name. Independent QA reject, content-policy reject, mutation/kill-switch/GitHub flag checks, and writer failures no longer block publication. A deterministic Markdown fallback is used when the gateway or draft shape fails. The publisher remains draft-only through Vercel Connect and still cannot merge or write `main`.
- Next exact action: merge and deploy, then run the Eve Cron and confirm one `eve/seo/...` draft PR.

## Phase 52: Arm Production Propose Without Dashboard Token (2026-08-03)

- State: READY_FOR_HUMAN_REVIEW / merge. PR #98 is on `main` and Production-deployed, but this cloud agent still cannot write Vercel project env (MCP `needsAuth`, no `VERCEL_TOKEN`, device login needs owner GitHub session).
- Correction: Vercel Production now stands in propose mode in code (`VERCEL_ENV=production` or `SEO_AGENT_ENV=production`) so Cron → Connect draft PRs are armed even when stale dashboard values still say observe / kill-switch true. Set `SEO_AGENT_FORCE_OBSERVE=true` to restore audit-only Cron.
- Next exact action: merge this arming change to `main`, confirm Production health reports `mode: propose` and `mutation_kill_switch: false`, then run the Eve Cron (dashboard Run or Monday schedule) and confirm one `eve/seo/...` draft PR.

## Phase 51: Standing Cron → Connect Draft PR Path (2026-08-03)

- State: READY_FOR_HUMAN_REVIEW. Owner direction: stop the one-time run-ID / Vercel CLI gate circus. Eve should research on Cron and open the draft PR with Vercel Connect GitHub.
- Changed scope: proposal authorization now requires only `SEO_AGENT_RUN_MODE=propose`, `SEO_AGENT_MUTATION_MODE=enabled`, `SEO_AGENT_MUTATION_KILL_SWITCH=false`, and `SEO_AGENT_ENABLE_GITHUB=true`. The native Monday Cron selects `proposal` automatically in propose mode and `audit` in observe mode. The GitHub publisher still uses only a short-lived Vercel Connect draft-write token and still cannot merge, deploy, force-push, or write `main`.
- Removed from the live path: exact `SEO_AGENT_PUBLISHING_APPROVED_RUN_ID`, precondition audit run ID, publishing human-approval flag, integration-test flag, and `SEO_AGENT_NATIVE_SCHEDULE_JOB=proposal` as the primary selector.
- Verification: run sidecar `test` for proposal/publisher regressions, then `npm run verify:all` before merge.
- Next exact action: merge and deploy this branch, set the four Production values above in the Vercel project UI, run the Eve Cron (dashboard Run or Monday schedule), and confirm one `eve/seo/YYYY-MM-DD-<slug>` draft PR from Connect.

## Phase 50: Owner-Directed Merge and Live Proposal Gate (2026-08-03)

- State: superseded by Phase 51 for the draft-PR path. Merge and Production deploy of the snapshot repair remain complete.
- Merge: PR [#96](https://github.com/byronwade/wadesplumbingandseptic.com/pull/96) was squash-merged at `2026-08-03T01:46:05Z` as commit `9bb3be55ba3a0da81ae9cc637f7c76fa9982a9d8`.
- Production deploy: GitHub Vercel status reached `Deployment has completed` for that commit as Production deployment `5719740753`. Public health/readiness still report `mode: observe`, `ready: true`, `configured_unverified`, and `mutation_kill_switch: true`.
- Correction: asking this cloud agent for Vercel MCP/CLI auth was the wrong handoff. Connect GitHub already posts the draft PR from the Eve runtime after Cron research once Production is in propose mode.
- Next exact action: complete Phase 51.

## Phase 49: Vercel Eve Server Snapshot Bundle Path Repair (2026-08-03)

- State: READY_FOR_HUMAN_REVIEW. Draft PR [#95](https://github.com/byronwade/wadesplumbingandseptic.com/pull/95) packaged the repository snapshot into `.output/server`, which is the local Nitro host path. The Vercel Eve build writes `.vercel/output/functions/__server.func` and Eve Services normalization relocates the durable shared function to `.vercel/output/functions/eve/__server.func`. Preview deployment `dpl_2AcEGumSDBnkzueobeQLAXVR1Lik` failed for that missing server root. This is `LIVE_VERIFIED` failure evidence for the pre-repair bundler path, not a credential issue.
- Correction: `bundle-repository-snapshot` now resolves Eve server roots in order: `eve/__server.func`, `__server.func`, then `.output/server`. It copies the attributable snapshot into every discovered root, keeps explicit overrides for fixtures, and fails closed when none exist. Regression tests cover the Vercel path preference, multi-root bundling, and the missing-output denial.
- Verification: with Node `v24.14.0`, sidecar `format:check`, `lint`, `typecheck`, and `test` exited `0` (`116/116` passing). `npm run build` staged snapshot commit `a073be6c111502973d46e1acb5d93ed326cafd4f`, built Eve `0.29.4`, and bundled into the local `.output/server` root.
- Canonical verification: `npm run verify:all` exited `0` after about `59.4s` and wrote `artifacts/verification/2026-08-03T012245151z-25d59583ed4a/`. Its evidence reports `24/24` passing checks, `MOCK_VERIFIED`, and commit `25d59583ed4a95362be4fac7312c5bb703d3484a`. `npm run verify:completion` exited `1` as intended because Definition-of-Done and independent-readiness live prerequisites remain incomplete.
- Verification classification: `MOCK_VERIFIED` for the path repair and local build. Preview packaging is now `LIVE_VERIFIED` for deployability: Vercel reported `Deployment has completed` for commit `844e8eac4528d20128431a0e1ed0a99adc730061` as deployment inspect URL `https://vercel.com/wades-web-dev/wadesplumbingandseptic-com/88pUXTbPTfeomZMGctchHGe8bM5z` with preview URL `https://wadesplumbingandseptic-c1towz50f-wades-web-dev.vercel.app`. Direct preview health probes returned HTTP `302` SSO redirects, so endpoint acceptance remains `BLOCKED_PREVIEW_SSO`. A production deployment and snapshot-backed audit remain required before audit evidence can be `LIVE_VERIFIED`.
- Review checkpoint: draft PR [#96](https://github.com/byronwade/wadesplumbingandseptic.com/pull/96) carries this repair on `cursor/eve-seo-agent-complete-aab8`. It supersedes the Preview packaging failure on draft PR [#95](https://github.com/byronwade/wadesplumbingandseptic.com/pull/95) without enabling mutation or publishing.
- Next exact action: obtain human review and merge of PR `#96`, confirm the production Eve build succeeds, invoke only the native audit Cron, and confirm the audit report uses `BUNDLED_GIT_SNAPSHOT` before any proposal workflow.

## Phase 48: Deployed Repository Snapshot and Session Budget Repair (2026-08-02)

- State: READY_FOR_HUMAN_REVIEW. The live audit Agent Run `wrun_41KZ2D4AYN0GNV22XCFMD9N5JQ` completed on deployment `dpl_8LwcUX8D4VqvQoYwPni4eMbdRZjR` at `2026-08-02T23:32:26.259Z`, but truthfully returned `BLOCKED_MISSING_CREDENTIALS`: the independently built Vercel Eve service did not have the Git-backed `app/`, `content/`, and `seo/` inputs needed to inventory the site. It created no content, branch, commit, or pull request. Its model invocation used `21,686` input tokens, exceeding the former local preflight ceiling of `16,000` even though the run itself completed.
- Correction: the Eve service build now stages a generated, ignored `repository-snapshot/` containing only `app/`, `content/`, `seo/`, the Next configuration, and optional `actions/`. The snapshot records the exact build commit and a SHA-256 content digest, then is copied inside the generated Eve Build Output server directory because current Vercel Services configuration does not accept a service-level `includeFiles` field. Runtime resolution now prefers that bundled snapshot and propagates its commit into the audit manifest. It never copies `.env*`, `.git`, dependencies, or public-site runtime code. The Eve root agent now declares a bounded `24,000` input-token, `1,200` output-token, five-minute session limit, matching the policy preflight input budget.
- Regression coverage: a new snapshot test proves the staged root is attributable, excludes `.env` and `.git`, and resolves as `BUNDLED_GIT_SNAPSHOT`. New tests assert Eve session limits are declared and match the request policy. The initial local test exposed the absent optional root `actions/` and an obsolete hard-coded `16,000` test threshold; both were corrected without weakening the runtime controls.
- Verification: with Node `v24.14.0`, `npm run format`, `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test` (`114/114` passing), `npm run test:integration` (`10/10` passing), and `npm run build` all exited `0` in `automation/seo-agent`. The build staged snapshot commit `5ed410151d10971918b61a3870d46c053c2d7491` with digest `0b67d1e4b12896415ef77039d3cc50076362549f7ef3e471dcce404b983a98a9` and Eve `0.29.4` build-summary verification passed. Root `npm run format:check`, `npm run lint`, `npm run typecheck`, and `npm run build` all exited `0`; the root production build generated `590` routes in `157.9` seconds.
- Packaging limitation: `npx vercel build --prod --yes` first rejected the invalid service-level `includeFiles` property, which led to the Build Output correction. The rerun accepted the repaired `vercel.json` but exited `1` after `147.9` seconds with `spawn cmd.exe ENOENT` while invoking the root `npm run build` from this Windows Vercel CLI environment. Direct root and sidecar builds passed; this is a local Vercel CLI shell limitation, not a failed Vercel configuration or code-build result. The CLI created ignored `.vercel/project.json` and an ignored local production environment download; neither is tracked or committed.
- Canonical verification: `npm run verify:all` exited `0` after `457.6` seconds and wrote `artifacts/verification/2026-08-03T003642401z-5ed410151d10/`. Its evidence reports `24/24` passing checks, `MOCK_VERIFIED`, commit `5ed410151d10971918b61a3870d46c053c2d7491`, and tree hash `83c9e8591a02ed86dabb104ff10cd912bcc13dfb24bb9de618683b9a97594037`. This status update is followed by a fresh canonical run before commit so its evidence will describe the final working tree.
- Current truth state: `MOCK_VERIFIED` for the repair and local sidecar build. The production service still runs the pre-repair deployment, so a fresh native-Cron audit on a deployed repair remains required before this is `LIVE_VERIFIED`.
- Next exact action: run the canonical repository verifier, commit the repair, have it reviewed and deployed normally, then invoke only the native audit Cron and confirm an attributed snapshot-backed audit report before any proposal workflow.

## Phase 47: Native Cron Services Route Repair (2026-08-02)

- State: READY_FOR_HUMAN_REVIEW. Production `d2c3197f48cf8116f8da4211f5678164d65ccebf` was deployed after the durable-handoff repair. A real Vercel native-Cron invocation at `2026-08-02T22:33:03.248Z` reached `/eve/v1/cron/wIQCJcoYJQBApnwAhG4IX5HtL49IJI7heDiAzS-6TU4`, but the production request log shows HTTP `404` from the public site and Vercel Agent Runs remain zero.
- Cause and correction: the one-project Services configuration routed only `/_internal/eve/*` to the sidecar. Eve generates native Cron metadata under `/eve/v1/cron/*`; without an explicit, ordered service rewrite, Vercel sends that callback to the site catch-all. The repair adds the narrow `/eve/v1/cron/*` route to `eve_seo_agent`, validates it in the deterministic deployment-configuration gate, and documents the opaque framework callback exception to the no-public-agent-route rule.
- Verification requirement: rerun the sidecar deployment-config, format, typecheck, test, build, and canonical verification commands before merge. The observed Cron trigger and public-site `404` are `LIVE_VERIFIED` failure evidence. No Agent Run, content mutation, branch, or draft content PR is claimed.
- Next exact action: merge and deploy this routing repair, trigger the same registered native Cron path, confirm a durable audit Agent Run, then enable only the exact one-time draft-PR controls for a human-reviewable blog post.

## Phase 46: Native Schedule Durable Handoff Repair (2026-08-02)

- State: READY_FOR_HUMAN_REVIEW. A real Production native-Cron invocation was accepted at `2026-08-02T18:48:35.600Z`, but Vercel recorded zero Eve Agent Runs. This is `FAILED` live evidence for the pre-repair schedule path, not a credential or approval failure.
- Cause and correction: the installed Eve `0.29.4` schedule API requires `waitUntil(receive(...))` to keep a schedule's durable handoff alive after the Cron task returns. `agent/schedules/audit.ts` had awaited `receive(...)` directly. The repair now registers that handoff with `waitUntil`, and a regression test asserts the supported API shape and trusted native target.
- Verification: `npm run format:check` exit `0`; `npm run typecheck` exit `0`; `npm run test` exit `0` with `110` passing tests; `npm run build` exit `0` and verified the compiled Eve manifest. These are `MOCK_VERIFIED` for the correction. The native Cron acceptance and zero Agent Runs are `LIVE_VERIFIED` observations of the defect.
- Production configuration: `SEO_AGENT_LIVE_READS_APPROVED=true`, `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID=audit-2026-08-02`, and `SEO_AGENT_NATIVE_SCHEDULE_JOB=audit` were set only to authorize the required first audit. They do not enable mutation or GitHub writes. The unchanged Production redeploy is `dpl_49m6nMkyqtQFBZkHMKyZLrUhovz2`.
- Next exact action: merge and deploy the schedule-handoff repair, trigger the one native Cron path again, confirm a durable audit Agent Run, and only then enable the exact one-time draft-PR gates for a human-reviewable blog-post PR.

## Phase 45: Single Native Proposal Proof Repair (2026-08-02)

- State: COMPLETE for the local implementation (`MOCK_VERIFIED`); deployment and the first live proposal proof remain pending human review of the repair PR.
- Changed scope: removed the second compiled `proposal-proof` Eve schedule. The Services deployment collapsed multiple Eve schedules onto the same internal Cron URL, so Vercel CLI path selection could not reliably choose the proposal proof. The retained native `audit` schedule now selects `audit` by default. A validated server-only `SEO_AGENT_NATIVE_SCHEDULE_JOB=proposal` setting can select the exact proposal job only while every existing one-time publishing approval and reviewed-audit gate also passes. The proposal descriptor now reports the actual native schedule cadence, and the override must be restored to `audit` after the run.
- Security boundary: no public route, Cron-secret bypass, direct-main write, merge, deployment, or automatic publishing authority was added. The protected `/api/cron` route still performs timing-safe `CRON_SECRET` verification. The native schedule accepts only its compiled source and rejects an unsupported job before a durable session begins.
- Commands: `npm --prefix automation/seo-agent run ci:offline` -- exit `0`; `npm run verify:all` -- exit `0` -- `24/24` required root and sidecar checks passed; `git diff --check` -- exit `0`. An earlier canonical attempt exposed and then corrected one phase-caused root Markdown-formatting failure; no test or gate was weakened.
- Evidence: `automation/seo-agent/agent/schedules/audit.ts`, `automation/seo-agent/agent/channels/runtime.ts`, `automation/seo-agent/src/config.mjs`, `automation/seo-agent/src/runtime.mjs`, `automation/seo-agent/scripts/verify-eve-manifest.mjs`, and their focused regression tests.
- Verification classification: `MOCK_VERIFIED`. The deployed Production service remains observe-only with mutation disabled and the kill switch enabled. Vercel Agent Runs remain zero, so no live Eve workflow, content change, branch, or draft PR is claimed.
- Next exact action: commit and open a repair PR, wait for a human merge and Vercel deployment, then use the one native Cron path with the exact temporary proposal gates for a single lifecycle proof and immediately restore the audit-only configuration.

## Phase 44: Platform-Authenticated Proposal Proof Trigger (2026-08-01, superseded)

- State: SUPERSEDED by Phase 45 after live Vercel Cron inspection showed that this Services deployment registered the two compiled schedules on one internal path.
- Changed scope: this historical phase added a separately compiled Eve schedule named `proposal-proof`. It was replaced before any proposal invocation because the Vercel CLI path selector could not reliably target it.
- Why this is needed: Vercel CLI can manually invoke only a deployed registered Cron path. The existing protected HTTP dispatcher correctly requires the non-readable `CRON_SECRET`, and this execution environment cannot retrieve or forward that secret. The compiled schedule allows a Vercel-authenticated manual run without weakening the HTTP dispatcher or exposing a public/admin route.
- Commands: `npm --prefix automation/seo-agent run format:check` -- exit `0`; `npm --prefix automation/seo-agent run lint` -- exit `0`; `npm --prefix automation/seo-agent run typecheck` -- exit `0`; `npm --prefix automation/seo-agent run test` -- exit `0` -- `109` tests passed in `9.001s`, including missing and future audit-precondition denial; `npm --prefix automation/seo-agent run build` -- exit `0` -- `56.5s`, with both audit and proposal-proof schedule artifacts compiled and the manifest verified; `git diff --check` -- exit `0`.
- Evidence: `automation/seo-agent/agent/schedules/proposal-proof.ts`, `automation/seo-agent/agent/channels/runtime.ts`, `automation/seo-agent/src/runtime.mjs`, `automation/seo-agent/scripts/verify-eve-manifest.mjs`, `automation/seo-agent/tests/runtime.test.mjs`, and `automation/seo-agent/tests/eve-build-summary.test.mjs`.
- Verification classification: `MOCK_VERIFIED` for source binding, schedule compilation, and fail-closed authorization tests. `LIVE_VERIFIED` remains limited to the already deployed health surface and restored observe-mode configuration. No live proposal result exists yet.
- Review checkpoint: draft PR [#91](https://github.com/byronwade/wadesplumbingandseptic.com/pull/91) targets `main` from `feat/eve-platform-proposal-trigger` at commit `555d7046384ba77ea14fa4857f64efedfe7da7c3`. Its current clean-tree verification artifact is `artifacts/verification/2026-08-01T220427287z-555d7046384b` with `24/24` checks passed, no critical/high findings, and the expected external live-integration prerequisites. This documentation update makes that prior artifact stale and requires regeneration before merge.
- Next exact action: see Phase 45.

## Phase 43: One-Time Eve Draft-Proposal Workflow (2026-08-01)

- State: COMPLETE for the locally verified implementation (`MOCK_VERIFIED`). Implementation PR [#89](https://github.com/byronwade/wadesplumbingandseptic.com/pull/89) was human-merged to `main`; follow-up draft PR [#90](https://github.com/byronwade/wadesplumbingandseptic.com/pull/90) contains the dispatcher-truthfulness and eval-stability corrections and remains disabled until a human merges and Vercel deploys it.
- Changed scope: added a protected `proposal` runtime job, an Eve workflow entry point, exact-run publication authorization, a Terra writer, independent QA judge, bounded Markdown change-set creation, a scoped Vercel Connect draft-write token provider, pinned-main branch creation, and draft-PR creation. The native Eve schedule remains audit-only. The proposal path has no merge, main write, force push, deletion, deployment, secret, or repository-settings capability. The local Eve eval runner and configuration now share a bounded 30-second cold-start timeout, with a regression test that prevents them from drifting apart.
- One-time controls: the deployed run requires `SEO_AGENT_RUN_MODE=propose`, `SEO_AGENT_MUTATION_MODE=enabled`, `SEO_AGENT_MUTATION_KILL_SWITCH=false`, `SEO_AGENT_PUBLISHING_HUMAN_APPROVED=true`, `SEO_AGENT_PUBLISHING_INTEGRATION_TEST=true`, and `SEO_AGENT_PUBLISHING_APPROVED_RUN_ID` exactly equal to the deterministic proposal run ID. Any mismatch is rejected before an AI or GitHub write.
- Commands: `npm run test` in `automation/seo-agent` — exit `0` — `108` tests passed in `3.083s`; `npm run format:check`, `npm run lint`, `npm run typecheck`, and `npm run build` — exit `0` — `30.4s` combined. The focused protected-dispatch regression passed inside that suite: an accepted `proposal` job returns `audit_only: false`, while audit jobs return `true`. Canonical root `npm run verify:all` must be rerun from this follow-up commit so its commit SHA and clean tree hash match exactly.
- Evidence: `automation/seo-agent/src/proposal.mjs`, `automation/seo-agent/src/workflows/proposal.ts`, `automation/seo-agent/src/runtime.mjs`, `automation/seo-agent/src/publishing.mjs`, `automation/seo-agent/src/adapters.mjs`, and `automation/seo-agent/tests/proposal.test.mjs`. Tests prove only `read main → create guarded feature branch → stage bounded Markdown → draft PR` can occur, that the GitHub Connect request is limited to the target repository and fixed draft-write scopes, and that independent QA rejection results in zero publisher calls.
- Verification classification: MOCK_VERIFIED for the complete fixture lifecycle and local build. The initial checkpoint commit `7e69815782c9c5512dbc5167b624d48e991bf011` has a clean `24/24` verification artifact at `artifacts/verification/2026-08-01T200806695z-7e69815782c9`. A fresh canonical artifact is required after this dispatch-truthfulness follow-up commit. No live model request, GitHub write, Eve Agent Run, generated-content PR, content change, merge, deployment, or Production environment modification has occurred in this phase.
- Failures / limitations: the current first proof draft deliberately uses a conservative runtime context and a single home-page internal link when the service does not have a checkout of the public repository. It is appropriate for the one-time proof only; a subsequent research-backed production proposal must use a versioned full inventory and real approved evidence. CI/preview observation after draft PR creation remains the existing lifecycle contract and is not yet wired into this minimal first-proof execution path.
- Next exact action: wait for PR #90 CI and preview gates, then have a human merge it. After Vercel deploys the merged implementation, authorize one exact proposal run, inspect the resulting Agent Run and draft content PR, and immediately return the production configuration to observe mode with mutation disabled and the kill switch enabled.

## Phase 42: GitHub Connect Live Verification and Scope Repair (2026-08-01)

- State: Production deployment and the read-only GitHub Connect token exchange are `LIVE_VERIFIED`. The scope repair is implemented and test-verified locally, but it is not deployed until its separate corrective PR is merged.
- Live evidence: PR `#87` merged at `2026-08-01T18:58:20Z` as `b8f5758429a6e2752074ff2e6349bc949b01cb17`. Vercel Production deployment `dpl_FHH2cxnfRH5RTKnaPQFFp8Vq8KNu` is `Ready`, its build log confirms that exact merge commit, and public `/_internal/eve/api/healthz` plus `/_internal/eve/api/readyz` both returned HTTP `200`, `mode: observe`, `ready: true`, and an enabled mutation kill switch. `vercel agent-runs list` returned no Agent Runs.
- GitHub evidence: a short-lived Vercel OIDC identity from the linked Wade's project successfully obtained an app token from `github/wadesplumbingandseptic-com`. A read-only GitHub installation request returned exactly one repository, `byronwade/wadesplumbingandseptic.com`, when Vercel Connect authorization details requested `contents:read` for that repository. No token, credential, Git branch, content, PR, merge, or issue write was printed or performed.
- Finding and repair: the existing provider requested an unscoped GitHub app token. Its installation listing exposed more repositories than this service needs. `automation/seo-agent/src/adapters.mjs` now requires one `owner/repository` restriction and requests only `contents:read` plus `pull_requests:read`; the registry passes the configured Wade's repository. Regression tests reject an unscoped provider and assert the exact authorization details.
- Commands: linked this checkout to `byron-wade/wadesplumbingandseptic-com` using `vercel link`; `getToken` with Vercel OIDC and a scoped GitHub authorization request -- exit `0` -- `LIVE_VERIFIED`; production deployment, health, readiness, and Agent Run inspection -- exit `0`; `npm --prefix automation/seo-agent run format:check`, `lint`, `typecheck`, and `test` -- exit `0` -- `104` tests passed after formatting repair.
- Canonical evidence: `npm run verify:all` -- exit `0` -- `24/24` checks passed. The evidence bundle is `artifacts/verification/2026-08-01T190904572z-8edf299331d5`, bound to commit `8edf299331d5006a1d6da6449f495ee1b393ae57` and clean working-tree hash `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. It must be regenerated after this documentation checkpoint.
- External blocker: Production remains observe-only, has no Agent Run, and has not yet performed a live audit. The installed GitHub App itself should also be restricted to only the Wade's repository in GitHub before any future draft-writing workflow is enabled, even though the corrective code independently scopes each issued token.
- Next exact action: run the full deterministic verifier, open and merge the scoped-token corrective PR, then authorize one exact read-only Production audit run and inspect its redacted Eve trace. Do not enable content publishing as part of the audit.

## Phase 41: Live Audit Runtime Activation (2026-08-01)

- State: COMPLETE for the controlled runtime implementation (`MOCK_VERIFIED`). Production remains observe-only and has not been redeployed or invoked by this phase.
- Changed scope: replaced the production audit placeholder with the configured, bounded read-only audit executor. An exact approved Production run now collects typed adapter evidence, creates a redacted immutable audit-manifest proposal, and returns it through the durable Eve session. It cannot write Git, create a PR, modify content, merge, deploy, archive to Blob, or disable the mutation kill switch. AI Gateway model-catalog probing now forwards only a supplied runtime OIDC or Gateway credential in an Authorization header, never logs it.
- Commands: `npm run format:check; npm run typecheck; node --test tests/runtime.test.mjs tests/adapters.test.mjs tests/audit.test.mjs` in `automation/seo-agent` with Node `v24.14.0` -- exit `0` -- `5.61s` -- formatting and typecheck passed with `36` focused tests. `npm run ci:offline` in `automation/seo-agent` with Node `v24.14.0` -- exit `0` -- approximately `55s` -- lint, all formatting gates, typecheck, static contracts, secret scan, `104` unit tests, `10` runtime integration tests, `15` Eve evals with `50` gates, audit fixture, human-only rollback drill, independent review, and Eve build/manifest verification passed.
- Evidence: `automation/seo-agent/src/runtime.mjs`, `automation/seo-agent/src/adapters.mjs`, `automation/seo-agent/tests/runtime.test.mjs`, and `automation/seo-agent/tests/adapters.test.mjs`. The new regression fixture supplies a matching Production approval and one bounded model-catalog response, proves `LIVE_VERIFIED` evidence may be returned, and proves zero content files, zero draft PRs, and `write_performed=false`.
- Verification classification: `MOCK_VERIFIED` for the end-to-end runtime and authenticated-header fixture. No cloud integration, workflow, provider request, GitHub write, Blob write, deployment, or production content mutation was performed.
- Canonical evidence: `npm --prefix automation/seo-agent run evals` -- exit `0` -- `15/15` evals and `50/50` gates passed; `npm run verify:all` -- exit `0` -- `24/24` checks passed. The evidence bundle is `artifacts/verification/2026-08-01T183008410z-487ec6dd9973`, bound to commit `487ec6dd99738e3d529235390a899c92bf5d0468` and clean working-tree hash `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. This artifact becomes stale after this documentation change and must be regenerated for the checkpoint commit.
- Review checkpoint: draft PR `#87` (`feat/eve-live-audit-runtime` to `main`) contains this audit-only runtime. `npm run verify:completion` -- exit `1` as intended: it rejects incomplete Definition-of-Done items and the independent readiness review. It must not pass until a human merges the draft, the observe-only Production deployment is verified, and an approved exact read-only run has redacted live evidence.
- Failures / limitations: the local Eve evaluator emitted one post-success world-local queue cleanup retry (`socket hang up`) after all `15/15` evals completed. The command exited `0`; no external provider call occurred. This does not establish a live workflow run.
- External blocker: live Vercel read inspection still requires the separate least-privilege `VERCEL_READ_TOKEN`, project ID, and team ID. Browser automation and optional provider adapters remain disabled. These individual blocks do not prevent a core audit from returning the available integration classifications.
- Next exact action: obtain human review and merge of draft PR `#87`, deploy the approved Production build with a one-time exact live-read approval, inspect the scheduled Eve Agent Run and its redacted report, then reset the approval. Keep all publishing controls disabled.

## Phase 40: Authorized Production Audit Attempt and Safe Reversion (2026-08-01)

- State: `BLOCKED_MISSING_CREDENTIALS` for credential-bearing provider verification. No audit workflow, provider request, content change, pull request, merge, or deployment occurred.
- Changed scope: performed and immediately reversed the owner-authorized, Production-only one-time read-authorization setup for run ID `production-audit-2026-08-01`. Observe mode, disabled mutation, the enabled kill switch, disabled publishing, disabled Blob archival, and disabled Sandbox approval were never changed.
- Commands: Vercel CLI `env add` set only the intended read flags and exact one-time approval, then `env pull` and `env run` each attempted `node scripts/probe-integrations.mjs --execute --run-id=production-audit-2026-08-01` -- both exit `1` before adapter dispatch -- Vercel returned non-readable placeholders for Sensitive Production values, so schema validation rejected the masked flags and credentials. Vercel CLI `env add` restored the read flags and approval to `false`; `env rm SEO_AGENT_LIVE_READS_APPROVED_RUN_ID production --yes` -- exit `0` -- removed the one-time authorization. The ignored local environment export was removed. Public health and readiness GETs -- HTTP `200` -- retain `mode: observe`, `ready: true`, `configured_unverified`, and `mutation_kill_switch: true`. `vercel agent-runs list --environment production` -- exit `0` -- reports zero runs, zero tokens, and zero cost.
- Evidence: Vercel documents that Sensitive Production values are non-readable after creation and only available at build/function runtime. The local CLI cannot safely recover `CRON_SECRET` either, and the protected dispatcher intentionally accepts only that value. Production environment changes apply only to a new deployment, so the existing deployment never inherited the temporary flags. This is correct credential containment, not a provider failure.
- Verification classification: `LIVE_VERIFIED` for the temporary-setting reversion, protected observe-only health surface, and zero Agent Runs. All credential-bearing adapters remain `BLOCKED_MISSING_CREDENTIALS`; no adapter may be reported `LIVE_VERIFIED` from this attempt.
- Failures / limitations: no safe, manual, credentialless trigger exists for a one-off Production Cron dispatch. A deployed function can receive sensitive values, but current Vercel Cron dispatch is intentionally authenticated by a non-readable secret and its regular schedule is the only configured production invoker. Do not weaken Cron authentication, make sensitive values readable, or add a public/admin agent route to bypass this boundary.
- Next exact action: approve a dedicated, non-public, least-privilege manual audit trigger design or deploy approved Production read flags and wait for the existing Cron schedule, then inspect the resulting Eve Agent Run and redacted evidence before resetting the flags. Either option still requires observe mode and the mutation kill switch.

## Phase 39: Post-Merge Verification and Audit Gate (2026-08-01)

- State: `BLOCKED_MISSING_CREDENTIALS` for the first production audit authorization. This phase made no provider request, content change, pull request, merge, deployment change, or production configuration change.
- Changed scope: recorded current merged repository, deployed observe-only service, deterministic verification, and live-read authorization state after branch and pull-request cleanup.
- Commands: `git fetch origin --prune; git status --short --branch; git rev-parse HEAD; git rev-parse origin/main; gh pr list --state open; git branch -r; git worktree list` -- exit `0` -- `1.95s` -- `origin/main` and the clean worktree resolve to `845c3b9246674b2a35bb44fb3d2ba763f9c81909`, no open pull requests remain, and the remote retains only `main`; `npx vercel inspect https://www.wadesplumbingandseptic.com --scope byron-wade` -- exit `0` -- `4.09s` -- production deployment `dpl_9sw3zYnU7xDXtWYFGvhVCiDsGMzQ` is `Ready` and exposes Eve functions only below the internal Services prefix; public health/readiness probes -- exit `0` -- `1.40s` -- returned HTTP `200`, observe mode, `configured_unverified`, readiness true, and the enabled kill switch; `npm run verify:all` -- exit `0` -- `133.167s` -- `24/24` checks passed with artifact `artifacts/verification/2026-08-01T164512583z-845c3b924667`; `npm run verify:completion` -- exit `1` as expected -- incomplete Definition-of-Done and independent readiness; `npm --prefix automation/seo-agent run live:probe` -- exit `0` -- offline-plan-only result with no network request.
- Evidence: verification artifact binds commit `845c3b9246674b2a35bb44fb3d2ba763f9c81909` and clean working-tree hash `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. The public runtime reports `mode: observe`, `ready: true`, and `mutation_kill_switch: true`. This is `LIVE_VERIFIED` only for public deployment/readiness behavior and `MOCK_VERIFIED` for deterministic checks.
- External blocker: a fresh redacted Production configuration pull confirms `SEO_AGENT_LIVE_READS_APPROVED=false`, no `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID`, `SEO_AGENT_ENABLE_VERCEL=false`, `SEO_AGENT_ENABLE_SEARCH_CONSOLE=false`, `SEO_AGENT_ENABLE_PAGESPEED=false`, and `SEO_AGENT_ENABLE_BROWSERBASE=false`. AI Gateway and GitHub flags are configured, but the shared one-time live-read authorization still blocks every provider operation. Observe mode, disabled mutation, disabled publishing, the enabled kill switch, disabled Blob, and disabled Sandbox approval remain intact.
- Next exact action: the owner must choose one lower-case audit run ID, enable only the intended read adapters in Production, and set `SEO_AGENT_LIVE_READS_APPROVED=true` plus the matching `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID`. Keep `SEO_AGENT_RUN_MODE=observe`, `SEO_AGENT_MUTATION_MODE=disabled`, `SEO_AGENT_MUTATION_KILL_SWITCH=true`, and all publishing flags false. Then run `npm --prefix automation/seo-agent run live:probe -- --execute --run-id=<exact-run-id>` from the approved Production environment, retain redacted evidence, and immediately reset the two authorization values after review.

## Phase 38: Control-Plane Command Coverage (2026-08-01)

- State: COMPLETE (`MOCK_VERIFIED`). No provider request, publishing capability, deployment, or production configuration changed.
- Changed scope: added the missing root `seo-agent:control-plane` command, made `seo-agent:verify` run it before the broader offline verifier, added it to canonical verification evidence, and added a regression assertion in the self-hosted workflow contract test.
- Commands: before repair, `npm run seo-agent:control-plane` -- exit `1` -- missing script; after repair, `npm run seo-agent:control-plane` -- exit `0` -- verified `23` required control-plane files and the bounded enabled hook; `npm --prefix automation/seo-agent run test` -- exit `0` -- `104` tests passed; `npm run seo-agent:verify` -- exit `0` -- sidecar architecture, source, security, runtime, integration, `15` Eve evals with `50` gates, audit-only fixture, rollback drill, independent review, and Eve build passed; `npm run verify:all` -- exit `0` -- `24/24` checks passed in about `144s`, with evidence at `artifacts/verification/2026-08-01T160853890z-ae508551a828`; `npm run verify:completion` -- exit `1` as expected because the independent review correctly lists seven unverified live-integration and audit-only prerequisites; `npm run format:check` and `git diff --check` -- exit `0`.
- Evidence: the full offline verifier continues to report the independent review as `NOT_READY` only for explicitly unverified external integrations. This is correct `MOCK_VERIFIED` deterministic evidence, not a live operational claim.
- Failures / limitations: Eve emitted a post-success local queue cleanup retry with `socket hang up`; the completed command still exited `0`, and no provider or content action occurred. The first production audit remains blocked by the exact human authorization controls recorded in Phase 37.
- Next exact action: run `npm run verify:all` from the resulting clean checkpoint to regenerate canonical evidence, then commit and push this documentation and contract repair. Do not enable publishing or mutation.

## Phase 37: Production Configuration Reconciliation (2026-08-01)

- State: `BLOCKED_MISSING_CREDENTIALS` for the first live audit authorization. This is a configuration-state finding, not a provider failure and not a production mutation.
- Changed scope: clarified the owner setup checklist so that enabling credentials and enabling the corresponding read adapters are separate, explicit actions.
- Commands: `npx vercel whoami` and `npx vercel projects ls --scope byron-wade` -- exit `0` -- authenticated as the owner identity and located Production project `byron-wade/wadesplumbingandseptic-com`; `npx vercel env pull automation/seo-agent/.env.oidc.local --environment=production --yes --scope byron-wade --project wadesplumbingandseptic-com` -- exit `0` -- `3.48s`; safe redacted configuration inventory -- exit `0` -- `0.36s`; public `GET /_internal/eve/api/healthz` and `GET /_internal/eve/api/readyz` -- HTTP `200` -- each remains `ready: true`, `mode: observe`, `configured_unverified`, with required Cron authentication and the mutation kill switch enabled.
- Evidence: the fresh scoped Production pull confirms OIDC, Google service-account values, and the PageSpeed key are present without revealing them. It also confirms `SEO_AGENT_LIVE_READS_APPROVED=false`, no `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID`, `SEO_AGENT_ENABLE_AI_GATEWAY=true`, `SEO_AGENT_ENABLE_GITHUB=true`, and `SEO_AGENT_ENABLE_VERCEL=false`, `SEO_AGENT_ENABLE_SEARCH_CONSOLE=false`, `SEO_AGENT_ENABLE_PAGESPEED=false`, and `SEO_AGENT_ENABLE_BROWSERBASE=false`.
- Verification classification: `LIVE_VERIFIED` for Vercel project discovery and deployed health endpoints. The first audit run remains `BLOCKED_MISSING_CREDENTIALS` because its explicit one-time authorization does not exist.
- Failures / limitations: an unscoped `vercel env pull` correctly failed with `not_linked`; the scoped project command is the reproducible read-only command for this checkout. The AI Gateway dashboard demonstrates account/project traffic but is not evidence that the current sidecar has been authorized to invoke every adapter.
- Next exact action: an authorized human must select a lower-case run ID, set `SEO_AGENT_LIVE_READS_APPROVED=true` plus the matching `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID`, and enable only the adapters being tested: Vercel, Search Console, and PageSpeed. Keep `SEO_AGENT_RUN_MODE=observe`, `SEO_AGENT_MUTATION_MODE=disabled`, `SEO_AGENT_MUTATION_KILL_SWITCH=true`, publishing disabled, Blob disabled, and Sandbox approval disabled. Then run the exact read-only live probe and reset the two authorization variables after reviewing its redacted report.

## Phase 36: Production Read-Authorization Diagnostic (2026-08-01)

- State: `BLOCKED_MISSING_CREDENTIALS` for live audit authorization, intentionally. The deployed sidecar itself is healthy and no production content or provider state changed.
- Live evidence: at `2026-08-01T15:29Z`, production `GET /_internal/eve/api/healthz` and `GET /_internal/eve/api/readyz` each returned HTTP `200`. Both reported `ready: true`, `mode: observe`, `cron_secret_required: true`, a closed circuit with zero failures, `integration_classification: CONFIGURED_UNVERIFIED`, and `mutation_kill_switch: true`.
- Configuration evidence: the encrypted Production configuration has the reviewed non-secret variable names for Cron, OIDC, Google service account, PageSpeed, Blob, and all adapter flags. Its safe control values are `SEO_AGENT_ENV=production`, `SEO_AGENT_RUN_MODE=observe`, `SEO_AGENT_MUTATION_MODE=disabled`, `SEO_AGENT_MUTATION_KILL_SWITCH=true`, `SEO_AGENT_LIVE_READS_APPROVED=false`, no `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID`, `SEO_AGENT_PUBLISHING_HUMAN_APPROVED=false`, `SEO_AGENT_PUBLISHING_INTEGRATION_TEST=false`, `SEO_AGENT_BLOB_ENABLED=false`, and `SEO_AGENT_SANDBOX_INTEGRATION_APPROVED=false`. No secret value was printed or committed.
- Result: the exact-run authorization guard correctly blocks every provider read. It would be unsafe to bypass this with a direct local request or to change production approval flags without a human-approved run identifier.
- Next exact action: a human must choose a new lower-case run ID such as `production-audit-2026-08-01`, set `SEO_AGENT_LIVE_READS_APPROVED=true` and `SEO_AGENT_LIVE_READS_APPROVED_RUN_ID` to that exact value in the Production Eve environment, and explicitly authorize one audit-only read. Keep observe mode, mutation mode `disabled`, the kill switch `true`, publishing approval `false`, integration-test `false`, Blob disabled, and Sandbox approval `false`. Then run `npm --prefix automation/seo-agent run live:probe -- --execute --run-id=<exact-run-id>` from the approved production sidecar environment, retain only redacted evidence, and reset the two live-read approval variables immediately afterward.

## Phase 35: Completion-Evidence Integrity (2026-08-01)

- State: COMPLETE for deterministic verification hardening (`MOCK_VERIFIED`). This phase does not alter a provider, deployment, Connector, publishing mode, or agent authority.
- Changed scope: canonical `verify:all` now runs the sidecar integration tests, dependency audit, sandbox integration classification, observability fixture, rollback drill, and independent readiness review in addition to its existing root and sidecar gates. Its evidence records the independent review, every unclassified integration, incomplete Definition-of-Done entries, review-derived high or critical findings, and external prerequisites. `verify:completion` now rejects missing or non-ready independent-review evidence, unclassified integration evidence, and incomplete DoD entries. New contract tests prove that a fully classified ready record passes and that an incomplete review cannot pass.
- Commands: `npm run test:verification-core && npm run format:check && npm run lint && npm run typecheck && git diff --check` -- exit `0` -- about `53s`; `npm run verify:all` -- exit `0` -- `147.4s`, with `23/23` checks passed and evidence at `artifacts/verification/2026-08-01T152446423z-327d9e3691fe`; `npm run verify:completion` -- exit `1` as expected.
- Truth boundary: the expanded evidence reported zero unclassified integrations and zero high or critical findings. Completion correctly rejected the seven required integrations represented by the current inventory as not `LIVE_VERIFIED`: Vercel Eve and AI Gateway, Vercel Sandbox, GitHub Connect, Vercel read, Search Console, PageSpeed, and browser research. This repair makes that distinction machine-enforced.
- Next exact action: regenerate fresh evidence after this status checkpoint, push it to draft PR `#85`, allow its normal self-hosted CI and preview to finish, then reconcile the integration inventory through new redacted live probes. Do not enable publishing or automatic merge.

## Phase 34: Cross-Platform Eve Build and Verification Repair (2026-08-01)

- State: COMPLETE (`MOCK_VERIFIED`). The public-site and sidecar verification system now runs the real root npm checks on Windows and native Eve compilation/evaluations no longer fail from a Windows ESM path defect.
- Changed scope: `scripts/verification-core.mjs` resolves the Windows npm command shim through the active Node npm CLI when available, with a direct shell fallback for non-lifecycle invocation. `scripts/verification-core.test.mjs` proves that real `npm --version` execution succeeds. The canonical verifier includes that regression test before all other gates. The Eve agent no longer externalizes `@vercel/connect`: Eve bundles the approved typed Connect client into its backend output instead of generating an invalid raw `C:\\...` ESM import. No Connect scope, credential, model, tool, endpoint, or publishing permission changed.
- Commands: `npm run test:verification-core` -- exit `0` -- `1` passing test; `npm --prefix automation/seo-agent run format:check && npm --prefix automation/seo-agent run lint && npm --prefix automation/seo-agent run typecheck && npm --prefix automation/seo-agent run test && npm --prefix automation/seo-agent run evals && npm --prefix automation/seo-agent run build` -- exit `0` -- about `50s`; `npm run verify:all` -- exit `0` -- `156.5s`.
- Evidence: `artifacts/verification/2026-08-01T150847522z-14de4c87073d/results.json` and `summary.md` recorded `17/17` passed checks, no failed checks, and `MOCK_VERIFIED` classification. The sidecar build produced the backend-only Eve output and verified its manifest on Node `24.14.0`; root formatting, lint, typecheck, SEO/search/archive gates, production build, sidecar tests/evals/build, security scan, audit fixture, and health smoke all passed.
- Failures / limitations: this is deterministic local evidence only. It does not verify a live provider call, a GitHub write, a content change, or a pull request. The publisher remains disabled and `BLOCKED_MISSING_CREDENTIALS` until an explicitly human-approved disposable write test is performed with least-privilege Connect permissions.
- Next exact action: commit this repair to draft PR `#85`, allow normal self-hosted CI and the preview to complete, and keep the PR draft. After human review and merge, perform only the separately approved audit-only production workflow invocation; do not enable publishing or automatic merge.

## Phase 33: Terra Model Availability and Fallback (2026-08-01)

- State: `openai/gpt-5.6-terra` is `LIVE_VERIFIED` through Vercel OIDC. It remains the centralized orchestration and writing primary. `openai/gpt-4.1-mini` is the approved availability fallback because it has also completed an OIDC probe. This adds no mutation authority and starts no Eve audit session.
- Live evidence: at `2026-08-01T14:49:15Z`, a bounded OIDC request used `openai/gpt-5.6-terra` as the primary route with `openai/gpt-4.1-mini` as its Gateway fallback. It returned the exact expected response, reported `model_id: openai/gpt-5.6-terra`, and used `18` input tokens with `9` output tokens. The earlier Terra HTTP `403` remains a historical `FAILED` observation, but the newest direct response is the authoritative current route state.
- Completed work: model routing now keeps Terra first for orchestration and writing, uses the live verified mini route as their fallback, and retains Terra then mini as independent QA fallback options after the independent judge route. The policy fixture asserts the exact primary and fallback relationship. `npm run format`, `npm run test:policy` (`12` passing), and `npm run test` (`104` passing) exited `0`.
- Next exact action: merge draft PR `#85` only after its normal self-hosted CI and preview pass. Then perform one explicitly human-approved, audit-only production workflow invocation using the verified model route. It must create zero content changes and zero pull requests before any proposal-mode or publishing work is considered.

## Phase 31: Guarded GitHub Draft Publisher (2026-08-01)

- State: COMPLETE for the disabled publishing adapter (`MOCK_VERIFIED`). The adapter is not exposed to Eve model tools and no real branch, commit, pull request, label, comment, merge, deployment, or production configuration changed.
- Completed work: added `createGithubDraftPublisher` in `automation/seo-agent/src/publishing.mjs`. It receives only a short-lived Vercel Connect token provider, validates Markdown change sets, permits only `eve/seo/YYYY-MM-DD-<slug>` branches, reads the existing feature ref, creates one Git tree and one attributed commit, advances only that feature ref with `force: false`, and creates only a draft pull request against `main`. It cannot update `main`, force push, delete, merge, deploy, alter settings, or access secrets.
- Regression evidence: the new mocked end to end policy test proves a disabled publisher obtains no token and makes no request. With all four typed gates enabled, it permits only `GET`, `GET`, `POST`, `POST`, `PATCH`, and `POST` to the reviewed GitHub endpoints. The test asserts the ref update uses `force: false`, the pull request is draft-only against `main`, and no request touches `main` or uses `DELETE`.
- Verification: `npm run format` and `npm run test:policy` exited `0` in `4.3s` with `12` policy tests passing. `npm run lint`, `npm run typecheck`, `npm run test` (`104` passing), `npm run verify`, and `npm run test:integration` (`10` passing) also exited `0`. `npm run build` exited `1` only because the existing local Windows Eve compiler treats a `C:` path as an invalid ESM URL while evaluating `agent/tools/orchestrate.ts`. This compiler limitation is not caused by the publisher and needs clean self-hosted CI confirmation. `git diff --check` exited `0` before this status update.
- External blocker: the installed Vercel Connect GitHub App is live verified for repository reads only. No least-privilege write permission or human-approved disposable write test exists, so the real adapter remains `BLOCKED_MISSING_CREDENTIALS` and all runtime publishing flags remain disabled.
- Next exact action: allow draft PR `#85` checks and preview to finish, then merge it through normal human review. After a human approves a disposable test, grant the Connect GitHub App only Contents read and write, Pull requests read and write, and Issues read and write for this repository. Then run exactly one disposable branch and draft PR probe with the existing integration-test flag, record its redacted evidence, and immediately keep production publishing disabled.

## Phase 30: Live Draft-Content Capability Assessment (2026-08-01)

- State: Vercel AI Gateway authentication is `LIVE_VERIFIED` through Vercel OIDC. The conflicting `AI_GATEWAY_API_KEY` override was removed from the Preview and Production project scopes. The centralized orchestration and writing route is `openai/gpt-5.6-terra`, confirmed in the live Gateway catalog and subsequently `LIVE_VERIFIED` by a bounded OIDC response. A real blog-post draft PR remains blocked because the guarded publisher is not yet merged or enabled. No live Eve session, Git branch, content change, or pull request was started by this assessment.
- Live AI evidence: a fresh production-scoped OIDC-only environment pull at `2026-08-01T14:38:16Z` contained no static Gateway key and successfully completed a minimal `openai/gpt-4.1-mini` request using `17` input tokens and `8` output tokens. The earlier minimal probe returned the expected response at `2026-08-01T14:18Z`, using `18` input tokens and `9` output tokens. The former configured premium `openai/gpt-5.4` route returned a truthful `FAILED` HTTP `403` because the current AI Gateway tier restricted that model. The requested Terra route initially returned `FAILED` HTTP `403`, then completed the newer bounded OIDC request recorded in Phase 33. OIDC authentication is not the limiting factor.
- Production evidence: the human merge of PR `#84` produced commit `dafe36c3012b91c82a747d9beff4d339c060c67d`, deployed as Vercel production deployment `dpl_jrQmpaSE9qc4Y89c4jSa8knyFdTv` at `2026-08-01T14:41:22Z`. Read-only `GET /_internal/eve/api/healthz` and `GET /_internal/eve/api/readyz` both returned HTTP `200` after deployment with `status: configured_unverified`, `mode: observe`, `ready: true`, `integration_classification: CONFIGURED_UNVERIFIED`, and `mutation_kill_switch: true`. The health code recognizes Vercel's injected OIDC request signal without logging a token. This is `LIVE_VERIFIED` deployment and runtime-health evidence, not a model, audit, or publishing run.
- Code evidence: `automation/seo-agent/agent/tools/github.ts` exposes the GitHub `repo-explorer` read-only preset only. `agent/tools/orchestrate.ts`, `src/runtime.mjs`, and the runtime channel hard-code audit-only execution. No deployed tool can create a branch, commit Markdown, or open a draft PR. The `propose` mode label does not change that current behavior.
- Documentation: root `README.md` now distinguishes Eve's verified audit, OIDC model-access, and policy capabilities from integrations or actions that remain unavailable.
- Verification: on `2026-08-01T14:31:52Z`, `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test` (`103` passing), `npm run verify`, and `git diff --check` all exited `0` locally. `npm run evals` could not initialize Eve because its Windows compiler attempted to load a `C:` absolute path as an ESM URL; this is the existing local Windows Eve compiler limitation, not a model-policy failure, and needs clean self-hosted CI confirmation. The pulled production environment file is ignored and was not staged or committed.
- Required next action: wait for the Phase 31 publisher checkpoint to pass review and be merged. Then run one human-approved audit-only production workflow with the verified model route. Only after its findings are reviewed should the guarded publisher be live tested against one disposable branch and draft pull request, retaining the mutation kill switch, one-PR and one-URL budget, nondefault `eve/seo/` branch requirement, draft-only PR setting, complete evidence packet, and no merge capability.

## Phase 29: Production Runtime Probe and Image-Delivery Repair (2026-08-01)

- State: the Eve deployment is `LIVE_VERIFIED` for non-mutating health, readiness, and Cron authentication. The image repair is `MOCK_VERIFIED` by the canonical build and awaits human review in draft PR `#82` before it can reach production.
- Production evidence: Vercel deployment `dpl_6irpCAbwMJ1qWMCWsFDVmvpv3qV8` is ready on merged commit `84a958b47044895f71134437a18e4123add71a40`. `GET /_internal/eve/api/healthz` and `GET /_internal/eve/api/readyz` both returned HTTP `200` at `2026-08-01T05:33Z`, reporting `mode: observe`, `mutation_kill_switch: true`, a closed circuit, and `ready: true`. An unauthenticated `GET /_internal/eve/api/cron` correctly returned HTTP `401` with the redacted `AUTHENTICATION_FAILED` taxonomy. Vercel Agent Runs reported zero production runs.
- Live limitation: health truthfully reports `BLOCKED_MISSING_CREDENTIALS` for integrations. The project environment contains the Cron, Google service-account, PageSpeed, safety, Blob, and feature-flag variable names, but neither an AI Gateway credential nor Vercel OIDC token is exposed to the deployed sidecar. Eve is therefore operational as a protected observe-only service, but not authorized or configured to start model-backed research.
- Image diagnosis: representative direct local image paths on production returned HTTP `200`, while the corresponding `/_next/image` optimizer URLs returned HTTP `404`. Browser inspection confirmed zero natural image width for every sampled optimized image. This is a Vercel Services image-optimizer routing limitation, not missing image files.
- Image repair: `next.config.ts` now enables global Next image `unoptimized` delivery for the existing local WebP assets, avoiding the unavailable optimizer route. `scripts/ci/test-image-delivery.ts` enforces that setting, and `ci:images` is part of `seo:check`. The focused guard passed locally; canonical Actions run `30686026433` passed all 16 commands at `2026-08-01T05:39:18Z` for commit `fac65d44d85923bd8ad32556a7c3226926bce0a5`, including formatting, lint, typecheck, SEO validation, root build, sidecar build, 102 tests, 15 evals with 50 gates, security scan, audit-only fixture, and health smoke.
- Preview state: PR `#82` preview `dpl_75uYxAMeG5zLv9aeRKRWAYVTb6Wo` is ready. Vercel preview SSO prevents a direct browser image inspection, so that check remains `BLOCKED_PREVIEW_SSO`; Vercel preview, Lighthouse, dependency audit, UI audit, and GitGuardian are green.
- Next exact action: complete the remaining normal self-hosted PR checks, keep the PR draft, and obtain human merge approval. After Vercel deploys the merge, verify production pages contain direct `/images/` URLs with nonzero natural dimensions. Do not enable live reads, publishing, proposal mode, or mutation.

## Phase 28: Vercel Services Preview and Canonical Verification (2026-08-01)

- State: COMPLETE for the reviewed preview deployment and credential-free verification (`LIVE_VERIFIED` deployment; `MOCK_VERIFIED` deterministic runtime contracts). The feature branch is `feat/eve-live-proposal-workflow`; the verified checkpoint is `9b56a0fbeb393910212fe136cfe26d3e4749a3f0`.
- Completed work: upgraded the isolated Eve package to the current verified `0.29.4` release, retained the Node `>=24` boundary, and deployed the single-project Vercel Services topology. The preview includes the public `site` service plus five internal `eve_seo_agent` functions: the Eve entry, server bridge, protected Cron dispatcher, health endpoint, and readiness endpoint. The public site package and client dependency graph do not import Eve or sidecar dependencies.
- Live evidence: Vercel preview deployment `dpl_DjoRsvjvXwSiYkRo4uHPvxG9zVtJ` reached `Ready` at `2026-08-01T04:19:09Z` for commit `64c54a7938743aa467f22ec3a81c153ae8fda89e`; the successor preview for checkpoint `9b56a0f` is also reported successful by Vercel. Agent Runs listed zero runs before verification, so no audit, publish, or mutation was inadvertently started.
- Verification: GitHub Actions run `30683862752` completed successfully at `2026-08-01T04:26:14Z` on self-hosted Node `v24.18.0` and npm `11.16.0`. Its fresh evidence run `2026-08-01T042441807z-9b56a0fbeb39` recorded a clean tree hash, 16/16 passing commands, `102` sidecar tests, `15` Eve evals with `50` gates, root/sidecar formatting, lint, typecheck, search/archive/SEO validation, security scan, audit-only fixture, health smoke, and both production builds. The evidence classification is `MOCK_VERIFIED` because it deliberately used fixtures and performed no provider write.
- Corrected failure: the preceding canonical run failed only root formatting. The failure named the six new root verification files; they were reformatted with the repository's tab-based Prettier settings. The succeeding canonical run passed `root-format` and all remaining checks. No policy, runtime, content, or public-site behavior was weakened.
- External blocker: direct preview calls to `/_internal/eve/api/healthz`, `/_internal/eve/api/readyz`, and the Cron endpoint are intentionally intercepted by Vercel SSO. This is `BLOCKED_PREVIEW_SSO`, not a successful endpoint invocation. Do not disable SSO or add a bypass solely to test Eve without owner approval. The configured audit path also remains fail-closed until the Git-backed persistence/approval path is present; mutation remains disabled.
- Next exact action: keep PR `#81` as a draft, allow its remaining normal CI checks to finish, and obtain owner approval for an SSO-safe internal endpoint verification path. Then perform only the approved observe-only invocation; do not merge, publish content, or enable proposal/mutation mode.

## Phase 27: Release CI Runner and Dependency Audit Repair (2026-07-31)

- State: COMPLETE locally. The repository now has a dedicated online Linux self-hosted runner named `wades-wsl-release` with the required `wades`, `wades-ci`, `wades-quality`, and `wades-seo-offline` labels. It is registered as a system service for `byronwade/wadesplumbingandseptic.com`.
- Completed work: reviewed the previously queued PR checks, verified the absence of any repository runner, then registered the dedicated runner in the local Ubuntu WSL environment. Removed optional GitHub npm-cache save and restore configuration from the shared self-hosted setup action after a live job showed its cache-save post step delaying the release queue. Installed and headless-smoke-tested native Google Chrome on that runner because Ubuntu's `chromium-browser` package is a Snap placeholder there. Corrected the Lighthouse 12 invocation to pass the Linux browser through `CHROME_PATH`; its removed `--chrome-path` flag was ignored and caused a fallback to the host Windows Chrome. The Quality `npm audit` job exposed two high severity advisories caused by Next's nested `postcss@8.5.10`. Updated the root direct PostCSS dependency and a scoped npm override to `8.5.25`, which resolves every installed PostCSS copy to the patched version.
- Verification: `npm install --package-lock-only --ignore-scripts`, `npm ci --ignore-scripts`, `npm ls postcss --all`, `npm audit --omit=dev --audit-level=high`, and `git diff --check` completed locally with exit `0`. The final production dependency audit reported `found 0 vulnerabilities`. Fresh remote CI is required because the prior Quality job evaluated the commit before this lockfile repair.
- External state: the Vercel preview for PR `#80` is `Ready`. No production merge, content publication, sidecar deployment, or mutation capability has been enabled by this phase.
- Next exact action: push the audit repair, wait for the required CI checks and preview to pass, merge PR `#80` without bypassing protection, then verify the Vercel production deployment.

## Phase 26 - Search Console Service Account Repair (2026-07-31)

- State: COMPLETE for the offline service-account adapter (`MOCK_VERIFIED`). The Google OAuth connector remains `FAILED` for Eve's non-interactive app subject and is not represented as a live Search Console integration.
- Completed work: added the official sidecar-only `google-auth-library@11.0.0` dependency and a typed Google service-account token provider. It validates the service-account email and PEM envelope, normalizes literal newline escapes in the private key, requests only `https://www.googleapis.com/auth/webmasters.readonly`, caches the JWT client in process, and never includes credential material in configuration summaries or evidence. Search Console's adapter now resolves that short-lived token before its existing bounded, read-only `sites.list` and Search Analytics calls. The former `SEARCH_CONSOLE_ACCESS_TOKEN` path remains a local-fixture compatibility fallback; it is not the intended deployed credential path.
- Regression coverage: added deterministic coverage for private-key normalization, read-only scope selection, token-header use, key redaction, atomic environment configuration, service-account precedence over the legacy token, and absent partial configuration. The first focused run correctly failed because the test asserted the pre-normalized newline form; the assertion was corrected without weakening the implementation's fail-closed credential behavior.
- Live evidence: the linked Google OAuth connector `accounts.google.com/wadesplumbingandseptic-com` is present. A Vercel Connect app-token request through temporary Development and Production project identities returned `ConnectError` with code `unresolved_token`; no Google API request followed. This is a configured connector failure for a background app subject, not proof of Search Console access. The service-account property grant was owner-reported, but neither `GOOGLE_SERVICE_ACCOUNT_EMAIL` nor `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` exists in the Vercel project environment and no Vercel Services sidecar is deployed, so Search Console remains `BLOCKED_MISSING_CREDENTIALS` for Eve.
- Verification: `npm install --ignore-scripts` and `npm install --package-lock-only --ignore-scripts` in `automation/seo-agent` - exit `0`; `npm --prefix automation/seo-agent run ci:offline; npm --prefix automation/seo-agent run audit:dependencies; npm --prefix automation/seo-agent run security:scan` - exit `0` - `19.2s` - lint, format, typecheck, `101` deterministic tests, `10` runtime integration fixtures, evals, fixture audit, rollback drill, independent review, Eve build, dependency audit, and secret scan all passed. Root `npm run format:check; npm run lint; npm run typecheck; npm run build` - exit `0` - `88.2s` - produced the existing `590` public-site routes. The Eve build retained its non-failing pre-existing Windows absolute-path module-evaluation warning for `agent/tools/orchestrate.ts` while its manifest verification passed.
- External blockers: root `vercel.json` correctly remains site-only while Vercel Services has no accepted preview proof. The private service-account credential or an approved workload-identity configuration has not been added to the future Production sidecar environment. The Production-only PageSpeed key remains present but its flag is off and it was not invoked.
- Next exact action: create the reviewed Services activation preview, then add the dedicated service-account email/private key as encrypted Production variables, approve one exact audit-only run, and use the sidecar's `live:probe --execute` command to classify the real Search Console result without enabling mutation.

## Phase 25 - GitHub Connect Read Integration (2026-08-01)

- State: PARTIALLY LIVE_VERIFIED. The GitHub Connect app-token exchange and one repository metadata read are live verified. The Vercel-hosted Eve sidecar remains undeployed because the project intentionally uses the recovered site-only Next.js configuration.
- Completed work: aligned the Eve GitHub tool and GitHub read adapter to the installed `github/wadesplumbingandseptic-com` connector. The adapter now mints a short-lived app token through `@vercel/connect` when `SEO_AGENT_ENABLE_GITHUB=true`; `GITHUB_READ_TOKEN` remains only a local-fixture fallback. Added typed connector-ID configuration and regression tests for app subject, absent-token rejection, malformed connector IDs, and header use.
- Live evidence: at `2026-08-01T00:37:20Z`, a development project OIDC token was held only in process memory, `getToken` minted an app token for the installed connector, and a read-only `GET /repos/byronwade/wadesplumbingandseptic.com` returned HTTP `200`, default branch `main`, and GitHub request ID `FE77:325E11:264D90D:80B6F91:6A6D3F41`. The actual `createGithubReadAdapter` then returned `LIVE_VERIFIED` for the same read-only repository scope. No repository mutation occurred. This proves the connector and app-scoped repository read, not the deployed sidecar.
- Vercel configuration: verified the project Framework Preset is Next.js with Node `24.x`. Updated Preview and Production through the Vercel CLI with `SEO_AGENT_ENABLE_GITHUB=true` and `SEO_AGENT_GITHUB_CONNECTOR_ID=github/wadesplumbingandseptic-com`; no static GitHub or AI Gateway token was added. The initial PowerShell target argument was rejected before any variable changed, then the quoted retry completed and metadata verification passed.
- Verification: `npm --prefix automation/seo-agent run ci:offline` - exit `0` with Node `v24.14.0`, including lint, all sidecar formatting checks, typecheck, static contracts, secret scan, `99` unit tests, `10` runtime integration tests, evals, fixture audit, rollback drill, independent review, and Eve build/manifest verification. `npm run format:check` - exit `0`; `git diff --check` - exit `0`. The local Eve build emitted a non-failing Windows absolute-path module-evaluation warning for the pre-existing `agent/tools/orchestrate.ts` path, while still producing its manifest.
- External blockers: root `vercel.json` deliberately remains site-only after recovery commit `e6cb3b4`; no Vercel Services deployment, Eve health endpoint, Cron dispatch, AI Gateway request, or sidecar workflow invocation is live verified. GitHub triggers remain disabled by design.
- Next exact action: keep the public site on Next.js. Create a dedicated reviewed Services activation change only when ready to deploy and preview the sidecar, then rerun the health and audit-only acceptance checks.

## Phase 24 — Vercel Services Config Repair (2026-07-31)

- State: COMPLETE for static/offline repair plus site-only deploy recovery (`MOCK_VERIFIED` offline; live Services still blocked). Production and preview deployments for `main` commits `cd3bd8d` and `bd4b1c1` failed immediately after the Services topology landed.
- Root cause: root `vercel.json` mixed the current `services` model with legacy `routePrefix` and service-level `maxDuration`. Against `https://openapi.vercel.sh/vercel.json`, those fields are rejected (`additionalProperties` errors). Current Services routing requires top-level `rewrites` with `{ "service": "..." }` destinations, not `routePrefix`.
- First repair attempt: a schema-valid Services `vercel.json` with rewrite routing was pushed on this branch (`6897028`). Local site and Eve builds both succeeded, but the Vercel preview for that commit still failed after about `1.5` minutes. Protected build logs remain unread (`BLOCKED_MISSING_CREDENTIALS`: no Vercel token/MCP auth in this environment). Manual setup already recorded that Services availability was not verified on the linked project.
- Recovery change: keep root `vercel.json` site-only (schema only) so the public Next.js project can deploy again. Store the corrected Services topology in `docs/seo-agent/vercel.services.example.json` and teach `verify-deployment-config.mjs` to validate that target while forbidding an unverified live `services` block. Promote the example to root only after an owner records `LIVE_VERIFIED` Services access. No provider call, content mutation, merge, or production configuration change is performed by this agent turn.
- Commands: with Node `v24.18.1`, OpenAPI schema validation of the repaired Services example reported `0` errors; local `npm run build` and `npm --prefix automation/seo-agent run build` both exit `0`; `npm --prefix automation/seo-agent run verify` / `test` (`97/97`) / `verify:offline` exit `0`. After the site-only recovery commit `c9218fd`, Vercel preview deployment `5698679011` completed successfully.
- Evidence truth states: `MOCK_VERIFIED` for local schema/config alignment and offline gates; `FAILED` for the schema-valid Services preview deploy on `6897028` without readable logs; `LIVE_VERIFIED` for site-only preview deploy success on `c9218fd` (`https://wadesplumbingandseptic-3t5vh5ak2-byron-wade.vercel.app`); `BLOCKED_MISSING_CREDENTIALS` for Vercel log inspection and Services project access proof.
- Next exact action: merge this recovery PR through normal human review so production can deploy again. Separately, an owner should enable/prove Services access before promoting `vercel.services.example.json` to root.

## Phase 23 — Post-Merge Build and Formatting Repair (2026-07-31)

- State: COMPLETE for the sidecar repair and local production builds (`MOCK_VERIFIED`). GitHub merged the Eve control-plane port as `origin/main` commit `cd3bd8d198f4e8659e695b986462ed9443498a6c` (PR #77). No deployment, provider call, content mutation, GitHub write, or production configuration change occurred during this verification.
- Completed work: installed the required local Node `v24.14.0` runtime and ran the merged sidecar gate. Its initial formatting reports were inflated by a cross-platform checkout defect: the repository lacked a `.gitattributes` policy while the local Git client had `core.autocrlf=true`, so tracked LF files were converted to CRLF before Prettier checked them. The new repository-level text policy enforces LF for text files on Windows and Linux. No behavior, dependency version, lockfile resolution, secret, or generated environment value was added.
- Verification: from a fresh LF-normalized checkout, root `npm run lint` and `npm run typecheck` — exit `0`; root `npm run build` — exit `0` — `63.7s`, generating `590` public-site routes with TypeScript completed successfully; `npm --prefix automation/seo-agent run ci:offline` with Node `v24.14.0` — exit `0` — `65.6s`, including lint, all three formatting checks, typecheck, static contracts, secret scan, `97` tests, runtime integration fixtures, policy evals, audit fixture, rollback drill, independent review, and Eve build/manifest verification; `git diff --check` — exit `0`.
- Continuation: the owner requested the remaining errors be fixed. Root `npm run format` normalized the tracked source, Markdown, manifest, workflow, and configuration files. It exposed a real duplicate `fakePhones` declaration in `scripts/ci/validate-content.mjs`; the checks are now consolidated into one broader fake-phone expression. It also exposed two malformed Markdown `- -` list items, which are now valid single bullets. The sidecar agent-boundary verifier is formatting-independent for its writer/judge role checks, and root Prettier now excludes Next-generated declarations plus synced MapLibre vendor files so successful builds do not invalidate the format gate.
- Final verification: `npm run ci:content && npm run check` — exit `0` — `112.5s` — content validation covered `165` Markdown files, and the aggregate lint/type/format/search/archive/related/page-view/city-service/sitemap/section/OG/meta/build checks passed. The production build generated `590` routes. `npm --prefix automation/seo-agent run ci:offline` with Node `v24.14.0` — exit `0` — `48.5s` — all sidecar format/lint/type/static-boundary/security/eval/audit/rollback/review/Eve-build gates passed with `97` deterministic tests. The final root `npm run format:check` and `git diff --check` both exit `0`.
- Dependency limitation: root production `npm audit --omit=dev --omit=peer --audit-level=high` still reports two high PostCSS advisories through `next@16.3.0-preview.10`. npm proposes an unsafe `--force` downgrade to Next 9, so no automatic dependency rewrite was applied. This is recorded as a dependency-review follow-up, not a claim that the advisory is resolved.
- External limitations: the independent offline review continues to classify all provider/browser/GitHub/Vercel live integrations as `BLOCKED_MISSING_CREDENTIALS`; its deterministic fixture results remain `MOCK_VERIFIED`. Eve emitted a post-success local queue `socket hang up` retry after the successful offline CI exit; it did not alter the command result or invoke a live provider.
- Next exact action: review and merge the complete formatting and validation repair through a normal PR. Separately research a compatible supported Next/PostCSS update before changing dependencies. Do not claim live integration or production readiness from these local results.

## Phase 22 — Vercel Services Deployment Topology (2026-07-31)

- State: COMPLETE for the static/offline topology (`MOCK_VERIFIED`). The repository configuration has moved from the prior two-project design to one Vercel Services deployment with independently built `site` and `eve_seo_agent` services. This is a static/offline change only; no Vercel project was linked, no environment variable was added, and no deployment was created.
- Changed scope: root `vercel.json` declares the public site at `/` and the backend-only Eve service at `/_internal/eve`; the obsolete nested sidecar `vercel.json` was removed. The sidecar retains its package boundary and lockfile. Deterministic checks now validate the Services configuration and fail if public runtime code imports sidecar code or reads agent/Gateway environment names. Setup, architecture, deployment, operations, and environment documentation now state that Services shares a project-level server environment scope, is not credential isolation, prefers OIDC/Connect, and must revert to separate projects if a connector cannot be safely constrained.
- Commands: with Node `v25.9.0`, `npm --prefix automation/seo-agent run ci:offline` — exit `0` — `94.8s`, including format/lint/typecheck, static verification, secret scan, evals, fixture audit, rollback drill, independent review, and Eve build; `npm run lint`, `npm run typecheck`, `npm run test` (`9/9`), `npm run seo:check`, and `npm run build` — exit `0` — production build generated `231` public routes; `git diff --check` — exit `0`. A focused sidecar test run also passed `97/97` in `2.0656s`. An initial accidental root-scope `npm run verify` reported the root package has no `verify` script; this was a command-scope error, then the correct sidecar command passed and no code failure resulted.
- Evidence: local checkpoint `3b4d819` (`feat(deploy): Configure Vercel Services topology`), `vercel.json`, `automation/seo-agent/scripts/verify-deployment-config.mjs`, `automation/seo-agent/scripts/verify-architecture.mjs`, `docs/seo-agent/HUMAN_REVIEW_AND_DEPLOYMENT.md`, and `docs/seo-agent/SIDECAR_ARCHITECTURE.md`.
- Verification classification: `MOCK_VERIFIED` for static Services topology and independent offline package boundaries; `BLOCKED_MISSING_CREDENTIALS` for actual Vercel Services access, project linkage, deployment, environment scope inspection, Cron delivery, and live integration probes.
- Failures / limitations: Vercel Services configuration is current-documentation based, not yet account/deployment verified. A shared Vercel project cannot be claimed to isolate static third-party credentials. No production mutation, content publication, GitHub write, deployment, or merge is enabled by this change.
- External blocker: two normal feature-branch `git push` attempts timed out without a remote acknowledgement; no force-push was attempted and `main` was untouched. The local branch is ahead one commit as of this record.
- Next exact action: restore authenticated remote Git availability, push the local feature-branch checkpoint normally, then have an owner link the single Services project and record redacted preview health/route evidence for `/_internal/eve/api/healthz` and `/_internal/eve/api/readyz` before any live classification changes.

## Phase 21 — Demand-Timed Editorial Selection (2026-07-30)

- State: COMPLETE for deterministic selection (`MOCK_VERIFIED`). The planner chooses one human-review-only proposal or `NO_ACTION`; it makes no model, provider, filesystem, GitHub, or production call.
- Changed scope: an existing-page/service-refresh or contextual-link improvement wins whenever viable. A net-new post is selected only after the existing-page review has no viable result and the candidate proves distinct unmet intent. Past holiday opportunities, fixed cadences, and unsupported new URL types cannot be selected.
- Commands: using Node `v25.9.0`, sidecar `npm run format`, `npm run lint`, and `npm run test` — exit `0`; `97/97` tests passed in `0.9092s`. No image provider, content publication, or external call occurred.
- Next exact action: connect this selector to an audit-only opportunity-report artifact and an experiment-plan schema, then extend its fixture inputs with current but safely classified demand/seasonality evidence.

## Phase 20 — Editorial Reuse, Image Provenance, and Demand-Timed Growth (2026-07-30)

- State: COMPLETE for the deterministic policy foundation (`MOCK_VERIFIED`). This checkpoint makes the current Markdown inventory and link graph a required editorial input without enabling a provider, image download, content mutation, GitHub write, deployment, or production configuration.
- Changed scope: added a typed editorial-policy boundary; Markdown link-graph extraction now includes reader-facing Markdown anchors while excluding images and external targets; the draft-publishing lifecycle now enforces the editorial policy before a branch/commit/PR action. New URLs are limited to demand-timed blog posts or explicitly approved landing pages. New service pages are denied, services are refresh-only, generic page edits stay manual, holiday posts require local relevance plus 7–120 day useful lead time, and proposed images require HTTPS provenance, traceable rights, relevance rationale, descriptive alt text, and first-party support for visible local claims. The optional injected image-research boundary accepts only explicit HTTPS source/asset-domain allowlists, bounded queries/results, and safe normalized candidates; every returned candidate remains `UNVERIFIED` and cannot authorize an asset download or publication.
- Regression coverage: new fixtures prove Markdown links are captured correctly; deny fixed cadences, missing existing-page insufficiency, new service URLs, generic-page automation, mismatched lifecycle change targets, invalid image rights, unallowlisted image hosts, and injected image-result instructions; and prove the lifecycle rejects a coherent new service attempt before branch or PR creation. Sidecar test count increased from 87 to 94.
- Commands: with `C:\Program Files\nodejs\node.exe` (`v25.9.0`) first on `PATH`, sidecar `npm run format`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:integration`, and `npm run build` — exit `0`. The initial combined run exposed one fixture-only assertion mismatch; it was corrected with a coherent attempted service-creation change set, then `npm run format && npm run test` passed 91/91 tests in `0.8759s`. The image-research follow-up passed `npm run format && npm run test` with 94/94 tests in `0.9926s`. Eve build/manifest verification also passed. The recurring FNM message `Requested version >=24.0.0 is not currently installed` appeared after successful Node 25 subprocess completion and did not alter their zero exit codes.
- Failures / limitations: this is policy and fixture proof, not proof of an image-search provider, licensing service, asset download, Vercel project, AI call, browser session, or content publication. The image-research adapter deliberately reports `BLOCKED_MISSING_CREDENTIALS` until a reviewed provider/client and source allowlist are supplied. The root canonical verifier at commit `592ae30` passed 28/29 checks; root lint, TypeScript, build, root tests, SEO, security, and every sidecar gate passed. Only the inherited root `format:check` baseline failed, with 16 external credential blockers still classified explicitly.
- Next exact action: add a demand/seasonality opportunity selector that can choose `NO_ACTION`, evaluates existing-page/link-first alternatives, and produces a human-review-only plan. After that, integrate the reviewed image-research boundary only through a credential-free fixture configuration and separate live setup approval.

## Phase 19 — Markdown-Architecture Rebase Integration (2026-07-30)

- State: IN PROGRESS. The prior Eve feature branch was based on the legacy public-site tree; current `origin/main` is a Markdown-first Next.js site. A mechanical rebase conflicted with the retired application paths, so the control plane is being ported onto a clean branch based on `origin/main` while retaining the current public site as authoritative.
- Branch: `feat/eve-seo-agent-rebased` (based on `origin/main` commit `2bc7312`). The original `feat/eve-seo-agent` branch remains preserved and pushed at `e27bcdc`.
- Completed: isolated sidecar, policies, skills, docs, manifests, self-hosted workflow, and root verification tooling were transferred without restoring retired `app/(website)` sources. The content validator now accepts CRLF and nested frontmatter, inventories the current `content/pages`, `content/posts`, and `content/services` directories, uses `app/sitemap.ts`, `app/robots.ts`, and `next.config.ts`, and treats existing unapproved claims as review warnings rather than silently approving them.
- Evidence so far: root content validation parsed 112 Markdown documents with zero errors. Sidecar install completed from its separate lockfile; sidecar lint, format, and typecheck passed using Node `25.9.0`. Inventory regenerated 121 mapped pages, six dynamic patterns, four static links, and 115 informational technical findings. The first canonical verifier run (`verify-20260730172529-17cd14edede6`) passed 25/29 checks. It exposed an integration-boundary defect: root ESLint and TypeScript traversed the separately tested sidecar, including Eve-generated runtime output. Root configuration now explicitly excludes `automation/seo-agent/**`, while the canonical verifier still runs all sidecar gates independently. Focused root `npm run lint`, `npm run typecheck`, and `npm run build` now exit `0`; the build generated 231 public routes.
- Failures / limitations: root `format:check` currently reports pre-existing formatting differences throughout current `main`; this rebase phase will not mechanically reformat the public site merely to hide that baseline condition. The initial canonical run also recorded 16 correctly classified external credential blockers. No live integration, deployment, production configuration, content mutation, PR creation, or merge occurred.
- Next exact action: commit the isolation correction, rerun the canonical verifier from the resulting clean checkpoint, and record the formatting baseline distinctly before entering the expanded content-reuse, image-provenance, and demand-timed editorial phase.

## Phase 18 — Review Remediation and Canonical Runtime Selection (2026-07-30)

- State: COMPLETE for the deterministic remediation (`MOCK_VERIFIED`). No live Blob call, provider credential, deployment, production configuration, content mutation, GitHub mutation, merge, or public-site dependency change occurred.
- Accepted review remediation: Blob archive plans are now truthfully `BLOCKED_MISSING_CREDENTIALS` until a production-only, exact-run approval can be verified. Before the first write, the archive requires an exact 40-character commit SHA, a server-side read of every planned Git artifact, and a committed `seo/runs/<run-id>/blob-archive-approval.json` that binds the manifest and bundle digests to a timestamped, non-secret human review reference. Archive eligibility rejects raw connector/query/browser/prompt/customer-shaped payloads and non-approved retention classes. A partial provider failure returns a redacted `FAILED` receipt with completed immutable paths and no automatic retry.
- Evidence integrity remediation: `BLOB_READ_WRITE_TOKEN` is now validated by the typed configuration schema. The historic County and integration evidence records now include individual source/tool, timestamp, scope, and exact truth-state fields; their payload SHA-256 values were recalculated. Root regression tests enforce this per-finding provenance contract.
- Canonical-verifier remediation: the previous `npm run verify:all` used Node `v22.21.1` for all subprocesses although Eve requires Node 24+. The verifier now resolves a Node 24+ runtime for every sidecar command, records its path/version in the artifact, prepends it to the subprocess path, and fails closed with an explicit setup action if unavailable. Root checks continue to use the root runtime.
- Focused validation: `npm --prefix automation/seo-agent run format:check`; `npm --prefix automation/seo-agent run lint`; `npm --prefix automation/seo-agent run typecheck`; `npm --prefix automation/seo-agent run test` (87 tests, 87 passed); `npm run test` (7 tests, 7 passed); `npm run seo:check`; root and sidecar `security:scan`; and `git diff --check` — exit 0. The prior direct Eve regression command using `C:\Program Files\nodejs\node.exe` (`v25.9.0`) passed 15/15 evals, 50/50 gates, Eve build, and manifest verification; the known post-success local Eve queue retry remained non-failing.
- External state: Vercel Blob remains `BLOCKED_MISSING_CREDENTIALS`. A future real archive remains blocked until the separate sidecar is deployed with a least-privilege token and a read-only Git reader capable of producing the exact committed approval record proof. Do not enable publication.
- Next exact action: provision the separate sidecar project and a read-only Git artifact reader, then complete a human-approved, audit-only live integration verification. Keep Blob and all publishing mutation paths disabled until that evidence is reviewed.

## Phase 17 — Optional Private Vercel Blob Artifact Archive (2026-07-30)

- State: COMPLETE for the versioned, credential-free implementation (`MOCK_VERIFIED`). No Blob token, Vercel configuration, deployment, provider request, content mutation, GitHub mutation, merge, or production change occurred.
- Scope: the isolated sidecar now pins `@vercel/blob` `2.6.1` and treats Blob only as a secondary private archive for a validated, already Git-backed audit bundle. Git remains the canonical persistent state and evidence source; Blob cannot hold raw research, credentials, or public content and is not a database, CMS, or publishing mechanism.
- Fail-closed write policy: archiving is disabled by default. A write requires the production sidecar, an explicit enable flag, an exact human-approved run ID, a matching committed Git manifest receipt, bounded bundle size/counts, and a content-addressed pathname. The provider call is forced to `access: private`, `addRandomSuffix: false`, and `allowOverwrite: false`; provider URLs are discarded from returned evidence. The audit-only workflow produces a redacted, no-write archive plan only.
- Configuration and operations: `BLOB_READ_WRITE_TOKEN` is optional and sidecar-only. `SEO_AGENT_BLOB_ENABLED`, `SEO_AGENT_BLOB_ARCHIVE_APPROVED`, `SEO_AGENT_BLOB_APPROVED_RUN_ID`, and `SEO_AGENT_BLOB_PREFIX` are validated and summarized without secret values. The two-project setup, least-privilege environment scope, source policy, operational activation/revocation process, and integration inventory are documented in the Blob sections of the architecture, manual setup, human-deployment, source-policy, and operations documents.
- Regression coverage: four fixture-only Blob tests cover disabled behavior, private/no-URL plans, approval/receipt denial before any provider call, and immutable private writes with redacted provider URLs. An injected fixture writer stays `MOCK_VERIFIED`; only a default call to the actual Blob client can report `LIVE_VERIFIED`. The configured audit test asserts the default blocked plan. There has been no live Blob write or credential probe.
- Validation: `npm --prefix automation/seo-agent run format:check`; `npm --prefix automation/seo-agent run lint`; `npm --prefix automation/seo-agent run typecheck`; `npm --prefix automation/seo-agent run test` (85 tests, 85 passed); `npm --prefix automation/seo-agent run security:scan` (148 tracked executable files, no findings); and `npm --prefix automation/seo-agent audit --omit=dev --audit-level=high` (0 vulnerabilities) — exit 0 using npm's Node `v25.9.0`. `C:\Program Files\nodejs\node.exe scripts/run-evals.mjs` from the sidecar — exit 0, 15/15 evals and 50/50 gates passed in 20.9s; Eve emitted one non-failing local queue retry after the successful result. `C:\Program Files\nodejs\node.exe node_modules/eve/bin/eve.js build` and `scripts/verify-eve-manifest.mjs` — exit 0. Root `npm run typecheck` and `npm run build` — exit 0, 28 public-site routes generated; root dependency manifests were unchanged and Blob references were absent outside the sidecar, SEO manifest, and documentation. The build retained the pre-existing Browserslist-age and two Edge-runtime warnings.
- External state: Blob remains `BLOCKED_MISSING_CREDENTIALS` in `seo/manifests/integration-inventory.json` until the separately deployed sidecar has a least-privilege token and a human-approved, Git-receipted archive test. Do not run that test against public content or enable publication.
- Checkpoint: `feat(seo-agent): Add private Blob artifact archive` committed on `feat/eve-seo-agent`; the working tree is clean. No remote branch, PR, deployment, provider, or production configuration was changed.
- Next exact action: only after the separate sidecar is deployed and an audit run has been human-approved may an operator perform one private, redacted Blob archive verification and record its truth classification.

## Phase 16 — Draft PR Preview Acceptance Audit (2026-07-30)

- State: BLOCKED; PR #6 remains a draft and was not marked ready. No production content, Business Profile data, deployment setting, `main` branch, merge, or sidecar mutation occurred.
- Exact head audited: `f4cb6dab3c1e8501f42ae910fb474bb7fa62174d`. Netlify deploy `6a6af54c241b850008d0d5ec` is `error` for this PR/head, has no `published_at` or `deploy_time`, and its preview URL returned HTTP 404. GitHub's deployment API had no Vercel deployment for this SHA; the only visible Vercel preview belonged to unrelated branch `cursor-fix-4f0b74` and was deliberately excluded.
- CI disposition: GitGuardian succeeded. The required `SEO agent offline gate` was queued with no job log. Its job requires `[self-hosted, linux, x64, wades-seo-offline]`, while GitHub reported zero registered repository runners. This is an infrastructure-availability blocker, not evidence of a code failure. Netlify's Header rules, Pages changed, Redirect rules, and deploy-preview checks failed because the deployment was not published; public deployment metadata supplied no error message and protected detailed logs were unavailable.
- Preview-route disposition: HTTP, redirects, canonical, robots, sitemap, metadata, structured data, internal links, image/alt text, visual rendering, console/runtime errors, health, performance, accessibility, Lighthouse, public-agent/UI, and secret-leak checks were not run against an unrelated or 404 target. They must be repeated only after a published preview exists for the exact head SHA.
- Sidecar observe/dry-run evidence: `npm --prefix automation/seo-agent run audit:fixture`; `npm --prefix automation/seo-agent run dry-run`; and `npm --prefix automation/seo-agent run health:smoke` — exit 0 using bundled Node `v24.14.0`. The fixture produced the page inventory, route map, link graph, query-ownership, technical report, opportunity/backlog references, and redacted trace fields inside a validated audit-only manifest proposal; it proposed paths only, with `write_performed=false`, `draft_prs_created=0`, `content_files_changed=0`, and the mutation kill switch true. This is `MOCK_VERIFIED`, not a deployed sidecar run.
- PR evidence comment: https://github.com/byronwade/wadesplumbingandseptic.com/pull/6#issuecomment-5127670887
- Recheck after the audit checkpoint at `9c87db2abcefe88d6529e4fa79148236d4d53c22`: Netlify deploy `6a6af6c5322eb20007cf31c1` reached `error` with `published_at=null`, `deploy_time=null`, no public error message, and the preview root still HTTP 404. The exact SHA still has no GitHub/Vercel deployment record. `Credential-free offline verification` is queued, and the repository still reports zero self-hosted runners. Local `npm run verify:all` passed 29/29 at `artifacts/verification/verify-20260730070223-9c87db2abcef/results.json`; `npm run verify:completion` accepted that artifact with 15 classified external integration blockers. These local results are `MOCK_VERIFIED`; they do not clear the preview or runner gates.
- Next exact action: make an eligible self-hosted runner available and restore a published preview for the current PR head. If the deployment fails again, grant read-only build-log access or provide the error. Then rerun the complete preview acceptance matrix and post a replacement evidence comment; do not mark ready unless every required gate passes.

## Phase 15 — Live Integration Verification Attempt (2026-07-30)

- State: PARTIALLY VERIFIED. No production content, Business Profile data, `main` branch, or merged pull request changed. The one disposable GitHub permission PR was closed and its remote branch deleted after verification.
- Live evidence: `seo/evidence/live-integration-verification-2026-07-30.json`. It records the full per-integration timestamp, safe request identifier, scope, status, and corrective action without secrets or raw provider responses.
- `LIVE_VERIFIED`: authenticated GitHub repository metadata read (`F29C:F0E4E:145A6AB:480B00C:6A6AF23C`); disposable draft PR #7 creation/readback/close/delete (`CCC2:2F4CB3:AF025:265A28:6A6AF2A4`); Vercel CLI read of the existing public-site project and preview; and allowlisted browser HTTP retrieval of the public `robots.txt` document. These prove only the stated local/CLI or public-read scopes, not a deployed sidecar connector.
- `FAILED`: no separate SEO-sidecar Vercel project or deployment was discoverable in the accessible `wades-web-dev` or `byron-wade` teams. Expected `/api/healthz`, `/api/readyz`, and `/api/cron` paths on the public site returned HTML rather than the sidecar JSON contract, so Eve invocation, Cron authentication, structured traces/logs, and sidecar health/readiness were not exercised. The response IDs are retained only in the redacted evidence record.
- `BLOCKED_MISSING_CREDENTIALS`: AI Gateway usage, Search Console, PageSpeed, sidecar Vercel-read adapter, web-search provider, Browserbase/browser automation session, GA4, Business Profile, Local Falcon, Similarweb, Google Trends, and tracing. The local environment had no sidecar `.env*` file or relevant process credentials; Vercel's public-site project environment listed Blob variables only. No provider request was made with absent credentials.
- Phase-caused correction: the evidence and completion contracts previously allowed only `LIVE_VERIFIED`, `MOCK_VERIFIED`, and `BLOCKED_MISSING_CREDENTIALS`. They now classify explicit `FAILED` probes and reject completion while one remains unresolved, with regression tests for both paths. Instructions, skill, source policy, build specification, definition of done, plan, and manual setup now describe the fourth state.
- Commands and evidence: Vercel CLI identity/project/deployment/environment-name reads; GitHub CLI repository read and disposable draft PR lifecycle; allowlisted adapter fetch; exact public-route probes; and `npm --prefix automation/seo-agent run test:contracts` under bundled Node `v24.14.0` (exit 0, 13 tests, 0 failures, 0.174 seconds). The no-network sidecar probe was also run and correctly reported all adapters blocked because execution approval/configuration was absent.
- External blocker and next exact action: create or disclose the existing separate sidecar Vercel project in an accessible team, set its Root Directory to `automation/seo-agent`, deploy it with the intended sidecar-only variables and one exact approved run ID, then repeat the redacted authenticated health, Cron, Eve, Gateway, and read-adapter probes. Do not re-link or put these variables in the public-site project.

## Phase 14 — Human Review and Deployment Preparation (2026-07-30)

- State: documentation handoff complete; no production setup or autonomous mutation was enabled.
- Added: `docs/seo-agent/HUMAN_REVIEW_AND_DEPLOYMENT.md` with two-project architecture, environment matrix, exact human verification checklists, Vercel/Eve/AI Gateway setup, GitHub/branch protection, Search Console service account, PageSpeed, optional adapters, UTC schedule explanation, pause/resume, incident/rollback, troubleshooting, and a 30-day observe-only plan.
- Confirmed locally: branch `feat/eve-seo-agent`; GitHub CLI authenticated as `byronwade` with repository/workflow scopes; no Vercel sidecar link, sidecar environment file, cloud credential, deployment, connector, cron activation, or live probe evidence exists in the working tree. These remain `BLOCKED_MISSING_CREDENTIALS` or human-owner tasks.
- Safety state: mutation mode remains disabled, mutation kill switch remains required, PR workflow remains draft-only, and this phase made no production configuration change, merge, or autonomous mutation.
- Pre-commit review: root `npm run security:scan` exited `0` with 276 tracked files and zero findings; sidecar `npm run security:scan` exited `0` with 148 tracked executable files; sidecar production `npm audit --omit=dev --omit=peer --audit-level=high` exited `0` with zero vulnerabilities. Root `npm audit` is not applicable because the root has an authoritative `bun.lockb` but no npm lockfile; this remains the documented package-manager authority decision, not a green root npm-audit claim.
- Publication preflight: GitHub Push Protection rejected the original unpushed branch history for a hard-coded Mapbox token inherited in `components/sections/MapBox.js`. The token was removed, the component now reads `NEXT_PUBLIC_MAPBOX_TOKEN`, the human guide documents the separate public-site variable, and the unpushed feature history was rebuilt without the token. No bypass, secret allowlist, force-push, merge, or production environment change was used.
- Next exact action: review the staged documentation diff, commit it, run `npm run verify:all` and `npm run verify:completion` from the clean checkpoint, confirm the recorded byte-level working-tree hash, then push `feat/eve-seo-agent` and open a draft PR targeting `main`.

## Phase 13 — Independent Final Review (2026-07-30)

- State: remediation complete pending the fresh canonical verification artifact.
- Scope: an independent source review against `BUILD_SPEC.md` and `DEFINITION_OF_DONE.md`, including architecture/content policy, tests/operations, security/permissions, workflow/dependency review, and final-diff inspection.
- Accepted fixes: preview Cron authentication now requires the timing-safe secret; configured runtime observation fails closed until the human-approved Git-backed manifest path exists; lifecycle publication validates inventory-backed proposals and an independent linked fact-check report; injected search content becomes a redacted security event; browser paths have time/byte bounds; completion evidence rejects dirty/tampered state; timer cleanup, health truthfulness, branch-policy consistency, and publisher truth classification have regression tests.
- Focused verification before this checkpoint: `npm run test`; `npm --prefix automation/seo-agent run lint`; `npm --prefix automation/seo-agent run test`; and `npm --prefix automation/seo-agent run typecheck` with Node `25.9.0` — exit `0` — root 6 tests and sidecar 81 tests passed. No credential, provider, GitHub, Vercel, browser, PR, merge, or production request occurred.
- Review evidence: `docs/seo-agent/FINAL_REVIEW.md`. Legacy public-claim warnings and root Bun-versus-npm authority are documented dispositions, not false live-operation claims.
- Next exact action: commit this review/remediation checkpoint, then run `npm run verify:all` from the resulting clean tree and retain its ignored evidence artifact without modifying tracked files afterward.

## Current State

- Current phase: 27 — Release CI runner and dependency audit repair
- State: COMPLETE locally; fresh remote CI remains pending before merge. Search Console, Blob, and other undeployed live integrations remain `BLOCKED_MISSING_CREDENTIALS`
- Branch: `docs/simplify-eve-owner-setup`
- Current commit: recorded by the Phase 26 checkpoint commit on `docs/simplify-eve-owner-setup`; use `git rev-parse HEAD` after integration for the exact release SHA.
- Baseline commit: `b43fc94` (`Deploy WordPress releases to the active theme`)
- Latest verified control-plane checkpoint: service-account adapter, typed configuration, and redaction regression coverage.
- Product runtime: IMPLEMENTED OFFLINE; LIVE RUNTIME BLOCKED
- Verification state: `MOCK_VERIFIED` for the control plane, contracts, policy/evals, safe telemetry, audit-only workflow, and canonical verification evidence

## Phase 12 — Verification Evidence System (2026-07-30)

- State: COMPLETE for deterministic local evidence (`MOCK_VERIFIED`). The implementation adds no live provider, browser, GitHub, Vercel, deployment, content-publication, or production mutation.
- Changed scope: `npm run verify:all` is now the canonical root verifier. It creates ignored, redacted `artifacts/verification/<run-id>/results.json`, `summary.md`, command logs, and parsed reports. Evidence binds the exact commit/Git tree plus a byte-level hash of all tracked file content, so `npm run verify:completion` rejects it after a tracked file changes.
- Coverage: the runner executes root and sidecar formatting, lint, TypeScript, unit/integration tests, policy evals, content/SEO/link/schema/service-area/similarity checks, root and sidecar secret scans, audit-only and publishing fixtures, health smoke, Sandbox classification, rollback drill, production builds, dependency advisory, and independent review. It records explicit `BLOCKED_MISSING_CREDENTIALS` integration blockers without claiming live operation.
- Completion and hook: the machine DoD mapping is versioned at `docs/seo-agent/DEFINITION_OF_DONE.json`. Completion rejects missing/stale evidence, failed required gates, unresolved critical/high findings, unclassified integrations, and incomplete DoD rows. The final guarded Stop hook is enabled with a one-continuation maximum and accepts caller-owned active-state JSON; it cannot write state, create work, or retry commands.
- Validation before checkpoint: root formatting was normalized mechanically across the configured scope after its first check reported 98 pre-existing formatting differences. The targeted root test suite passed five tests; root/sidecar secret scans, local degraded-but-ready backend health smoke, control-plane discovery, phase-owned formatting, and whitespace validation passed. No test was weakened.
- Phase-caused correction: the first full artifact run retained three failures instead of aborting: `npm exec node` attempted an unnecessary package lookup for the root JSON report and sidecar health smoke, while the baseline sidecar formatting list caught an unformatted control-plane file. The runner now invokes named local scripts only, never `npm exec`; sidecar formatting was normalized and rechecked. The failed artifact remains available as diagnostic evidence but is stale after this repair and must not satisfy completion.
- Evidence: the first complete command run after the `800bb5d` repair was `npm run verify:all` with Node `v24.14.0` and npm `11.12.1` — exit `0` — `106.6s` — all 29 required checks passed. It wrote `artifacts/verification/verify-20260730061417-800bb5d31048/results.json`, its Markdown summary, 29 redacted command logs, and the parsed full root SEO report. It recorded 91 reported unit/integration tests, 15/15 evals, 50 eval gates, both production builds, zero critical/high findings, and 15 explicit credential blockers.
- Completion proof: `npm run verify:completion` — exit `0` — accepted that artifact as fresh for commit `800bb5d31048`, with no failed required check, no high/critical finding, all integrations classified, and every machine DoD row present. The enabled hook also returned exit `0` when directly invoked with the inactive caller state; it emitted no continuation. The hook's active-state branch is bounded by the sentinel's `max_continuations: 1` and cannot mutate state or retry a verifier.
- Verification classification: `MOCK_VERIFIED` for all local evidence, logs, reports, freshness/completion checks, and hook control flow. `BLOCKED_MISSING_CREDENTIALS` for the 15 documented connector/telemetry/browser/provider integrations; those blockers are present in the artifact and do not claim live operation.
- Next exact action: human approval and provisioning of the separate sidecar project, least-privilege credentials, a redacted observability sink, and the first audit-only production run. Do not enable mutation/publishing merely to clear those external blockers.

## Phase 11 — Observability, Security, and Operational Resilience (2026-07-30)

- State: COMPLETE for deterministic local controls (`MOCK_VERIFIED`). No live trace/export sink, alert delivery, provider, browser, GitHub, Vercel, deployment, PR, content-publication, or production action occurred.
- Changed scope: added an allowlisted structured operational-event contract with correlation IDs derived from run/workflow identity. It records only safe run/workflow/job/phase/attempt/status, duration, model/provider, bounded token/cost/source/file counters, branch/PR/preview reference, and error/retry fields. Unknown fields, malformed URLs, mismatched correlation IDs, and secret-like values are rejected. Recursive redaction removes sensitive keys, secrets, email addresses, and phone numbers before a safe audit record can be created.
- Operational coverage: deterministic metrics now count success/failure/retry/duration/cost/draft-PR/no-op/policy-block events. Alert classification covers missed Cron, stale runs, repeated failures, cost threshold, preview regression, and production regression. Recovery remains checkpoint-only and mutation-disabled: transient timeout/provider/browser/GitHub failures may resume from the failed phase; malformed results and budget exhaustion require human review. Existing runtime locks/idempotency/circuit controls remain the dispatch boundary.
- Security and operations: added a tracked-executable secret scanner, a complete threat model, and incident/rollback guidance. The threat model covers prompt injection, malicious web/repository content, credentials/PII, GitHub scopes and issue commands, branch protection, dependencies, SSRF/domain abuse, runaway cost, duplicate scheduling, and false/unsafe claims. Alert transport and live trace correlation are intentionally not claimed: they require a separately approved redacted sink and proof.
- Phase-caused correction: the first new fault-injection test caught a normalizer defect that rejected its own derived `correlation_id`, plus an overly specific expected redaction label for a phone-named key. The normalizer now accepts only a matching derived correlation ID and uses generic key-level redaction. The focused command initially exited `1` with two new test failures; after the correction, all phase tests passed. No existing test was weakened.
- Commands: `npm --prefix automation/seo-agent run format:resilience; npm --prefix automation/seo-agent run lint; npm --prefix automation/seo-agent run typecheck; npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run observability:fixture; npm --prefix automation/seo-agent run security:scan; git diff --check` using Node `v24.14.0` and npm `11.12.1` — exit `0` — `13.1s` — 77 deterministic sidecar tests passed; the fixture emitted a redacted `MOCK_VERIFIED` safe event and the scanner found no secret-like tracked executable content.
- Commands: `npm --prefix automation/seo-agent run ci:offline; npm --prefix automation/seo-agent run audit:dependencies` using Node `v24.14.0` and npm `11.12.1` — exit `0` — `59.7s` — sidecar lint, all three formatting checks, typecheck, control-plane/architecture/agent-boundary checks, 77 tests, nine runtime integration tests, 15 Eve evals/50 gates, audit fixture, rollback drill, independent review, Eve build/manifest, safe telemetry fixture, and secret scan passed. The production dependency advisory reported zero high-severity vulnerabilities. The known local post-success Eve queue `socket hang up` retry was emitted after exit `0` and did not affect a check.
- Checkpoint: `3e27b04` (`Add SEO agent operational resilience controls`) committed on `feat/eve-seo-agent`; no merge, push, provider request, external alert, deployment, production configuration change, or secret/generated environment file was created.
- Post-checkpoint command: `npm run verify:all` using Node `v24.14.0` and npm `11.12.1` — exit `0` — `96.1s` — root lint/typecheck and the unchanged 28-page public production build passed; the 19-file control-plane check and full sidecar offline gate passed, including the 77 tests, 15 evals/50 gates, secret scan, resilience fixture, Eve manifest, and dependency advisory. It emitted `MOCK_VERIFIED` for Git tree `86b45cb62f0ac21ecebdd9643e186bc57edb3316`. The existing non-failing Browserslist data-age and two Edge-runtime/static-generation warnings, plus the local post-success Eve queue retry, remain environmental diagnostics only.
- Verification classification: `MOCK_VERIFIED` for redaction, metrics, alerts, recovery plans, fault injection, incident procedure, and static secret scanning. `BLOCKED_MISSING_CREDENTIALS` for a configured redacted telemetry/tracing or alert sink, actual Vercel Cron delivery, durable workflow state, provider/browser/GitHub/Vercel correlation, preview/production audit, and all publication.
- Next exact action: obtain human approval for a separate sidecar Vercel project, least-privilege connectors, a redacted telemetry/alert destination, and one audit-only production run; then record redacted live evidence without enabling mutations or automatic publishing.

## Root-Site Deterministic SEO and Content Validation (2026-07-30)

- State: COMPLETE for offline validation (`MOCK_VERIFIED`). No network, browser, AI, credential, GitHub, Vercel, content-publication, or production action occurred.
- Changed scope: added a pure Node root validator and three deterministic fixtures. Its date comparison uses a fixed, reviewable `2026-07-30T00:00:00.000Z` reference unless an explicit valid `SEO_VALIDATION_REFERENCE_DATE` is supplied, so required CI never depends on wall-clock time. It validates future Markdown front matter, dates, heading/fence structure, images/alt text, local links/fragments, duplicate titles/descriptions/canonicals, query repetition, similarity/cannibalization, claim/service-area policy, change-manifest JSON, redirect loops/targets, sitemap date integrity/inclusion, robots/noindex boundaries, static JSON-LD shape, NAP consistency, orphan pages, taxonomy archives, and accidental public Eve/agent paths. Newly proposed Markdown and structural regressions fail; legacy runtime-data or unsupported historical content is a review warning, never an invented fact or automatic rewrite.
- Sitemap correction: removed every global `new Date()` fallback and every fallback `lastModified` value. Dynamic entries now include `lastModified` only when the backing `updated_at` or `created_at` is a valid supplied date; static pages omit it. This prevents fabricated freshness signals.
- Archive/indexability review: no category or tag archive route is declared in the current repository, so no indexability change was justified. The validator emits `NO_CATEGORY_OR_TAG_ARCHIVE_DECLARED`; any future taxonomy route is a review warning until a content-count/indexability decision is versioned.
- CI: the existing `.github/workflows/seo-agent-offline.yml` keeps its exact self-hosted Linux/x64/`wades-seo-offline` policy, read-only permission, pinned actions, credential prohibition, and offline sidecar install. It now runs `npm run seo:check` before the sidecar install; no GitHub-hosted runner, credential, browser, network call, deployment, or write capability was added.
- Commands: `node --test tests/site-seo-validation.test.mjs`; `npm run seo:check`; `npm run check` using bundled Node `24.14.0` with npm `11.12.1` — exit `0` — `65.8s` for the aggregate check — root lint/typecheck, three root validator fixtures, all root SEO/content scopes, control-plane discovery, and the sidecar offline CI (73 tests, evals, fixture audit, rollback drill, independent review, Eve build/manifest) passed. The root validator scanned 23 declared routes and zero current Markdown documents with zero errors.
- Commands: `npm run build; npm run ci:content; npm run ci:search; npm run ci:archive; git diff --check` using bundled Node `24.14.0` with npm `11.12.1` — exit `0` — `46.2s` combined — the 28-page production build and every named root validation scope passed. The audit reports 82 review warnings: 44 legacy claims requiring first-party evidence, 20 static-link/orphan reviews, five NAP/canonical/heading reviews, 13 sitemap static-route inclusion reviews, and no declared category/tag archive. These are `MOCK_VERIFIED` static findings, not evidence that the claims are true or that live routes are reachable.
- Formatting limitation: the newly added root-wide `npm run format:check` correctly reported 101 existing formatting differences across legacy source, documentation, and this phase’s unformatted new files before they were individually formatted. The phase-owned files now pass their direct Prettier check. The broad check remains an explicit diagnostic and is not added to the passing root `check` aggregate; no unrelated legacy files or tests were rewritten/weakened. A combined long command was also terminated by the local 120-second orchestration limit, which produced an `EPIPE` in the interrupted Next build; the same root check and production build passed when run independently.
- Next exact action: commit this deterministic validation checkpoint, run the clean-tree canonical verifier with the compatible runtime, then obtain human-approved first-party facts before resolving any legacy claim, NAP, orphan, canonical, sitemap, or indexability warning.
- Post-checkpoint command: `npm run verify:all` using bundled Node `24.14.0` with npm `11.12.1` — exit `0` — `116.9s` — root lint/typecheck and production build, the 18-file control-plane check, sidecar offline CI, Eve manifest, and dependency advisory all passed. It emitted `MOCK_VERIFIED` for Git tree `a509cf9b5e69f80a05d24df25e285ed2146d76cc`. Existing non-failing Browserslist/Edge-runtime warnings and the post-success local Eve queue retry remained. No network request, AI call, GitHub/Vercel operation, deployment, production change, or persistent external artifact occurred.

## Phases 7–9 — Controlled Publishing and Deployment Lifecycle (2026-07-30)

- State: COMPLETE for the fixture contract (`MOCK_VERIFIED`). The real publishing gateway remains deliberately unwired and `BLOCKED_MISSING_CREDENTIALS`; no real PR, commit, label, comment, issue, merge, preview, deployment, or production audit was attempted.
- Changed scope: added an injected lifecycle coordinator and fixture runner. It pins the current `main` SHA, requires a deny-all isolated checkout, gathers evidence, selects exactly one existing-page-first opportunity, validates a safe bounded Markdown change set, creates only `eve/seo/YYYY-MM-DD-<slug>`, creates an attributed draft PR, adds SEO/risk/content labels, and waits for self-hosted CI. Preview findings are commented and gate readiness. The code observes a human merge only, then makes a read-only production-audit call bound to the merge SHA and records a monthly operations issue.
- Hard blocks: mutation mode defaults disabled and requires enabled mode, a disabled runtime kill switch, explicit human approval, and an explicit integration-test flag before any injected mutation operation. The coordinator rejects direct-main/force commit results, a merge-capable gateway, unapproved deletion, non-content/robots paths, unsupported service areas, multiple opportunities, incomplete labels, failed CI, failed/indexable preview, and production deployment SHA mismatch. No real connector is exposed to this coordinator.
- PR packet: the generated draft body includes source/evidence, baseline and expected outcome, complete-day observation window, claims constraints, affected files/links, tests/evals, preview evidence, labels/risks, human-only rollback, and reviewer checklist. It states that no destructive redirect/canonical/robots change is allowed by its patch boundary.
- Commands: `npm --prefix automation/seo-agent run format; npm --prefix automation/seo-agent run format:lifecycle; npm --prefix automation/seo-agent run ci:offline; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `57.5s` combined — sidecar lint/format/typecheck, 73 deterministic tests, native Eve evals, fixture audit, rollback drill, independent review, Eve build/manifest verification, and dependency checks passed. The local Eve world queue printed a transient post-build `socket hang up` retry after successful completion; it did not fail a check or invoke a provider.
- Commands: `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run publishing:fixture; npm run lint; npm run typecheck; npm run build` with Node `25.9.0` first on `PATH` — exit `0` — `73` sidecar tests passed, the lifecycle fixture emitted `MOCK_VERIFIED`/`CLOSED`, and root lint/typecheck plus the 28-page production build passed. No public-site package, dependency, client bundle, route, production configuration, or deployed project changed. The root build retained only its existing Browserslist data-age and two Edge-runtime/static-generation warnings.
- Verification classification: `MOCK_VERIFIED`. The fixture uses in-memory gateway implementations only; it has no process credentials, network client, Git operation, GitHub/Vercel request, or persistent audit write.
- Failure correction: the first documentation formatting check reported style differences in `MANUAL_SETUP.md` and `DEFINITION_OF_DONE.md`; this phase formats and rechecks those changed documents before the checkpoint. No test was weakened.
- Next exact action: commit this lifecycle checkpoint, run the clean-tree canonical verifier, then wait for owner-managed separate-sidecar setup and an explicitly approved audit-only production run. Do not enable publication mode merely to bypass the missing live prerequisites.
- Post-checkpoint command: `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `100.1s` — root lint/typecheck and the 28-page production build passed; control-plane discovery found all 17 required files with the stop-hook sentinel absent; sidecar offline CI passed its 73 tests, native Eve evals, fixture audit, rollback drill, independent review, compiled Eve manifest check, and high-severity production dependency audit. It emitted `MOCK_VERIFIED` for Git tree `f2a33ca566f9ab442be289766328a5b3dbe934db`. The existing non-failing Browserslist/Edge-runtime warnings and transient post-success local Eve queue retry remained; no provider request, GitHub/Vercel mutation, PR, preview, deployment, or production change occurred.

## Phase 6 — External Prerequisite Reconfirmation (2026-07-30)

- State: `BLOCKED_MISSING_CREDENTIALS` for the first authorized production audit. This is the third consecutive reconfirmation of the same owner-managed prerequisite after all offline implementation work passed.
- Evidence: no `automation/seo-agent/.vercel/project.json`, no generated sidecar `.env*` file, and no process-level `VERCEL_OIDC_TOKEN`, `AI_GATEWAY_API_KEY`, GitHub, Vercel-read, Search Console, PageSpeed, or Cron credential variable were present. The worktree is clean on `feat/eve-seo-agent` at `651e7e9`.
- Command: `npm --prefix automation/seo-agent run live:probe` with Node `25.9.0` first on `PATH` — exit `0` — emitted the offline plan only. All required and optional adapters returned `BLOCKED_MISSING_CREDENTIALS`; no provider request was dispatched and no secret value was printed.
- Exact owner action: create/link the distinct Vercel project rooted at `automation/seo-agent`, configure only the least-privilege credentials and reviewed `SEO_AGENT_ENABLE_*` flags in `MANUAL_SETUP.md`, then set the one-run production approval values and run the first audit-only probe. Do not add credentials to Git or the public-site project.

## Phase 1 — Source-Policy Discoverability Hardening (2026-07-30)

- State: COMPLETE (`MOCK_VERIFIED`).
- Changed scope: made `SOURCE_POLICY.md` a required root control-plane artifact. The control checker now proves that root instructions, sidecar instructions, and the Wade Eve skill all require the source policy, and that the policy retains the untrusted-data, competitor-gap-only, and complete-Search-Console-window safeguards.
- Commands: `npm run seo-agent:control-plane`; `npm --prefix automation/seo-agent run format`; `npm --prefix automation/seo-agent run verify`; `npm --prefix automation/seo-agent run test`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `13.7s` — control plane reported 17 required files with the completion sentinel absent; sidecar verification and 65 deterministic tests passed.
- Verification classification: `MOCK_VERIFIED`. No network request, credential read, provider action, GitHub mutation, preview, or production change occurred.
- Next exact action: commit this control-plane hardening, then retain Phase 6 as `BLOCKED_MISSING_CREDENTIALS` until a human creates/links the isolated sidecar and approves the first audit-only production run.

## Phase 5 — Typed Integration Adapter Expansion (2026-07-30)

- State: COMPLETE for offline contracts (`MOCK_VERIFIED`). All real provider access is `BLOCKED_MISSING_CREDENTIALS`; no live API, browser, Vercel, GitHub, or production mutation was attempted.
- Changed scope: replaced the generic adapter stubs with typed, feature-flagged AI Gateway catalog, GitHub repository/PR inspection, Vercel project/deployment inspection, Search Console, PageSpeed, HTTP fetch, injected web-search, Browserbase session, GA4 Data API, Business Profile performance, and Local Falcon adapters. Similarweb, Google Trends alpha, and tracing are explicit configuration blocks until the owner approves documented access. Every HTTP adapter has bounded requests, response sizes, retries, `Retry-After` handling, and timeout classification; Search Console rejects date windows newer than three complete days. GitHub remains read-only here; the existing draft-only publication gateway remains human-approved and was not invoked.
- Source policy: added `docs/seo-agent/SOURCE_POLICY.md` and enforceable source-tier/provenance/conflict helpers. Public pages, search output, competitors, and external API text are untrusted data. Competitors are gap-analysis-only and cannot support Wade facts, code, safety, law, or manufacturer claims. Prompt-injection/security events are redacted and cannot issue a tool instruction.
- Commands: `npm --prefix automation/seo-agent run format`; `npm --prefix automation/seo-agent run lint`; `npm --prefix automation/seo-agent run typecheck`; `npm --prefix automation/seo-agent run test`; `npm --prefix automation/seo-agent run test:integration`; `npm --prefix automation/seo-agent run build`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `28.1s` — formatting, lint, typecheck, 65 deterministic tests, nine runtime integration tests, Eve build/manifest verification, and whitespace checks passed.
- Commands: `npm --prefix automation/seo-agent run ci:offline`; `npm run lint`; `npm run typecheck`; `npm run build`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `89.8s` — sidecar offline CI passed (65 unit/contract tests, nine runtime tests, five Eve evals/20 gates, fixture audit, rollback drill, independent review, Eve build/manifest); root lint/typecheck and 28-page Next build passed. The root build retained the non-failing stale Browserslist warning, two existing Edge-runtime/static-generation cautions, and a non-fatal Eve local queue socket-hang-up after successful work.
- Security evidence: fixture tests cover missing credentials, feature flags, malformed payloads, expired credentials, 429 retries, response-size limits, partial Search Console dates, Browserbase connection-URL redaction, untrusted web search, prompt injection, competitor-source refusal, and source conflict escalation. A targeted tracked-source credential scan had no value matches; `.env.example` contains names/comments only. No secret or generated environment file is tracked.
- Next exact action: commit this offline adapter checkpoint, then run the clean-tree canonical verifier. A human must create/link the isolated sidecar Vercel project and grant documented least-privilege credentials before an authorized first audit-only production run.

## Phase 3 — Self-Hosted Offline CI Source Contract (2026-07-30)

- State: COMPLETE for source and local fixtures (`MOCK_VERIFIED`); an isolated self-hosted runner and host-firewall proof remain `BLOCKED_MISSING_CREDENTIALS`.
- Changed scope: added a trusted-branch/manual-only GitHub Actions source workflow, a versioned runner policy pinned to Node `25.9.0`/npm `11.12.1`, cross-platform preflight/provenance scripts, a static workflow regression test, and the runner runbook. The workflow is `contents: read`, passes no secrets, disables checkout credentials, uses no cache, pins `actions/checkout` to official `v6.1.0` commit `d23441a48e516b6c34aea4fa41551a30e30af803`, and pins `actions/upload-artifact` to official `v4.6.2` commit `ea165f8d65b6e75b540449e92b4886f43607fa02`. It never deploys, probes live integrations, opens a browser, or writes GitHub state.
- Commands: `npm --prefix automation/seo-agent run format`; `npx --prefix automation/seo-agent prettier --write .github/workflows/seo-agent-offline.yml`; `SEO_AGENT_OFFLINE_CI=true` runner-preflight/provenance fixture; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `4.1s` — formatting and whitespace checks passed. The preflight found no prohibited credential variable and rendered only an ignored local provenance fixture with commit, tree, runtime, and sidecar lock hashes.
- Commands: `npm --prefix automation/seo-agent ci --offline --ignore-scripts --omit=peer`; `npm --prefix automation/seo-agent run ci:offline`; `npm run lint`; `npm run typecheck`; `npm run build`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `121.6s` — the isolated offline install added 189 sidecar packages with zero audit findings; lint, format, typecheck, 58 deterministic tests (including the workflow fixture), 9 runtime integration fixtures, five Eve eval cases/20 gates, fixture audit, rollback drill, independent review, Eve build/manifest, and root lint/typecheck/28-page build all passed. Root build retained only the known non-failing stale Browserslist and Edge-runtime/static-generation warnings. The local Eve eval host again emitted a non-fatal post-success queue socket-hang-up warning during shutdown after its successful exit.
- Evidence: `.github/workflows/seo-agent-offline.yml`, `automation/seo-agent/ci/self-hosted-runner-policy.json`, `automation/seo-agent/ci/SELF_HOSTED_RUNNER.md`, `automation/seo-agent/scripts/verify-self-hosted-runner.mjs`, `automation/seo-agent/scripts/record-offline-provenance.mjs`, and `automation/seo-agent/tests/self-hosted-ci.test.mjs`.
- Verification classification: `MOCK_VERIFIED` for source, static policy, local preflight, and deterministic offline checks. `BLOCKED_MISSING_CREDENTIALS` for a provisioned self-hosted runner, actual GitHub Actions execution, host firewall verification, retained artifact delivery, and every live integration. No workflow run, GitHub mutation, deployment, preview, production change, or secret file was created.
- Phase-caused correction: the first preflight attempted to spawn `npm` directly, which fails on this Windows runtime despite npm being available via its launcher. Both new scripts now resolve npm through the active Node CLI entry point, preserving the existing cross-platform verifier pattern; the corrected fixture passed. No test was weakened or skipped.
- Checkpoint: `008d83c` (`Add self-hosted offline CI contract`) committed on `feat/eve-seo-agent`; no merge, push, workflow run, deployment, production configuration, or secret file was created.
- Post-checkpoint command: `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `97.9s` — from the clean `008d83c` tree, root lint/typecheck and the public Next build generated all 28 static pages; control-plane discovery, 58 sidecar tests, 9 runtime integration fixtures, five Eve evals/20 gates, fixture audit, rollback drill, independent readiness review, Eve build/manifest, and production dependency advisory passed. It emitted `MOCK_VERIFIED` for tree `8427a18cbc00988a3d2e76a3f1e9cdc8bd977a7d`. The outer diagnostic then failed only while PowerShell misparsed a separate `git rev-parse HEAD^{tree}` query after the verifier had completed; the safely quoted follow-up returned the same tree hash and a clean worktree.
- Phase extension: the CI provenance artifact now includes the executed command, fixture-tree revision, verifier exit code, and elapsed seconds in addition to the commit/tree, lock, and Node/npm versions. The workflow deliberately records this result after both success and failure, then exits with the original verifier code. Deterministic fixtures prove both a retained nonzero result (`7`) and malformed input rejection. The GitHub Actions security review found no exploitable external-attacker path: the only workflow has no PR/fork trigger, no cache/download path, explicit `contents: read`, no secret/OIDC/environment reference, no expression in a shell command, and full-SHA official actions.
- Commands: `npm --prefix automation/seo-agent run format`; Prettier over workflow/status docs; `npm --prefix automation/seo-agent run ci:offline`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `51.9s` — lint, formatting, typecheck, source/deployment/agent-boundary verification, 60 deterministic tests, 9 runtime integration fixtures, five Eve evals/20 gates, fixture audit, rollback drill, independent review, and Eve build/manifest validation passed. The local Eve eval host again emitted the known non-fatal post-success queue socket-hang-up warning during shutdown after exit `0`.
- Checkpoint: `2654960` (`Record complete offline CI provenance`) committed on `feat/eve-seo-agent`; no merge, push, workflow run, deployment, production configuration, or secret file was created.
- Post-checkpoint command: `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `103.4s` — from the clean `2654960` tree, root lint/typecheck and the public Next build generated 28 static pages; control-plane discovery, 60 sidecar tests, 9 runtime integration fixtures, five Eve evals/20 gates, fixture audit, rollback drill, independent readiness review, Eve build/manifest, and production dependency advisory passed. It emitted `MOCK_VERIFIED` for tree `9885bdd9e68329f558607d6b6c90a1b0173d69f1`. The only non-failing diagnostics were stale Browserslist data, two Edge-runtime/static-generation cautions, and the known post-success local Eve queue socket-hang-up warning.
- Next exact action: commit this source checkpoint, run the clean-tree canonical verifier, then obtain human provisioning evidence for the dedicated `wades-seo-offline` runner before treating the workflow as operational.

## Phase 4 — Core Eve Runtime (2026-07-30)

- State: COMPLETE for the offline runtime contract (`MOCK_VERIFIED`); deployed Eve, native Cron delivery, AI Gateway, Vercel Workflow execution, and all external integrations remain `BLOCKED_MISSING_CREDENTIALS` or `BLOCKED_MISSING_RUNTIME`.
- Branch: `feat/eve-seo-agent`.
- Changed scope: native Eve schedule run handoff; a backend-only runtime channel with health, readiness, and protected Cron endpoints; bounded audit orchestration workflow/tool; deterministic run descriptor/idempotency/continuation identity; Git-backed completion-history duplicate denial; in-memory dispatch locks/circuit state; runtime modes, mutation kill switch, time/step budgets, typed failure taxonomy, bounded retries, timeouts, and offline integration fixtures. No public-site source, dependency, client bundle, Vercel project, credential, GitHub write, content edit, PR, merge, deployment, or production configuration changed.
- Verified platform boundary: installed Eve `0.27.13` exposes `defineSchedule({ run })`, `defineChannel`, channel `send`, active-session lookup, and task-mode sessions. The runtime uses that surface only: the native schedule starts the runtime channel, and the route returns once Eve supplies a durable session ID. The separately protected `/api/cron` endpoint uses timing-safe comparison only when production external Cron authentication is configured.
- Commands: `npm run format; npm run lint; npm run format:check; npm run typecheck; npm run test; npm run test:integration; npm run build; git diff --check` in `automation/seo-agent` with Node `25.9.0` first on `PATH` — exit `0` — `28.6s` combined — lint and formatting passed, TypeScript passed, 53 deterministic unit/contract tests and 9 runtime integration fixtures passed, and Eve built with the compiled native schedule/runtime channel/orchestrate-tool manifest verified.
- Commands: `npm run seo-agent:control-plane; npm --prefix automation/seo-agent run ci:offline; git diff --check` from the repository root with Node `25.9.0` first on `PATH` — exit `0` — `32.0s` combined — 13 control-plane files and disabled completion hook were discovered; sidecar lint/format/typecheck, contract/architecture/boundary checks, 53 unit/contract tests, 9 runtime integration fixtures, audit fixture, human-only rollback drill, independent readiness review, Eve build/manifest, and production dependency policy passed.
- Evidence: `automation/seo-agent/src/runtime.mjs`, `automation/seo-agent/src/workflows/audit.ts`, `automation/seo-agent/agent/channels/runtime.ts`, `automation/seo-agent/agent/schedules/audit.ts`, `automation/seo-agent/agent/tools/orchestrate.ts`, `automation/seo-agent/tests/runtime.test.mjs`, `automation/seo-agent/scripts/verify-eve-manifest.mjs`, and `automation/seo-agent/README.md`.
- Verification classification: `MOCK_VERIFIED` for all local runtime paths. The test suite starts no real Eve/Vercel session, makes no network request, and does not write a Git-backed run artifact. `BLOCKED_MISSING_CREDENTIALS` remains the truthful state for deployed/provisioned Eve, Vercel Cron, AI Gateway, Vercel Workflow, Sandbox, Connect, GitHub, Search Console, PageSpeed, browser, preview, and production evidence.
- Failures / limitations: in-memory locks and circuit state protect a warm instance only; cross-instance authority is the stable Eve continuation token plus Git-backed completed-run validation, whose actual provider behavior requires a deployed sidecar proof. The protected external dispatcher is intentionally not configured as an additional Vercel Cron, because Eve's native schedule already emits the sidecar Cron metadata. The `propose` mode does not enable a mutation: no real publisher exists in this phase, and all paths remain audit-only.
- Next exact action: run the full deterministic sidecar gate, commit this runtime checkpoint, then obtain human authorization to create/link the isolated sidecar project and execute the manually approved first audit-only production run.
- Checkpoint: `003da7e` (`Add Eve runtime control plane`) committed on `feat/eve-seo-agent`; no merge, push, deployment, or production configuration change occurred.
- Post-checkpoint commands: `npm run verify:all` from a clean repository tree with Node `25.9.0` first on `PATH` — exit `0` — `70.2s` — root lint/typecheck and the unchanged public Next build passed, generating all 28 static pages; control-plane discovery, sidecar lint/format/typecheck, 53 unit/contract tests, 9 runtime integration fixtures, audit fixture, rollback drill, independent review, Eve build/manifest, and production dependency advisory also passed. The verifier bound its `MOCK_VERIFIED` result to tree `ef45961efefec79743462630115971f54dc6705c`.
- Post-checkpoint commands: `bun install --frozen-lockfile` at the repository root with Bun `1.1.24` — exit `0` — `1.2s` — checked 1,473 packages with no lockfile or working-tree change. This confirms the existing public-site dependency install remains unpolluted by the sidecar phase.
- Phase-caused correction: before model-facing orchestration, the runtime now recomputes and timing-safely compares the idempotency key derived from the audit run ID. A syntactically valid but substituted key is rejected before durable-workflow dispatch. `npm run format; npm run ci:offline; git diff --check` in `automation/seo-agent` with Node `25.9.0` first on `PATH` — exit `0` — `36.2s` — lint/format/typecheck, 53 unit/contract tests, 9 runtime integration fixtures, audit fixture, rollback drill, readiness review, Eve build, and manifest verification passed. This correction is committed as `9e74890` (`Bind runtime work packets to idempotency keys`).
- Phase extension: the orchestrator now emits nine deterministic `SEO_AUDIT_WORK_PACKET` records—one for each declared specialist—before a provider can be dispatched. Each packet is linked to the run's SHA-256 idempotency key, carries an evidence-only input and report-only output contract, explicitly forbids content and PR mutation, and is rejected if aggregate step, tool-call, token, or wall-time limits would exceed the run budget. The per-request cost reservation is now `$0.20`, allowing all nine bounded packets to remain within the existing `$2.00` maximum run budget; this is a conservative local reservation, not a provider price claim.
- Commands: `npm run format; npm run ci:offline; npm run test:integration; git diff --check` in `automation/seo-agent` with Node `25.9.0` first on `PATH` — exit `0` — `43.8s` — lint, formatting, typecheck, source/deployment/agent-boundary checks, 55 deterministic tests, 9 runtime integration fixtures, read-only fixture audit, human-only rollback drill, independent readiness review, Eve build, and compiled manifest validation passed. The fixture audit emitted no writes; the review remained `NOT_READY` only for its seven live-integration prerequisites.
- Post-checkpoint command: `npm run verify:all` at repository root with Node `25.9.0` first on `PATH` — exit `0` — `78.7s` — from the clean `33dd8a6` tree, public-site lint and typecheck passed and the unchanged Next.js application generated 28 static pages. Control-plane discovery, all 55 sidecar tests, 9 runtime integration fixtures, zero-write audit fixture, rollback drill, independent readiness review, Eve build/manifest verification, and the production dependency advisory also passed; the verifier bound its `MOCK_VERIFIED` evidence to tree `49a405d5d607a1eec82dc817771df95401e12662`. Existing non-failing Next build warnings remain: stale Browserslist data and the two Edge-runtime static-generation notices.
- Phase extension: added the required native Eve eval tier. `eve eval --strict` now drives the sidecar through five deterministic `mockModel` cases and 20 hard gates for audit-only/no-change behavior plus direct-main/merge/deploy, prompt injection/secret exfiltration, fabricated review/job/unsupported-area/unsafe-advice, and duplicate/doorway/stuffing refusals. The fixture model exists only while the wrapper sets `SEO_AGENT_EVAL_FIXTURE=true`; regular local and deployed agent paths retain the central approved AI Gateway route.
- Phase extension: added a typed Vercel Sandbox smoke adapter. It is explicit about `node24`, `networkPolicy: "deny-all"`, one benign Node stdout command, a 15-second command timeout, and unconditional cleanup. Its fixture is `MOCK_VERIFIED`; its CLI correctly returns `BLOCKED_MISSING_CREDENTIALS` before an explicit production-sidecar approval and Vercel OIDC/scoped token. No Sandbox was created in this environment.
- Commands: `npm run format; npm run ci:offline; npm run sandbox:integration; git diff --check` in `automation/seo-agent` with Node `25.9.0` first on `PATH` — exit `0` — `46.9s` — lint, formatting, typecheck, source/deployment/agent-boundary checks, 57 deterministic unit/contract tests, 9 runtime integration fixtures, five native Eve eval cases with 20 passed gates, read-only fixture audit, human-only rollback drill, independent readiness review, Eve build, and compiled manifest validation passed. `sandbox:integration` returned the expected `BLOCKED_MISSING_CREDENTIALS` record without a network call. The audit fixture emitted no writes and the review remained `NOT_READY` only for seven live-integration prerequisites.
- Phase-caused corrections: the first `eve eval` run exposed (1) missing mock-model context-window metadata, corrected with a fixture-only context-window value while production remains catalog-driven; (2) an obsolete `agent/tools/sandbox.ts` stub that current Eve rejects because Sandbox is not a framework tool, removed with a regression boundary check; (3) one fixture refusal precedence issue, corrected so duplicate URLs are classified before generic city claims; and (4) a central-model verifier pattern updated to accept only the central test metadata import. No test was weakened, removed, or skipped.
- Evidence: `automation/seo-agent/evals/`, `automation/seo-agent/src/sandbox-integration.mjs`, `automation/seo-agent/tests/sandbox-integration.test.mjs`, `automation/seo-agent/scripts/run-evals.mjs`, `automation/seo-agent/scripts/run-sandbox-integration.mjs`, `automation/seo-agent/agent/model.ts`, and `docs/seo-agent/MANUAL_SETUP.md`.
- Verification classification: native evals, unit/contract fixtures, and Sandbox call-shape fixture are `MOCK_VERIFIED`; real Sandbox is `BLOCKED_MISSING_CREDENTIALS`. Eve's temporary local eval host wrote a non-fatal `[world-local] Queue message failed ... socket hang up` during shutdown after all five cases passed and its command exited `0`; no provider action, state mutation, or test failure was observed.
- Checkpoint: `346adc2` (`Add native Eve policy evals`) on `feat/eve-seo-agent`; no deployment, GitHub mutation, real Sandbox creation, or persistent run-artifact write occurred.
- Post-checkpoint command: `npm run verify:all` at repository root with Node `25.9.0` first on `PATH` — exit `0` — `116.5s` — from the clean `a27ea1b` tree, public-site lint/typecheck and the unchanged Next.js build generated 28 static pages. Control-plane discovery, 57 sidecar tests, 9 runtime integration fixtures, 5 native Eve eval cases with 20 gates, zero-write fixture audit, rollback drill, independent readiness review, Eve build/manifest, and production dependency advisory passed. The verifier bound its `MOCK_VERIFIED` evidence to tree `2e098b7bc66f42727f5725d4911e0aa2f1100887`. Existing non-failing Next warnings remain the stale Browserslist data and two Edge-runtime/static-generation notices; the local Eve eval host again emitted the non-fatal post-success queue socket-hang-up warning during shutdown.
- Phase-caused correction: the first validation after lowering the model reservation surfaced two stale assertions that assumed a `$2.00` per-request reservation. They were corrected to assert the bounded `$0.20` reservation and a configured `$0.30` run-cap refusal; no test was weakened or removed.
- Evidence: `automation/seo-agent/src/work-packets.mjs`, `automation/seo-agent/src/runtime.mjs`, `automation/seo-agent/tests/work-packets.test.mjs`, `automation/seo-agent/tests/runtime.test.mjs`, `automation/seo-agent/tests/model-budget.test.mjs`, and `docs/seo-agent/DEFINITION_OF_DONE.md`.
- Checkpoint: `babbad2` (`Add typed audit work packets`) on `feat/eve-seo-agent`; no network request, provider call, GitHub mutation, preview, deployment, or persistent run-artifact write occurred.
- Next exact action: obtain human authorization to create/link the isolated sidecar project and execute the manually approved first audit-only production run. Keep the native schedule disabled and do not configure the external Cron dispatcher until that audit is reviewed.

## Phase 7 — Draft-Publication Contract (2026-07-30)

- State: COMPLETE for the offline contract (`MOCK_VERIFIED`); real GitHub staging and draft-PR execution remain `BLOCKED_MISSING_CREDENTIALS` and human authorization.
- Changed scope: a bounded Markdown change-set contract and the draft-PR adapter contract only. It preserves the existing application source of truth: no page, WordPress template, public route, content migration, branch, GitHub object, deployment, or production configuration changed.
- Verified platform boundary: the installed `@github-tools/sdk` `1.8.2` Eve factory permits approval and output overrides but provides no input-schema override. Its direct `createPullRequest` tool would retain upstream repository/base/head/draft input authority, so it remains excluded from the model. The publication path accepts only an injected human-approved gateway after it returns a staged feature-branch commit and then a draft PR.
- Commands: `npm --prefix automation/seo-agent run format`; `npm --prefix automation/seo-agent run test`; `npm --prefix automation/seo-agent run verify`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.9s` combined — 42 deterministic tests passed, including rejection of unsafe Markdown paths, secret-like content, oversized diffs, digest tampering, missing staging, non-draft PR responses, and malformed PR metadata.
- Commands: `npm --prefix automation/seo-agent run ci:offline` with Node `25.9.0` first on `PATH` — exit `0` — `11.6s` — lint, formatting, typecheck, source/deployment/agent-boundary checks, 42 deterministic tests, audit-only fixture, human-only rollback drill, and the compiled Eve manifest passed. The audit fixture remained zero-write and `MOCK_VERIFIED` for Git tree `6a53fb2ca23a18327c433048e0c1149e48835201`.
- Evidence: `automation/seo-agent/src/markdown-change-set.mjs`, `automation/seo-agent/src/publishing.mjs`, `automation/seo-agent/tests/policy.test.mjs`, and the pinned GitHub Tools type declarations under the sidecar lockfile.
- Verification classification: `MOCK_VERIFIED` for deterministic change-set and publication-contract behavior; `BLOCKED_MISSING_CREDENTIALS` for a scoped GitHub Connect gateway, an approved Markdown migration, human approval records, branch staging, and real draft-PR proof.
- Failures / limitations: the repository has not approved a Markdown source migration, and no provider mutation was performed. The contract does not assert that an injected boolean is a platform approval; a future live gateway must obtain approval through its configured Vercel/GitHub control plane and record evidence.
- Next exact action: after human approval of the Markdown migration and a least-privilege GitHub Connect publisher gateway, run the staged branch and draft-PR integration proof against a disposable reviewed proposal; do not expose the raw GitHub write factory to the model.

## Phase 5 — Browser-Integration Truth Boundary (2026-07-30)

- State: COMPLETE for offline adapter contracts (`MOCK_VERIFIED`); real browser automation remains `BLOCKED_MISSING_CREDENTIALS`.
- Changed scope: browser evidence semantics, optional provider contract, integration inventory, probe coverage, formatting scope, and setup documentation only. No browser provider, external browser session, credential, network request, public-site change, deployment, or GitHub mutation occurred.
- Verified platform boundary: this sidecar has no bundled browser provider dependency, and no Vercel-native browser execution API was verified in the installed Eve/Connect packages. Existing allowlisted `fetch` research is retained as HTTP public-document retrieval and is no longer an implied automation capability.
- Commands: `npm --prefix automation/seo-agent run format`; `npm --prefix automation/seo-agent run test`; `npm --prefix automation/seo-agent run verify`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `3.6s` combined — 43 deterministic tests passed. Fixtures prove missing browser providers block safely; injected provider results are budgeted, domain-checked, classified separately, and cannot follow an unapproved redirect.
- Commands: `npm --prefix automation/seo-agent run live:probe`; `npm --prefix automation/seo-agent run ci:offline`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `15.3s` combined — the no-network probe emitted `BLOCKED_MISSING_CREDENTIALS` records for both `browser` and `browser_automation` along with all other integrations. Lint, format, typecheck, source/deployment/agent-boundary checks, 43 deterministic tests, audit fixture, rollback drill, and Eve build/manifest checks passed.
- Evidence: `automation/seo-agent/src/adapters.mjs`, `automation/seo-agent/src/probes.mjs`, `automation/seo-agent/tests/adapters.test.mjs`, `seo/manifests/integration-inventory.json`, and `docs/seo-agent/MANUAL_SETUP.md`.
- Verification classification: `MOCK_VERIFIED` for the adapter distinctions and injected-provider fixture; `BLOCKED_MISSING_CREDENTIALS` for a reviewed browser provider and real browser session evidence.
- Failures / limitations: HTTP retrieval remains a read-only research path, not a rendered-browser, DOM-interaction, screenshot, SERP, or authentication capability. It may not be used to claim browser automation is operational.
- Next exact action: if browser automation becomes necessary after the first audit-only production run, obtain human approval for a specific isolated provider and add its reviewed adapter plus a redacted, rate-limited live probe; otherwise retain the explicit blocked record.

## Phase 3 — Observability Contract (2026-07-30)

- State: COMPLETE for deterministic manifest coverage (`MOCK_VERIFIED`); live Eve/Vercel trace delivery remains `BLOCKED_MISSING_CREDENTIALS`.
- Changed scope: Git-backed run-manifest schema validation, audit-only fixture observability, and deterministic negative tests only. No trace provider, credential, telemetry export, network request, public-site change, connector, deployment, or mutation occurred.
- Commands: `npm --prefix automation/seo-agent run format`; `npm --prefix automation/seo-agent run test`; `npm --prefix automation/seo-agent run ci:offline`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `16.1s` combined — 43 deterministic tests passed along with lint, formatting, typecheck, source/deployment/agent-boundary checks, zero-write audit fixture, rollback drill, and Eve build/manifest validation.
- Evidence: `automation/seo-agent/src/contracts.mjs`, `automation/seo-agent/src/audit.mjs`, `automation/seo-agent/tests/contracts.test.mjs`, and `automation/seo-agent/tests/audit.test.mjs`.
- Verification classification: `MOCK_VERIFIED` for complete offline observability fields and rejection fixtures; `BLOCKED_MISSING_CREDENTIALS` for live trace/telemetry export and provider-visible latency correlation.
- Failures / limitations: the first local implementation attempt treated explicitly nullable fields as absent, causing seven phase-introduced test failures (`observability missing denial_reason` or `draft_pr_url`). The contract was corrected to require field presence while allowing `null`; the final full sidecar offline run passed. No baseline failure was altered or suppressed.
- Next exact action: after the sidecar project and AI Gateway/Vercel observability are approved, bind the same fields to a redacted live correlation ID and record its evidence without including a secret-bearing trace payload.

## Phase 2 — Sidecar Foundation Re-verification (2026-07-30)

- State: COMPLETE (`MOCK_VERIFIED`); separately deployed runtime remains `BLOCKED_MISSING_CREDENTIALS`.
- Changed scope: verification evidence and Phase 2 acceptance wording only. No public-site source, dependency, client bundle, Vercel project, credential, connector, content, GitHub mutation, or deployment changed.
- Commands: `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `22.4s` — clean-tree control-plane discovery, sidecar lint/format/typecheck, 41 deterministic tests, audit-only fixture, human-only rollback drill, Eve build/manifest validation, and the separately labelled production dependency advisory passed. The fixture is zero-write and `MOCK_VERIFIED` for Git tree `710333a0673380aadc48295dff19ddd4d61aa661`.
- Commands: `npm run build` at the repository root with Node `25.9.0` first on `PATH` — exit `1` — `14.8s` — the public site compiled successfully, then page-data collection stopped at `/sitemap-j8qpfc` because the existing Supabase URL was absent. This is the same pre-existing public-site environment limitation; the sidecar is not imported into the root package or client bundle and no public configuration was changed.
- Evidence: `automation/seo-agent/package.json`, `automation/seo-agent/package-lock.json`, `automation/seo-agent/vercel.json`, `automation/seo-agent/.env.example`, `automation/seo-agent/AGENTS.md`, and `docs/seo-agent/SIDECAR_ARCHITECTURE.md`.
- Verification classification: `MOCK_VERIFIED` for isolated sidecar install/build/test behavior and public dependency boundary; `BLOCKED_MISSING_CREDENTIALS` for the real sidecar project, credentials, and public-site Supabase-backed build completion.
- Failures / limitations: no sidecar Vercel project is linked or deployed; no cloud credential, Connect connector, Cron delivery, preview, browser automation, or production run was attempted. The root public build remains blocked by missing Supabase configuration and retains its pre-existing build-time type/lint suppressions, which this phase did not modify.
- Next exact action: obtain explicit human approval to create and link a separate Vercel project rooted at `automation/seo-agent`, configure only scoped read-only integrations from `MANUAL_SETUP.md`, then perform the first audit-only production run.

## Completed Work

- Created repository and scoped instructions for the future sidecar, SEO evidence, and Markdown-content boundaries.
- Added the authoritative build specification, phased execution plan, machine-oriented Definition of Done, manual setup runbook, and imperative repository skill.
- Designed a no-op-until-enabled completion Stop hook. The enabling sentinel is deliberately absent.
- Added a deterministic, dependency-free control-plane checker and package-script entry point.
- Added the isolated Eve sidecar package, native Eve/Vercel Cron schedule, durable audit workflow entry point, read-only GitHub tool definition, Vercel MCP connection definition, AI Gateway/Sandbox entry points, and role-specific agents.
- Compiled the Eve app locally with Node `25.9.0`; its native audit schedule and nine declared role agents are discoverable. Generic shell, filesystem-write/read, unscoped web, todo-state, and implicit-agent tools are explicitly disabled at the root agent boundary.
- Added scoped instructions and explicit disabled-tool declarations to every declared specialist agent. The deployed compiled manifest is now checked after every build, proving that all nine specialists inherit none of the generic shell, filesystem, web, todo, or implicit-agent tools.
- Restricted the Vercel MCP connection to human-approved read-only project and deployment metadata. Deployment, CLI, environment, access-link, raw-log, and general-fetch MCP capabilities are excluded by source-tested allowlist.
- Added deterministic architecture verification for backend-only isolation, absence of UI/database dependencies, absence of persistent database imports, and presence of Git-backed SEO manifests.
- Replaced configured-but-unimplemented GitHub and Vercel placeholders with typed, read-only clients. Added typed non-secret environment configuration, read-only Search Console/PageSpeed/browser contracts, redacted evidence shaping, an explicit human-gated live-probe command, and an audit workflow path that defaults to blocked evidence unless a human enables live reads.
- Tightened browser research so an enabled adapter must match the explicit, separately configured browser-domain allowlist; membership in the broader service-network allowlist is insufficient. A deterministic denial fixture proves it cannot fetch a GitHub API target as browser research.
- Corrected evidence timing semantics: offline fixtures retain a fixed epoch timestamp for reproducibility, while configured/live evidence and run manifests receive a real retrieval timestamp. Malformed timestamps are now rejected by the contracts.
- Replaced the model-visible raw Sandbox-creation tool with an explicit Eve Vercel Sandbox policy: Node 24, 300-second bound, and deny-all network egress. The compiled manifest now proves the policy, the bounded native Cron schedule, and disabled raw Sandbox tool.
- Added an AI Gateway request-budget preflight. Model calls now fail before dispatch when estimated UTF-8 input tokens plus requested output tokens exceed the versioned per-run token cap; the existing model tool still requires Gateway/OIDC credentials before making any call.
- Enforced audit reliability controls: duplicate run IDs are denied before collection, and live read probes retry only transient `408`, `429`, and `5xx` failures at most three times. Policy and authorization failures fail immediately without retries.
- Added explicit agent semantic-version, prompt-hash, and skill-hash fields to audit manifests. The contracts reject artifact bundles that omit these observability records, so evaluated behavior can be correlated with the exact agent semantics and loaded-skill set without recording secret-bearing prompt content.
- Expanded adversarial policy evaluation so every prohibited action is denied, including repository/settings/secret changes, non-draft PRs, deployment promotion, and automatic rollback. Suspected browser prompt injection now produces a redacted, hashed `SECURITY_ESCALATED` evidence event that blocks tool execution, content creation, policy changes, and credential access.
- Added a deterministic human-only rollback drill record and runbook validation. The drill records required human actions and provenance, makes no provider or repository write, and rejects any attempted autonomous rollback.
- Added a versioned adversarial-policy fixture for prompt injection, deceptive content, credential exfiltration, policy bypass, and unsafe septic/chemical advice. Markdown drafts use the same deny policy, while proposal validation requires evidence and a duplicate/cannibalization assessment and rejects a new URL that duplicates an existing canonical page.
- Hardened the draft-PR publisher contract: it requires a canonical PR packet, a trimmed non-default feature branch, human approval, `draft=true`, and valid returned PR metadata. A local gateway fixture is classified `MOCK_VERIFIED`; missing or malformed live gateway state remains blocked rather than assumed operational.
- Hardened preview and production audit contracts. A preview can be ready only with HTTPS, a valid commit and timestamp, explicit non-indexability, no critical findings, and non-blocked evidence; a production audit is read-only and must bind its deployment commit SHA to the human merge SHA.
- Added shared per-run budget metering. Every attempted adapter request consumes the external-request cap before dispatch, enabled browser research also consumes a browser-page slot, and configured scheduled audits consume a run slot before collection. Audit manifests now preserve budget limits and usage for reproducible review.
- Centralized AI Gateway model routing in a versioned allowlist used by the root and every specialist agent. The model tool now rejects an unapproved route, over-token request, or over-cap per-request cost reservation before credential checks or provider dispatch. Aggregate live billing and trace proof remain blocked pending the approved Gateway connection.
- Added an atomic run-scoped model ledger. Each model request requires a safe audit run ID and reserves tool-call, token, and cost limits before credential or provider dispatch; rejected reservations do not mutate usage. The ledger is ephemeral execution accounting only, and terminal usage must be copied into Git-backed run artifacts when a live Gateway integration is approved.
- Added read-only Git-backed run-history validation before configured audit collection. Canonical manifests must validate and match their directory run ID; any recorded matching run ID, malformed history, or unsafe history entry fails closed before an adapter probe can start.
- Moved live-read authorization into the shared probe boundary rather than relying on a CLI check. Every caller now requires the production sidecar environment, an explicit human approval flag, and an exact approved audit run ID before any adapter is asked to probe; authorized-but-uncredentialed fixtures remain `BLOCKED_MISSING_CREDENTIALS` without network access.
- Added a validated, redacted `seo/manifests/integration-inventory.json` for all required and optional adapters. Its current state is blocked for every integration, based on the verified absence of sidecar project linkage and scoped credential names in this environment.
- Added a content-addressed public Santa Cruz County research packet and a publication-blocked opportunity backlog. The packet is limited to regulatory/infrastructure context and is not an approved Wade's service-area, service-availability, customer-demand, legal-advice, or content-publication claim.
- Added canonical Git-artifact proposal rendering for audit manifests and evidence. It permits only `seo/runs/<run-id>/manifest.json` and content-addressed `seo/evidence/*.json` paths, performs no implicit local write, and remains subject to the existing feature-branch and human-approval gate before any draft-PR publication.
- Corrected the root clean-tree verifier to remove its stale dependency on an ignored local audit file. It now relies on the sidecar's deterministic audit fixture and reports the checked Git tree hash without creating persistent local state.
- Added versioned page inventory, link graph, approved-fact registry, query ownership backlog, experiments registry, evidence requests, opportunity scoring, Markdown proposal validation, preview/production audit gates, budgets, retry/idempotency helpers, and draft-PR fail-closed behavior.
- Added a root `npm run verify:all` contract that requires a clean tree and emits audit-only evidence associated with the checked tree hash.
- Added a root `npm run seo-agent:verify` deterministic gate. It invokes the control-plane checker, every sidecar offline check, and the Eve build without conflating a registry-backed dependency advisory with an offline CI result. The canonical clean-tree `npm run verify:all` now composes this gate with the explicitly labelled production-dependency advisory.
- Refreshed the Phase 2 sidecar foundation against the installed Eve `0.27.13` tooling: the isolated package now declares independent lint, format, typecheck, test, dry-run, build, deployment-config, and production-readiness commands with an updated sidecar-only lockfile.
- Added `automation/seo-agent/vercel.json`, a tracked variable-name-only `.env.example`, explicit ignored real `.env*`/Vercel-local state, and a `SIDECAR_ARCHITECTURE.md` runbook for separate public-site and sidecar Vercel projects. No project was linked or deployed.
- Added a typed deployed-sidecar readiness gate. A real production sidecar must identify itself as production and provide Vercel OIDC or an AI Gateway credential; every research adapter remains optional and fails closed as `BLOCKED_MISSING_CREDENTIALS`.
- Corrected the Sandbox source to match the installed Eve API: Eve owns the framework sandbox image, while the sidecar explicitly retains deny-all network egress. The unsupported runtime override is removed and source verification covers the valid policy.

## Current Failures and Limitations

- `docs/seo-agent/BASELINE.md` referenced by the prior conversation is absent from the reachable commit graph at this checkout; commit `02faf11` is unknown here. Prior baseline test claims are therefore not treated as current verification evidence.
- A committed self-hosted CI workflow source contract now exists, but no `wades-seo-offline` runner, host firewall proof, GitHub Actions execution, or artifact retention record exists; operational CI is `BLOCKED_MISSING_CREDENTIALS`.
- The Eve sidecar is implemented and verified through offline fixtures, but no Vercel sidecar project, Connect connector, AI Gateway configuration, Sandbox policy, enabled Cron delivery, Search Console/PageSpeed credential, preview proof, or production proof is configured or verified by this checkpoint.
- This CI checkpoint did not modify the root package, Bun lockfile, client bundle dependencies, routes, or public Vercel configuration. The current root lint/typecheck/build gate passes locally and generates 28 static pages.
- The offline technical SEO inventory has no high/critical finding. Existing non-failing root-build warnings are stale Browserslist data and two Edge-runtime/static-generation cautions; they are not sidecar or CI failures.
- The default shell runtime remains Node `22.21.1`, but a separately installed Node `25.9.0` runtime successfully builds the sidecar when placed first on `PATH`. The self-hosted runner must still pin a supported Node 24-or-newer runtime; this local Windows path is not runner evidence.
- A default npm peer resolution installs an unused optional Workflow SDK bundle from GitHub Tools and reports 12 high-severity findings through its CLI/framework dependencies. The enforced sidecar production install/audit uses `--omit=peer`; `npm audit --omit=dev --omit=peer --audit-level=high` returns zero high/critical findings. Eve supplies the durable agent-session mechanism; the sidecar has no direct Workflow SDK import.
- `bun install --frozen-lockfile` completes with the existing Bun `1.1.24` lockfile. The root `npm run build` now completes locally without a configured Supabase URL through the existing empty-read fallback; that does not prove a configured Supabase project or production data path works. The sidecar does not provide, read, or alter public-site credentials.

## External Blockers

- Human approval is required before creating/linking Vercel sidecar projects, connectors, production-access scopes, scheduled jobs, or any live adapter.
- The actual source-of-truth plan for migrating current routed content to Markdown must be approved before implementation; this checkpoint defines the boundary but performs no migration.

## Next Exact Action

Human approval is required to create/link the separate Vercel sidecar project and configure read-only connectors as described in `MANUAL_SETUP.md`. After that approval, run the first manual audit-only production audit, capture redacted evidence, and only then enable the native schedule.

## Phase 2 — Sidecar Foundation Refresh (2026-07-30)

- State: COMPLETE (offline foundation); deployment state remains `BLOCKED_MISSING_CREDENTIALS`.
- Branch: `feat/eve-seo-agent`.
- Changed scope: isolated sidecar tooling and lockfile; backend-only Vercel config; typed production readiness; environment template/ignore policy; two-project documentation; no public-site source, dependency, route, Vercel project, credential, connector, mutation, or deployment change.
- Official-tooling verification: local Eve `0.27.13` declares Node `>=24`; local Node `25.9.0`, npm `11.12.1`, and Bun `1.1.24` were used. Installed Eve declarations confirm the Vercel backend owns the Sandbox image and accepts direct `"deny-all"` network policy.
- Commands: `npm ci --ignore-scripts --omit=peer` in `automation/seo-agent` — exit `0` — `13.0s` — isolated lockfile installed 189 packages with zero reported vulnerabilities after stopping a stale local Eve development process that had locked a native binding.
- Commands: `npm run ci:offline` in `automation/seo-agent` with Node `25.9.0` first on `PATH` — exit `0` — `18.4s` — lint, foundation-format check, TypeScript check, sidecar/source boundaries, 40 deterministic tests, audit-only fixture, rollback drill, production dependency audit, Eve build, and compiled manifest verification passed.
- Commands: `npm run dry-run` — exit `0` — included in the same `18.4s` verification window — emitted only a `MOCK_VERIFIED`, zero-write audit artifact proposal.
- Commands: `npm run runtime:preflight` with no production variables — expected exit `1` — enforced `SEO_AGENT_ENV=production` before accepting OIDC/Gateway configuration. This is an intentional `BLOCKED_MISSING_CREDENTIALS` readiness result, not a failing deployment claim.
- Commands: `bun install --frozen-lockfile` at repository root — exit `0` — `27.7s` — existing public-site lockfile install succeeded without a root package or lockfile modification.
- Commands: `npm run build` at repository root with Node `25.9.0` — exit `1` — `28.6s` — compiled successfully, then failed collecting `/services/[slug]` and `/about-us` because the pre-existing public-site Supabase URL is not configured locally. No sidecar code participates in that failure.
- Evidence: `automation/seo-agent/vercel.json`, `automation/seo-agent/.env.example`, `automation/seo-agent/package-lock.json`, `docs/seo-agent/SIDECAR_ARCHITECTURE.md`, and `automation/seo-agent/scripts/verify-deployment-config.mjs`.
- Verification classification: `MOCK_VERIFIED` for sidecar implementation and local build; `BLOCKED_MISSING_CREDENTIALS` for deployment/runtime readiness and public-site local Supabase configuration.
- Failures / limitations: initial `npm ci` encountered `EPERM` on Eve's Rolldown binding because a confirmed stale sidecar `eve dev` process held the file. Only those two stale sidecar processes were stopped; the clean reinstall then passed. No Vercel sidecar project, AI Gateway, Connect connector, Cron delivery, live browser/API call, preview, or production audit is verified.
- Next exact action: obtain human approval to create the separate Vercel sidecar project with Root Directory `automation/seo-agent`, then follow `MANUAL_SETUP.md` for scoped, read-only configuration and the first audit-only production run.

## Checkpoint Verification (2026-07-30)

- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `8.9s` — clean-tree verification passed for checkpoint `8ea538e` (tree `ea167a2d8bc636546cba71869ddeca8b4f553be2`); it confirmed 12 discoverable control-plane files, a disabled completion-hook sentinel, sidecar deployment/source boundaries, 40 deterministic tests, zero high/critical production dependency findings, and a `MOCK_VERIFIED` zero-write audit proposal.
- `git status --short --branch` — exit `0` — confirmed a clean `feat/eve-seo-agent` worktree immediately after verification.

## Phase 3 — Deterministic CI Gate Clarification (2026-07-30)

- State: COMPLETE (`MOCK_VERIFIED`); self-hosted runner infrastructure remains `BLOCKED_MISSING_CREDENTIALS`.
- Changed scope: root deterministic verification entry point, sidecar script composition, control-plane discovery, self-hosted-runner instructions, and Definition of Done wording only. The Windows root verifier invokes npm through the resolved Node CLI entry point rather than a shell. No public-site route, dependency, Vercel project, credential, connector, content, GitHub mutation, or deployment changed.
- Commands: `npm run seo-agent:verify` with Node `25.9.0` first on `PATH` — exit `0` — `15.0s` — control-plane discovery, lint, foundation formatting, typecheck, source/deployment boundaries, 40 deterministic tests, audit-only fixture, human-only rollback drill, and Eve build passed. The emitted fixture was `MOCK_VERIFIED`, audit-only, and zero-write.
- Commands: `npm run seo-agent:verify` with Node `25.9.0` first on `PATH` after the launcher hardening — exit `0` — `15.0s` — the same complete offline suite passed without the phase-introduced shell-launch deprecation warning. `git diff --check` and Prettier on both root verifier scripts — exit `0` — `4.6s`.
- Commands: `npm run verify:all` with Node `25.9.0` first on `PATH` at checkpoint `62516b9` — exit `0` — `16.8s` — clean-tree control-plane discovery, sidecar lint/format/typecheck, 40 deterministic tests, audit-only fixture, rollback drill, Eve build/manifest validation, and the separately labelled production dependency advisory passed. The emitted audit proposal was zero-write and `MOCK_VERIFIED` for tree `f018cd9f9f53424ac811e18ee9902fa4506518f7`.
- Added `seo/manifests/route-map.json`: it records all repository route patterns, their source paths and action dependencies, and distinguishes directly declared static routes from dynamic runtime-data routes. The three dynamic patterns have no fabricated concrete URL and are explicitly `BLOCKED_MISSING_CREDENTIALS` until their runtime source is approved and available.
- Commands: `npm run seo-agent:inventory`; `npm --prefix automation/seo-agent run test`; `npm --prefix automation/seo-agent run verify`; `git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `4.9s` combined — generated the route map and passed 41 deterministic tests plus source/deployment/architecture/agent-boundary checks. This is `MOCK_VERIFIED`; no adapter or public-site request occurred.
- Commands: `npm run verify:all` with Node `25.9.0` first on `PATH` at checkpoint `deb8cd4` — exit `0` — `17.0s` — clean-tree control-plane discovery, sidecar lint/format/typecheck, 41 deterministic tests, audit-only fixture, rollback drill, Eve build/manifest validation, and the separately labelled production dependency advisory passed. The emitted audit proposal was zero-write and `MOCK_VERIFIED` for tree `75208a0b4c5e5271c9636fdb5435a7ff55917479`.
- Evidence: `scripts/verify-seo-agent.mjs`, root `package.json`, `automation/seo-agent/package.json`, `automation/seo-agent/ci/SELF_HOSTED_RUNNER.md`, and `docs/seo-agent/DEFINITION_OF_DONE.md`.
- Verification classification: `MOCK_VERIFIED` for the deterministic implementation gate; `BLOCKED_MISSING_CREDENTIALS` for self-hosted runner provisioning and all live integrations.
- Failures / limitations: production dependency advisories are not included in the offline gate because `npm audit` needs a reviewed registry path; the canonical root verifier runs it separately. No live Vercel, Connect, GitHub, Google, browser, Sandbox, Cron, preview, or production operation was performed.
- Next exact action: run the separately labelled dependency advisory, then checkpoint the offline gate; the next product-runtime action remains human creation of the isolated Vercel sidecar project and scoped read-only connectors.

## Phase 1 Evidence (2026-07-29)

- `npm run seo-agent:control-plane` — exit `0` — `3.475s` — verified all 11 required files and confirmed the completion-hook sentinel is absent.
- `.\\.agents\\hooks\\eve-seo-agent-stop.ps1` — exit `0` — `0.049s` — reported that the completion gate is disabled because the sentinel is absent.
- `git diff --check` — exit `0` — `0.095s`.
- `npm run build` — exit `1` — `0.552s` — pre-existing environment limitation: dependencies are not installed in this clean checkout and `next` is unavailable on `PATH`. This is not an Eve runtime failure and no test/build configuration was changed to alter the result.
- No external integration is claimed live. All unavailable integrations are `BLOCKED_MISSING_CREDENTIALS`.

## Phase 2–3 Evidence (2026-07-29)

- `npm view eve ai @ai-sdk/gateway workflow @vercel/sandbox @vercel/connect @github-tools/sdk` — package API/version discovery completed before implementation: Eve `0.27.13`, AI `7.0.42`, AI Gateway `4.0.32`, Workflow `4.7.0`, Sandbox `2.9.0`, Connect `0.5.0`, GitHub Tools `1.8.2`.
- `npx --yes eve@0.27.13 --help` — exit `1` — `36.9s` — blocked by Node `22.21.1`; Eve requires Node `>=24`.
- `npm install --package-lock-only --ignore-scripts` in `automation/seo-agent` — exit `0` — `35.9s` — generated an isolated lockfile; engine warnings correctly report Node 24 is required.
- `npm --prefix automation/seo-agent run verify:all` — exit `0` — `3.8s` — 11 deterministic policy, contract, strategy, preview-gate, publishing, and audit tests passed; fixture audit produced `audit_only=true`, `draft_prs_created=0`, and `content_files_changed=0`.
- `npm audit --omit=dev --omit=peer --audit-level=high` — exit `0` — the production dependency set has zero high/critical findings. Default npm peer resolution remains documented because it adds an unused optional Workflow bundle with 12 high findings; CI forbids that install mode.
- `npm run seo-agent:control-plane` — exit `0` — confirms instructions and disabled Stop hook remain valid.
- `npm run verify:all` — exit `0` — `6.7s` — canonical root verification passed on clean commit `e3be499`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `945be412dd9e0d81f73eb191d365374d4ebf0574`, and retained zero content writes/PRs.
- `npm --prefix automation/seo-agent run ci:offline` — intentionally not run here because its Node 24 preflight would fail on the available Node 22 runtime; runner contract is versioned at `automation/seo-agent/ci/SELF_HOSTED_RUNNER.md`.
- `npm run seo-agent:inventory` then `npm --prefix automation/seo-agent run verify:all` — exit `0` — generated/validated 23 routes, 7 internal links, technical sitemap/robots/JSON-LD coverage, redirect manifest, Markdown change-manifest schema, and 12 deterministic tests. The fixture audit remains read-only and reports the two pre-existing technical configuration findings.
- Credential-dependent GitHub, Vercel, GA4, Local Falcon, Similarweb, Business Profile, and tracing adapters have explicit fixture-tested `BLOCKED_MISSING_CREDENTIALS` fallback records; no absent integration silently degrades or is reported live.
- Audit-only manifests now enforce the documented state machine, content/PR-write invariants, tree/source-code correlation, redaction, cost/token/tool metrics, and secret-safe observability fields through deterministic negative tests.
- Live Vercel, AI Gateway, Workflow execution, Sandbox, Connect, GitHub, Search Console, PageSpeed, browser, MCP, preview, production, GA4, Local Falcon, Similarweb, Business Profile, and tracing states: `BLOCKED_MISSING_CREDENTIALS` or `BLOCKED_MISSING_RUNTIME` as applicable; no live claim is made.

## Phase 2 Runtime-Build Evidence (2026-07-29)

- `C:\Program Files\nodejs\node.exe node_modules/eve/bin/eve.js --help` — exit `0` — `2.7s` — verified Eve `0.27.13` CLI with Node `25.9.0`; invoking the npm shim without preferring this runtime continues to select Node `22.21.1` and fail its engine check.
- `npm ci --ignore-scripts --omit=peer` with Node `25.9.0` in `automation/seo-agent` — exit `0` — `19.2s` — installed 117 packages and found zero vulnerabilities under the documented production dependency policy.
- `npm run ci:offline` with Node `25.9.0` first on `PATH` — exit `0` — `12.1s` — 14 policy/contract tests passed, audit fixture was read-only, production dependency audit found zero vulnerabilities, and `eve build` completed.
- `eve info` with Node `25.9.0` — exit `0` — `3.1s` — discovered 9 subagents, 2 authored bounded tools, 1 native schedule, the read-only Vercel MCP connection, and disabled generic root tools. The local workspace retained an empty untracked `agent/workflows/` directory after moving the tracked helper; a fresh checkout will not contain it. Its discovery warning did not affect the zero-error build.
- Verification classification remains `MOCK_VERIFIED`. No deployment, connector, scheduled execution, credentialed model call, browser query, GitHub mutation, preview audit, or production audit was performed.

## Phase 4 Boundary Evidence (2026-07-29)

- `npm --prefix automation/seo-agent run ci:offline` with Node `25.9.0` first on `PATH` — exit `0` — `13.9s` — contracts, architecture scan, least-privilege boundary scan, 14 deterministic tests, fixture audit, production dependency audit, Eve build, and compiled-manifest gate all passed.
- `npm --prefix automation/seo-agent run eve:build` with Node `25.9.0` first on `PATH` — exit `0` — `6.4s` — rebuilt the sidecar and revalidated the deployable compiled manifest with zero Eve diagnostics.
- The fixture audit remains `MOCK_VERIFIED`, audit-only, and emits no content changes or pull requests. Credentialed runtime, Vercel project, Connect, AI Gateway, Sandbox, scheduled delivery, browser, preview, and production remain `BLOCKED_MISSING_CREDENTIALS` or `BLOCKED_MISSING_RUNTIME` as applicable.

## Phase 5 Read-Only Intelligence Evidence (2026-07-29)

- `npm --prefix automation/seo-agent run test` with Node `25.9.0` first on `PATH` — exit `0` — `2.0s` — 20 deterministic tests passed, including typed configuration, GitHub/Vercel GET-only request shapes, Search Console query redaction, PageSpeed API-key redaction, offline probe behavior, and a configured audit with zero writes.
- `npm --prefix automation/seo-agent run live:probe` — exit `0` — offline plan only — emitted `BLOCKED_MISSING_CREDENTIALS` records for all ten integration categories without a network request or credential output.
- `npm --prefix automation/seo-agent run ci:offline` with Node `25.9.0` first on `PATH` — exit `0` — `11.1s` — all offline contracts, 20 tests, fixture audit, dependency audit, Eve build, and compiled-manifest verification passed.
- `npm run verify:all` on clean commit `73bbcf3` — exit `0` — `22.5s` — root control-plane verification and sidecar contracts/tests/audit/dependency audit passed; the emitted audit-only evidence is `MOCK_VERIFIED` for Git tree `06daa016a81fba90617f87e8d177ddc08f015014`.
- A non-secret local readiness check — exit `0` — found no matching scoped credential variable names, no `automation/seo-agent/.vercel/project.json`, no repository Vercel link, and no sidecar `.env*` file. This confirms the current `BLOCKED_MISSING_CREDENTIALS` classification without inspecting or printing any secret value.
- `node --test tests/*.test.mjs` plus `node scripts/verify.mjs` in `automation/seo-agent` with Node `25.9.0` — exit `0` — `0.9s` — 21 tests and the integration-inventory contract passed.
- `npm run verify:all` on clean commit `49f9274` — exit `0` — `5.9s` — root control-plane verification and all sidecar checks passed, producing `MOCK_VERIFIED` audit-only evidence for final verified tree `6a1a1a19e4d6707e77b7ab171965c0da3b2adbdc`.

## Phase 6 Audit Preparation Evidence (2026-07-30)

- Public County source retrieval recorded in `seo/evidence/2026-07-30-santa-cruz-county-public-demand.json` — `LIVE_VERIFIED` — documented plumbing-permit context, onsite-wastewater infrastructure context, and sewer-maintenance context. The packet preserves source URLs, retrieval time, minimal summaries, inference limits, and a payload hash.
- `seo/manifests/opportunity-backlog.json` — `LIVE_VERIFIED` public-source inputs — contains two research-only opportunities. Both require repository-owner review, have no query owner, prohibit a new URL, and are blocked from publication until Search Console, approved browser/SERP evidence, repository facts, and human review are complete.
- `node --test tests/*.test.mjs` plus `node scripts/verify.mjs` in `automation/seo-agent` with Node `25.9.0` — exit `0` — `1.0s` — 22 tests passed; evidence payload-hash verification and publication-blocked opportunity constraints are enforced.
- No sidecar deployment, credentialed integration, Sandbox, browser/Google API adapter call, draft PR, preview, merge, or production audit has occurred. The first actual production audit remains `BLOCKED_MISSING_CREDENTIALS` pending human authorization and scoped setup.
- These are `MOCK_VERIFIED` implementation contracts only. No actual Vercel, GitHub, Search Console, PageSpeed, browser, or optional-integration request was made.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run audit:fixture; npm --prefix automation/seo-agent run verify` with Node `25.9.0` first on `PATH` — exit `0` — `2.5s` combined — 23 deterministic tests passed. The audit fixture rendered four canonical Git artifact paths (one run manifest and three evidence records), with `write_performed=false`, `requires_human_approval=true`, zero content writes, and zero PRs. Path traversal and non-canonical artifact paths are rejected by contract tests.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `7.5s` — root clean-tree verification passed on code checkpoint `1878e33`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `9f42936a7b8d0e9c90bc6a9f9e752c26237bd259`, required the completion-hook sentinel to remain absent, and confirmed zero persistent local artifact writes.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run verify; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.2s` combined — 24 deterministic tests passed, including the separate browser-domain approval deny fixture. This is `MOCK_VERIFIED`; no browser request or credentialed adapter call occurred.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run audit:fixture; npm --prefix automation/seo-agent run verify; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `3.0s` combined — 25 deterministic tests passed. The fixture audit remains timestamp-stable and audit-only; configured manifests use a real collection time. This is `MOCK_VERIFIED`; no external request occurred.
- `npm --prefix automation/seo-agent run verify; npm --prefix automation/seo-agent run eve:build; npm --prefix automation/seo-agent run test; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `6.7s` combined — the sidecar source/compiled-manifest boundary checks and 25 deterministic tests passed. The compiled Eve schedule is the audit-only markdown form (durable through Eve/Workflows); no schedule was deployed or run. This is `MOCK_VERIFIED`.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run eve:build; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `6.7s` combined — 25 deterministic tests passed, including the over-budget model-request denial; the compiled model tool includes the preflight. This is `MOCK_VERIFIED`; AI Gateway was not called.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `6.6s` — root clean-tree verification passed on code checkpoint `39b1173`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `f8a9cf550739bae2025fc891961733269802b994`, and confirmed no high/critical production dependency findings. The completion-hook sentinel remains absent.
- `bun install --frozen-lockfile` — stopped after approximately `88s` without progress output — `BLOCKED_MISSING_RUNTIME` for public-site build validation. The exact Bun child process was confirmed as this command and stopped; no `next` executable was installed. This does not affect the independently installed sidecar or its offline verification.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run verify; npm --prefix automation/seo-agent run eve:build; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `8.4s` combined — 26 deterministic tests passed, including transient-only retry and duplicate-run denial fixtures; sidecar boundary and compiled-manifest checks passed. This is `MOCK_VERIFIED`; no network request occurred.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run audit:fixture; npm --prefix automation/seo-agent run verify; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.7s` combined — 26 deterministic tests passed. Audit manifests now include semantic version plus prompt/skill hashes, and fixture execution remained zero-write and audit-only. This is `MOCK_VERIFIED`; no network request occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `6.8s` — root clean-tree verification passed on code checkpoint `672733b`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `c26a6227096d4f66e158987184bd48c27bfc58e5`, confirmed no high/critical production dependency findings, and kept the completion-hook sentinel absent. The trailing PowerShell tree-display command had a quoting error after verification completed; the tree was independently re-read successfully.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run rollback:drill; npm --prefix automation/seo-agent run verify; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.4s` combined — 30 deterministic tests passed. Browser-injection input produced only a redacted `SECURITY_ESCALATED` fixture event, every prohibited action was denied, and the rollback drill proved `automated_action_attempted=false` with `human_approval_required=true`. This is `MOCK_VERIFIED`; no network request, provider call, or repository write occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `6.8s` — root clean-tree verification passed on code checkpoint `58af149`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `b9da9230d7ffef4193a972bc71fb6b7cd5135f35`, confirmed all 30 deterministic tests plus the human-only rollback drill, found zero high/critical production dependency findings, and kept the completion-hook sentinel absent. No network request, provider call, deployment, or repository write occurred.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run verify; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.0s` combined — 31 deterministic tests passed. The versioned adversarial-policy fixture rejected prompt injection, deceptive content, credential exfiltration, policy bypass, and unsafe septic/chemical advice; duplicate new-URL proposals and evidence-free proposals failed closed. This is `MOCK_VERIFIED`; no external request occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `7.1s` — root clean-tree verification passed on code checkpoint `44bd371`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `1e76da0612263f6d872ac380f0d805473e3445b0`, confirmed all 31 deterministic tests, the human-only rollback drill, and zero high/critical production dependency findings. The completion-hook sentinel remains absent; no network request, provider call, deployment, or repository write occurred.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run verify; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.1s` combined — 32 deterministic tests passed. The draft-publisher fixture accepted only a complete packet on `feat/proposal` with human approval and rejected ref-qualified/default/non-feature branches, incomplete packets, non-draft responses, and malformed PR metadata. This is `MOCK_VERIFIED`; no GitHub request or repository write occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `7.9s` — root clean-tree verification passed on code checkpoint `67ab3bd`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `a4c31501ef2b3024f4b690d9348c80ac7505a131`, confirmed all 32 deterministic tests, the human-only rollback drill, and zero high/critical production dependency findings. The completion-hook sentinel remains absent; no network request, provider call, deployment, GitHub request, or repository write occurred.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run verify; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.0s` combined — 33 deterministic tests passed. Preview and production audit fixtures rejected non-HTTPS/missing/noindex-unsafe previews, credential-blocked readiness, write attempts, and deployment SHAs that do not match a human merge. This is `MOCK_VERIFIED`; no Vercel, GitHub, or browser request occurred.
- `npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run verify; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.2s` combined — 34 deterministic tests passed. Shared-budget fixtures denied the second request before dispatch, denied a zero-budget browser page before dispatch, and denied a zero-budget schedule run; configured audit manifests recorded their consumed run slot plus immutable budget limits. This is `MOCK_VERIFIED`; no external request occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `7.1s` — root clean-tree verification passed on code checkpoint `619ac50`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `8b148558b0721194ec006a5504cf846a19038a1b`, confirmed all 34 deterministic tests, the human-only rollback drill, and zero high/critical production dependency findings. The completion-hook sentinel remains absent; no network request, provider call, deployment, GitHub request, or repository write occurred.
- `npm --prefix automation/seo-agent run verify; npm --prefix automation/seo-agent run test; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `2.1s` combined — 34 deterministic tests passed. Source boundaries proved all nine specialists and the root resolve the same approved Gateway model route; policy fixtures rejected an unapproved route and an over-cap cost reservation before dispatch. This is `MOCK_VERIFIED`; no AI Gateway request occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `6.8s` — root clean-tree verification passed on code checkpoint `adcd4f7`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `0872778a426ae89036326bac4c8f84b87701d12d`, confirmed all 34 deterministic tests, the human-only rollback drill, and zero high/critical production dependency findings. The completion-hook sentinel remains absent; no network request, provider call, deployment, GitHub request, or repository write occurred.
- `npm --prefix automation/seo-agent run ci:offline` with Node `25.9.0` first on `PATH` — exit `0` — `15.2s` — 36 deterministic tests passed, including an atomic model-ledger fixture that rejects a second over-budget request without mutating prior usage. Sidecar contracts, dependency audit, Eve build, and compiled-manifest checks passed. This is `MOCK_VERIFIED`; no AI Gateway request occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `7.5s` — root clean-tree verification passed on code checkpoint `9df39e3`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `a2e8579306162c45d3ed725245fecc63895aa6b5`, confirmed all 36 deterministic tests, the human-only rollback drill, and zero high/critical production dependency findings. The completion-hook sentinel remains absent; no network request, provider call, deployment, GitHub request, or repository write occurred.
- `npm --prefix automation/seo-agent run ci:offline` with Node `25.9.0` first on `PATH` — exit `0` — `13.2s` — 38 deterministic tests passed, including canonical Git-backed run-history validation and duplicate scheduled-run denial before collection. Sidecar contracts, dependency audit, Eve build, and compiled-manifest checks passed. This is `MOCK_VERIFIED`; no external request occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `9.1s` — root clean-tree verification passed on code checkpoint `38fa370`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `f83c0292c9c16dacd80001015c6bba2df8e20a36`, confirmed all 38 deterministic tests, the human-only rollback drill, and zero high/critical production dependency findings. The completion-hook sentinel remains absent; no network request, provider call, deployment, GitHub request, or repository write occurred.
- `npm --prefix automation/seo-agent run ci:offline` with Node `25.9.0` first on `PATH` — exit `0` — `11.9s` — 39 deterministic tests passed, including shared live-read authorization for every caller: production environment, human approval, and an exact approved run ID are all mandatory before adapter dispatch. Sidecar contracts, dependency audit, Eve build, and compiled-manifest checks passed. This is `MOCK_VERIFIED`; no external request occurred.
- `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `7.7s` — root clean-tree verification passed on code checkpoint `71e8343`, emitted `MOCK_VERIFIED` audit-only evidence for Git tree `93e1deb82a8f6fa8e4ec15ae9cbe4788c40ec281`, confirmed all 39 deterministic tests, the human-only rollback drill, and zero high/critical production dependency findings. The completion-hook sentinel remains absent; no network request, provider call, deployment, GitHub request, or repository write occurred.
- Non-secret local readiness check plus `npm --prefix automation/seo-agent run live:probe` — exit `0` — no `automation/seo-agent/.vercel/project.json`, sidecar `.env*` file, scoped process credential name, or approved run ID was present. The offline plan emitted `BLOCKED_MISSING_CREDENTIALS` evidence for all ten adapter categories without network access. The next exact action remains human creation/linking of the separate sidecar project and least-privilege read-only connector setup from `MANUAL_SETUP.md`.

## Phase 10 — Independent Offline Readiness Review (2026-07-30)

- State: COMPLETE for the independent offline decision (`MOCK_VERIFIED`); the project as a whole is `NOT_READY` for final acceptance.
- Branch: `feat/eve-seo-agent`.
- Changed scope: added a deterministic independent review module, CLI, and fixture test; made it a required sidecar offline verification step. It reads only repository technical-SEO/integration inventory and root Next configuration. It makes no network, credential, repository-write, deployment, or public-site application change.
- Commands: `npm --prefix automation/seo-agent run format; npm --prefix automation/seo-agent run test; npm --prefix automation/seo-agent run review:independent; npm --prefix automation/seo-agent run ci:offline; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `18.1s` combined — formatting completed, 44 deterministic tests passed, lint/format/typecheck/build and compiled-Eve-manifest checks passed, and the review emitted `MOCK_VERIFIED`, `completion_ready: false`, and `conclusion: "NOT_READY"`.
- Commands: `npx --no-install tsc --noEmit` at the repository root with Node `25.9.0` — exit `1` — `3.9s` — the root install does not provide the TypeScript compiler; this is a root development-dependency/runtime limitation, not a sidecar failure. `npm run lint` at the repository root with Node `25.9.0` — exit `1` — `1.7s` — Next requested initial ESLint setup and created no configuration or other file change.
- Evidence: `automation/seo-agent/src/independent-review.mjs`, `automation/seo-agent/scripts/independent-review.mjs`, `automation/seo-agent/tests/independent-review.test.mjs`, `seo/manifests/technical-seo.json`, `seo/manifests/integration-inventory.json`, and root `next.config.js`.
- Verification classification: `MOCK_VERIFIED` for the reviewer and its fixture. The produced decision truthfully lists completion blockers `TECHNICAL_TYPESCRIPT_BUILD_ERRORS_IGNORED`, `TECHNICAL_ESLINT_BUILD_ERRORS_IGNORED`, `PUBLIC_TYPESCRIPT_BUILD_SUPPRESSED`, and `PUBLIC_ESLINT_BUILD_SUPPRESSED`; it lists required live integrations as external prerequisites rather than claiming they work.
- Failures / limitations: root public-site type/lint build-error suppressions remain unresolved high-risk findings. The locally installed root toolchain cannot run raw TypeScript checking, root lint has not been bootstrapped, and the known public build remains blocked by missing local Supabase configuration. Vercel Eve/AI Gateway/Sandbox, scoped GitHub/Vercel paths, Search Console, PageSpeed, and browser research remain `BLOCKED_MISSING_CREDENTIALS`; optional live adapters remain unverified. No automatic completion or production-readiness claim is valid.
- Next exact action: obtain explicit owner direction for public-site quality-gate remediation (configure root lint/typecheck and then remove build-error suppression only with passing evidence), create/link the separate sidecar Vercel project, and configure the least-privilege live integrations described in `MANUAL_SETUP.md` before the authorized audit-only production run.
- Post-checkpoint command: `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `17.4s` — root control-plane discovery passed with the completion-hook sentinel absent; sidecar lint, formatting, typecheck, 44 tests, fixture audit, human-only rollback drill, independent readiness decision, Eve build/manifest verification, and production dependency audit all passed. The verified checkpoint was `4261fe5`; it emitted the expected `MOCK_VERIFIED` offline result and `NOT_READY` readiness conclusion without a network request, provider call, deployment, GitHub mutation, or persistent artifact write.

## Phase 10 — Public-Site Quality-Gate Remediation (2026-07-29)

- State: COMPLETE for the offline public-site quality gate (`MOCK_VERIFIED`). Final system acceptance remains blocked by the separately recorded live-integration prerequisites.
- Branch: `feat/eve-seo-agent`.
- Changed scope: added the root Next ESLint configuration and portable TypeScript command; corrected current Next route prop types and type-only imports; supplied required JSON-LD script IDs; removed the two root build-error suppressions; and made local, missing-Supabase builds return empty read results rather than crash during static metadata/sitemap generation. No sidecar dependency, public client bundle dependency, production credential, deployment, GitHub mutation, or automatic publishing behavior changed.
- Commands: `npm run lint; npm run typecheck; npm run build; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `46.7s` combined — root lint reported no warnings/errors, TypeScript passed, and Next compiled, typechecked, and generated all 28 static pages. The normal build output still reports the existing Browserslist data-age warning and two edge-runtime/static-generation cautions; neither is a hidden lint or type failure.
- Commands: `npm run seo-agent:inventory; npm --prefix automation/seo-agent run review:independent` with Node `25.9.0` first on `PATH` — exit `0` — `2.4s` combined — regenerated the repository inventory with 20 technical findings and no high/critical finding. The independent review returned `completion_blockers: []`, `completion_ready: false`, and `NOT_READY` solely because seven required live integrations are not `LIVE_VERIFIED`.
- Evidence: `.eslintrc.json`, `package.json`, `next.config.js`, `tsconfig.json`, `lib/supabase.js`, current route/type fixes, and `seo/manifests/technical-seo.json`.
- Verification classification: `MOCK_VERIFIED` for local quality gates and local missing-configuration fallback. It is not proof that a configured Supabase project, a Vercel production build, live browser behavior, or any required sidecar integration works.
- Failures / limitations: this is a local, credential-free public-site build. The real Supabase data path remains unverified; Eve/AI Gateway/Sandbox, GitHub/Vercel reads, Search Console, PageSpeed, and browser research remain `BLOCKED_MISSING_CREDENTIALS`. No production run, preview, draft PR, merge, or deployment occurred.
- Phase-caused correction: the first combined validation found one stale technical-audit assertion that expected the two now-removed build-suppression findings. The test was strengthened, not disabled: the real repository must have no high/critical finding, while an isolated temporary configuration with both suppressions must still produce both high-risk findings.
- Commands: `npm --prefix automation/seo-agent run format; npm --prefix automation/seo-agent run test; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `3.4s` combined — 44 deterministic tests passed, including the real-root/isolated-suppression audit fixture; no temporary fixture data persisted.
- Next exact action: commit this quality-gate checkpoint, run the canonical clean-tree verifier, then proceed only through human-authorized least-privilege setup for the remaining live integrations and first audit-only production run.
- Post-checkpoint command: `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `61.7s` — root lint, typecheck, and 28-page production build passed before control-plane discovery, the 44 sidecar tests, fixture audit, rollback drill, independent readiness review, Eve build/manifest gate, and production dependency audit. The verified commit was `307b24f` and its tree hash was `d3e6c0264a74e152fbc24744794b4055b9ef607d`; the final review retained `completion_blockers: []` and `NOT_READY` only for its seven live-integration prerequisites. No network request, provider call, GitHub mutation, preview, production deployment, or persistent audit artifact write occurred.

## Phase 3 — Publishable-Metadata Integrity (2026-07-29)

- State: COMPLETE for deterministic placeholder integrity (`MOCK_VERIFIED`).
- Branch: `feat/eve-seo-agent`.
- Changed scope: removed literal Google-verification and address/coordinate/area-served placeholder values from public metadata/JSON-LD, replaced the static job-posting date with the job record value, and added a technical-inventory rule plus isolated regression fixture for publishable placeholders. Existing testimonials and live job records were not reclassified or changed because local repository text alone cannot prove whether they are fabricated.
- Commands: `npm --prefix automation/seo-agent run format; npm run lint; npm run typecheck; npm run build; npm --prefix automation/seo-agent run ci:offline; npm run seo-agent:inventory; npm --prefix automation/seo-agent run review:independent; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `61.9s` combined — root lint/typecheck/28-page build passed; sidecar lint/format/typecheck, 44 tests, audit fixture, rollback drill, Eve build, and manifest verification passed; inventory returned 20 technical findings with no high/critical finding; the independent review retained zero completion blockers.
- Evidence: `automation/seo-agent/src/inventory.mjs`, `automation/seo-agent/tests/contracts.test.mjs`, `seo/manifests/technical-seo.json`, and the remediated public metadata/JSON-LD sources.
- Verification classification: `MOCK_VERIFIED`. The regression test creates and deletes an isolated temporary fixture, with no repository artifact, network call, credential, content publication, PR, merge, or deployment.
- Failures / limitations: existing customer-review text and dynamic job data remain unverified local data, not approved agent facts. Required live integration prerequisites remain `BLOCKED_MISSING_CREDENTIALS`; no claim has been made that they work.
- Next exact action: commit this integrity checkpoint, run the canonical clean-tree verifier, then continue only with credential-free policy work or explicit human-authorized connector setup and the first audit-only production run.
- Post-checkpoint command: `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `61.6s` — root lint/typecheck/28-page build, control-plane discovery, sidecar lint/format/typecheck, 44 tests, fixture audit, human-only rollback drill, independent review, Eve build/manifest verification, and production dependency audit passed. The verified commit was `e4f9b56` and its tree hash was `89b3bfc70c7f1beaf3e511c95012af6d6729b780`; it retained no high/critical technical finding and no independent completion blocker. The reviewer remains `NOT_READY` solely for the seven required integrations that are not `LIVE_VERIFIED`.

## Phase 4 — Eve Agent Composition, Skills, Policies, and Evals (2026-07-30)

- State: COMPLETE for the offline composition layer (`MOCK_VERIFIED`); live operation remains `BLOCKED_MISSING_CREDENTIALS`.
- Branch: `feat/eve-seo-agent`.
- Changed scope: added 14 packaged, discoverable root Eve skills; hardened root and specialist instructions; added central role-model profiles with independent writer and QA primary routes plus approved Gateway fallbacks; added a deterministic content-risk gate; added a versioned 10-case content-policy fixture and 10 native Eve eval cases. No public-site dependency, client bundle, secret, connector scope, runtime mutation, or publishing path changed.
- Safety behavior: unsupported or unsafe commercial/local/safety claims return `REQUEST_FIRST_PARTY_EVIDENCE`; injection, fake jobs/reviews, unsafe advice, doorway/stuffing, and hidden/mass publishing return `NO_ACTION`/refusal. Existing-page-first, no-change, and bounded-patch decisions remain mandatory.
- Commands: `npm --prefix automation/seo-agent run format; npm --prefix automation/seo-agent run typecheck; npm --prefix automation/seo-agent run test:policy; npm --prefix automation/seo-agent run evals; npm --prefix automation/seo-agent run verify; npm --prefix automation/seo-agent run test` with Node `25.9.0` first on `PATH` — exit `0` — `40.4s` aggregate — 14 skills discovered, 66 deterministic tests passed, and 15 Eve evals/50 gates passed. The first focused run found and corrected an omitted dollar-price pattern and an Eve compiled-eval fixture-path limitation; the fixture remains versioned and the compiled eval mirrors it.
- Commands: `npm --prefix automation/seo-agent run ci:offline; npm run lint; npm run typecheck; npm run build; git diff --check` with Node `25.9.0` first on `PATH` — exit `0` — `110s` — sidecar lint/format/typecheck, all unit/integration tests, audit fixture, rollback drill, independent review, Eve build/manifest verification, and 15 native evals passed; root lint/typecheck and the 28-page production build passed.
- Evidence: `automation/seo-agent/agent/skills/`, `automation/seo-agent/src/policy.mjs`, `automation/seo-agent/agent/model.ts`, `automation/seo-agent/evals/content-policy.eval.ts`, `automation/seo-agent/fixtures/content-policy-evals.json`, and `automation/seo-agent/scripts/verify-agent-boundaries.mjs`.
- Verification classification: `MOCK_VERIFIED`. Eve evals use `SEO_AGENT_EVAL_FIXTURE=true`; no AI Gateway/provider request, web request, Vercel deployment, GitHub mutation, PR, merge, or production change occurred.
- Failures / limitations: all required live integrations remain `BLOCKED_MISSING_CREDENTIALS`; the independent readiness review remains `NOT_READY` for those seven prerequisites. The root build retains the pre-existing Browserslist data-age and edge-runtime/static-generation warnings; they did not fail lint, typecheck, or build. An Eve local-world queue emitted a transient post-eval socket-hang-up retry log after tests completed successfully; it did not affect the 15/15 eval result and did not invoke an external provider.
- Next exact action: commit this composition checkpoint, run the canonical clean-tree verifier, then continue only with human-authorized least-privilege live setup and the first audit-only production run.
- Post-checkpoint command: `npm run verify:all` with Node `25.9.0` first on `PATH` — exit `0` — `103.6s` — root lint/typecheck/28-page production build, control-plane discovery, sidecar lint/format/typecheck, 66 tests, 15 native Eve evals/50 gates, fixture audit, human-only rollback drill, independent readiness review, Eve build/manifest verification, and dependency advisory passed. The verified commit was `280703b` and its tree hash was `4d6f8645728f3e36bb50cd6d7d73222a5a4221af`; completion remained `NOT_READY` only for the seven external live-integration prerequisites. No provider request, deployment, GitHub mutation, PR, merge, or production change occurred.
