# Eve SEO Agent Execution Plan

## Operating Rule

Complete phases in order. A phase is not complete until its acceptance checks have recorded commands, evidence, timestamps, classifications, and limitations in `IMPLEMENTATION_STATUS.md`. A blocked external integration does not justify a substitute claim; record `BLOCKED_MISSING_CREDENTIALS` and continue only with the phase's fixture/offline path.

| Phase                               | Deliverable                                                                                                                          | Dependencies                                             | Acceptance checks                                                                                                          |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 0. Reconcile baseline               | Confirm repository ref, public-site source of truth, current checks, and missing baseline discrepancy.                               | None                                                     | `git status --short --branch`; documented command/version/exit-code matrix; no runtime change.                             |
| 1. Control plane                    | This specification, instructions, plan, DoD, setup guide, skill, control checker, disabled Stop hook.                                | 0                                                        | `npm run seo-agent:control-plane`; docs and instructions discoverable; hook sentinel absent.                               |
| 2. Sidecar scaffold                 | Isolated `automation/seo-agent` Eve package, independent lockfile/build config, typed configuration loader.                          | 1; Vercel deployment decision                            | Sidecar installs/builds independently; no public frontend routes; no shared production secrets; architecture review.       |
| 3. Offline contracts                | Versioned schemas, fixtures, content model, page/query/link/fact/experiment manifests, secret scan, self-hosted CI gate.             | 2; dedicated runner provisioned or documented as blocked | Network-off deterministic contract suite; malformed fixtures rejected; runner provenance recorded; status `MOCK_VERIFIED`. |
| 4. Policy and tool guardrails       | Typed tools, Connect boundaries, approval policy, allowlists, sandbox constraints, injection defense.                                | 2-3; credentials may be deferred                         | Evals prove denial of direct-main writes, merge, deployment, secret access, unsafe claims, and injected instructions.      |
| 5. Read-only intelligence           | Search Console, PageSpeed, browser, GitHub, Vercel adapters; optional GA4/Local Falcon/Similarweb/Business Profile/tracing adapters. | 3-4; credentials per adapter                             | Fixtures always pass offline; live adapter probe is `LIVE_VERIFIED` or `BLOCKED_MISSING_CREDENTIALS`; no write capability. |
| 6. Audit-only production run        | Demand/intent research, source inventory, query ownership, link graph, technical audit, opportunity shortlist.                       | 5; human production authorization                        | Run creates evidence/manifests only; attempts zero PRs/content writes; human signs off on findings.                        |
| 7. Drafting pipeline                | Orchestrator and specialized agents; existing-page-first strategy; Markdown proposal/migration path; draft-PR-only publisher.        | 3-6; human approval                                      | Evals and fixture run create a draft PR packet only; claims, areas, links, canonical and QA fields complete.               |
| 8. Preview gate                     | Services preview with isolated site/Eve route prefixes, deterministic CI, preview crawl/performance/metadata/JSON-LD checks.         | 7; Vercel Services access                                | Failing preview blocks PR readiness; passing evidence references exact commit and URL.                                     |
| 9. Human-merge and production audit | Human-approved merge flow, automatic normal Vercel production deployment, read-only post-deploy audit/rollback runbook.              | 8; production authorization                              | No agent merge/deploy permission; verified production URL/deployment association and audit record.                         |
| 10. Final verification              | Re-run all DoD checks, review threat model/budgets/runbooks, enable Stop-hook sentinel briefly for final completion check.           | 1-9                                                      | Entire DoD green or documented blocked external checks; hook reports success without mutation; human accepts completion.   |
| 11. Operational resilience          | Safe redacted traces, metrics, alerts, recovery plans, threat model, and fault injection.                                            | 4-10                                                     | Fixture-driven observability/security gate passes; live telemetry remains explicitly classified.                           |
| 12. Verification evidence system    | Canonical root verification artifacts, completion checker, machine DoD mapping, and bounded enabled Stop hook.                       | 1-11                                                     | Fresh evidence passes all required local gates; only classified credential blockers may remain; hook cannot loop.          |

## Measurable Milestones

- Phase 1: exactly one offline control-plane command verifies required files and proves the sentinel is absent.
- Phase 3: schema and fixture suite has zero network calls and a reproducible exit code.
- Phase 4: every prohibited action has at least one negative eval.
- Phase 6: first real production run reports `audit_only: true`, `draft_prs_created: 0`, and `content_files_changed: 0`.
- Phase 7: each proposed URL has exactly one owner decision and an explicit existing-page assessment.
- Phase 8: every draft PR includes a preview URL or a `BLOCKED_MISSING_CREDENTIALS` evidence record; it cannot be marked ready without a passing preview audit.
- Phase 9: production evidence confirms a human merge and a Vercel-created production deployment; it never credits an agent-run deployment.

## Phase 22 Update: Services Preview Boundary (2026-08-01)

The one-project Services topology has now reached a `LIVE_VERIFIED` preview deployment: the public site and the isolated Eve service built separately and the service functions are present only below `/_internal/eve`. This completes the deployment-topology proof, not live agent operation. Direct endpoint acceptance remains `BLOCKED_PREVIEW_SSO`; provider reads, observe-only workflow invocation, and all mutation paths remain separately gated. The measurable next acceptance check is an owner-approved SSO-safe health/readiness and Cron-auth verification followed by one audit-only invocation that produces zero content files, zero PRs, and zero provider writes.

## Phase 31 Update: Guarded GitHub Draft Publisher (2026-08-01)

The Phase 7 draft publisher now has an explicit Vercel Connect compatible GitHub REST adapter. It is disabled by default and is not exposed to Eve model tools. Before it can make any request, it requires enabled publishing mode, a disabled kill switch, an explicit human approval record, and the separate integration-test execution flag. Its write allowlist is limited to one `eve/seo/YYYY-MM-DD-<slug>` feature ref, one Git tree and commit, a non-force feature-ref update, and a draft pull request targeting `main`. It has no merge, deletion, default-branch update, repository-settings, secret, or deployment operation.

The adapter and its mocked end to end flow are `MOCK_VERIFIED`. A live GitHub write remains `BLOCKED_MISSING_CREDENTIALS` until the Vercel Connect GitHub App is granted the reviewed minimum permissions and a human approves one disposable integration test. This phase does not enable mutation mode or change production configuration.

## Phase 32 Update: Production OIDC Runtime Verification (2026-08-01)

After the human merge of PR `#84`, Vercel deployed commit `dafe36c3012b91c82a747d9beff4d339c060c67d` as production deployment `dpl_jrQmpaSE9qc4Y89c4jSa8knyFdTv`. Read-only `GET` probes to the internal health and readiness routes returned HTTP `200`, `mode: observe`, `ready: true`, `integration_classification: CONFIGURED_UNVERIFIED`, and an enabled mutation kill switch. This is `LIVE_VERIFIED` only for the deployment and OIDC-aware health surface. It starts no audit, makes no model request, and enables no publishing path.

## Phase 33 Update: Terra Model Availability and Fallback (2026-08-01)

## Phase 34 Update: Cross-Platform Eve Build and Verification Repair (2026-08-01)

- Acceptance scope: make canonical Windows verification execute its actual npm gates, and make the current official Eve build/eval workflow portable to the owner workstation without changing any agent capability or privilege.
- Acceptance checks: `npm run test:verification-core`, sidecar format/lint/typecheck/test/evals/build, and `npm run verify:all` must pass. The verifier must record a fresh artifact with every required root and sidecar check, and the public site must remain dependency-isolated from Eve.
- Result: complete. The npm command shim is executed by the active Node npm CLI when possible, and its real execution has a regression test. Removing the Eve `@vercel/connect` externalization prevents its Windows bundler from writing a bare drive-letter ESM import; the typed client is now part of the sidecar bundle. `npm run verify:all` passed `17/17` checks in `156.5s` and emitted `artifacts/verification/2026-08-01T150847522z-14de4c87073d`. This is `MOCK_VERIFIED`, not live publishing proof.
- Dependencies / next gate: draft PR `#85` remains subject to existing self-hosted CI and preview review. No live mutation is authorized by this phase.

## Phase 35 Update: Completion-Evidence Integrity (2026-08-01)

- Acceptance scope: make `verify:all` enumerate all applicable deterministic resilience and independent-review checks, and make `verify:completion` fail closed when any Definition-of-Done or integration-classification evidence is missing or incomplete.
- Acceptance checks: a regression record with all classified ready evidence passes; records with an unclassified integration or non-ready independent review fail completion. Canonical evidence must include sidecar integration, dependency, Sandbox classification, observability, rollback, and independent-review output.
- Dependencies / next gate: no external state is changed. Current integration records are valid `BLOCKED_MISSING_CREDENTIALS` entries, so canonical deterministic verification may pass but final completion must remain rejected until the required human-approved live probes are recorded.

## Phase 36 Update: Production Read-Authorization Diagnostic (2026-08-01)

- Acceptance scope: inspect the deployed observe-only control state before attempting any live provider read and prove that a missing exact-run approval fails closed.
- Result: production health and readiness are `LIVE_VERIFIED` at the stated endpoints. The sidecar reports a closed circuit, required Cron secret, observe mode, and an active mutation kill switch. Live reads are not authorized because the exact Production controls remain `SEO_AGENT_LIVE_READS_APPROVED=false` and no approved run ID exists. This is correctly `BLOCKED_MISSING_CREDENTIALS`, not an integration failure.
- Dependencies / next gate: requires a human-selected one-time audit run ID and explicit approval. It does not authorize proposal mode, publishing, Blob archival, a Sandbox session, direct-main writes, force pushes, automatic merges, or deployment changes.

## Phase 37 Update: Production Configuration Reconciliation (2026-08-01)

- Acceptance scope: distinguish project-level connector and credential setup from the sidecar's explicit runtime authorization and adapter flags, without making a provider request or changing production configuration.
- Result: the authenticated Vercel CLI identifies the Production project as `byron-wade/wadesplumbingandseptic-com` on Node `24.x`. A fresh scoped environment pull confirms that OIDC, the Google service-account values, and the PageSpeed key are present without exposing values. It also confirms the safety boundary remains active: live reads are `false`, no exact approved run ID exists, and the Vercel, Search Console, PageSpeed, and Browserbase adapters remain disabled. AI Gateway and GitHub are configured but may not run while the exact live-read authorization is absent.
- Dependencies / next gate: the owner must intentionally enable only the desired read adapters, choose an exact lower-case audit run ID, and set the one-time live-read approval. Observe mode, disabled mutation, and the enabled kill switch remain required. The next command is the read-only production probe, not a publishing workflow.

## Phase 38 Update: Control-Plane Command Coverage (2026-08-01)

- Acceptance scope: align the root command contract with the published Definition of Done and ensure canonical verification cannot omit the control-plane discovery check.
- Result: `npm run seo-agent:control-plane` now executes the existing sidecar control-plane verifier. The root offline gate invokes it first, canonical `verify:all` records it as a dedicated check, and a self-hosted workflow regression test requires both command names. This is a deterministic contract repair and does not change Eve authority or provider configuration.
- Dependencies / next gate: fresh canonical evidence must include the new control-plane check. The human-approved, read-only production audit remains the next live phase.

## Phase 39 Update: Post-Merge Verification and Audit Gate (2026-08-01)

- Acceptance scope: confirm the merged remote state, the deployed backend-only service boundary, the canonical deterministic evidence, and the human-controlled production-read gate without enabling any adapter or mutation capability.
- Result: `origin/main` is commit `845c3b9246674b2a35bb44fb3d2ba763f9c81909`; no remote feature branches or open pull requests remain. Vercel reports the production alias deployment `dpl_9sw3zYnU7xDXtWYFGvhVCiDsGMzQ` as `Ready` and lists only internal Eve functions below the Services prefix. Public health and readiness probes returned HTTP `200`, `mode: observe`, `ready: true`, `configured_unverified`, and an enabled mutation kill switch. This is `LIVE_VERIFIED` only for deployment/readiness observation, not an audit or provider operation.
- Deterministic evidence: `npm run verify:all` passed `24/24` checks in `133.167s` at commit `845c3b9246674b2a35bb44fb3d2ba763f9c81909`, with artifact `artifacts/verification/2026-08-01T164512583z-845c3b924667`. `npm run verify:completion` correctly rejected completion because Definition-of-Done and independent-readiness live prerequisites remain incomplete.
- Dependencies / next gate: Production remains explicitly closed for live reads: `SEO_AGENT_LIVE_READS_APPROVED=false`, no matching approved run ID, and the Vercel, Search Console, PageSpeed, and browser adapters are disabled. The owner must choose the exact lower-case audit run ID, enable only the intended read adapters, set the matching one-time authorization, and retain observe mode with mutation disabled and the kill switch enabled. Only then may the read-only production audit proceed.

## Phase 40 Update: Authorized Production Audit Attempt (2026-08-01)

- Acceptance scope: use the owner-approved one-time read authorization without exposing sensitive values, starting a durable observe-only audit only if the platform can safely invoke it.
- Result: the exact authorization and intended read flags were set temporarily, but both `vercel env pull` and `vercel env run` supplied Vercel's non-readable Sensitive-value placeholders rather than usable runtime credentials. The probe failed during local schema validation before any adapter dispatched. The temporary flags were restored to false and the matching run ID was removed. The ignored local env file was deleted. Live health/readiness remained `200` in observe mode with the kill switch enabled, and `vercel agent-runs list` confirmed zero runs, tokens, and cost.
- Dependencies / next gate: Vercel only applies environment changes to new deployments, and the existing protected Cron requires the deliberately non-readable `CRON_SECRET`. A genuine live audit therefore needs either an approved deployment carrying the temporary flags and its normal scheduled Cron invocation, or an approved non-public least-privilege manual trigger. Do not expose a secret, loosen Cron authentication, add a public agent route, enable mutation/publishing, or treat this failed-local execution path as provider verification.

## Phase 41 Update: Live Audit Runtime Activation (2026-08-01)

- Acceptance scope: replace the audit-only runtime placeholder with a bounded configured audit that can collect real read-only adapter evidence only after the exact Production approval gates pass.
- Result: the durable Eve task now calls `createConfiguredAuditOnlyRun` with the runtime-generated descriptor and typed configuration. It emits a redacted immutable artifact proposal through the Eve run, while retaining `audit_only=true`, zero Git or content writes, zero PRs, zero Blob writes, disabled mutation, and the kill switch. The AI Gateway catalog adapter correctly uses a supplied short-lived runtime credential without logging it. Focused runtime/adapter tests and the complete offline sidecar gate pass.
- Verification: the isolated Eve eval suite passed `15/15` cases and `50/50` gates. The canonical root verifier passed `24/24` checks with evidence at `artifacts/verification/2026-08-01T183008410z-487ec6dd9973` for commit `487ec6d`; a fresh artifact is required after this plan/status checkpoint.
- Dependencies / next gate: this code must be human-merged and deployed before a Production runtime can receive the updated implementation and its environment. The first execution remains observe-only and must retain the exact one-time live-read approval. Vercel REST inspection still has a separately scoped-token prerequisite; optional adapters remain non-blocking and disabled.

The requested `openai/gpt-5.6-terra` route is now `LIVE_VERIFIED`. A bounded OIDC request with the approved `openai/gpt-4.1-mini` availability fallback returned the expected response and reported Terra as the model used. The primary route remains Terra. The verified fallback is retained only to preserve audit availability if the primary route becomes temporarily unavailable. This does not authorize a live audit, draft publication, merge, or production mutation.

## Implementation Progress (2026-07-29)

| Phase | Current evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Status                                                                                                                                                 |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 0–1   | Repository reconciled; control plane and disabled Stop hook pass their deterministic check. The root checker now also proves that the versioned source policy is present and required by root/sidecar/skill instructions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Complete (`MOCK_VERIFIED`)                                                                                                                             |
| 2     | Isolated sidecar package, Eve tree, native Eve Cron schedule, workflow entry point, agent roles, separate lockfile, backend-only Vercel configuration, typed environment readiness gate, and independent lint/format/typecheck commands are committed. Eve builds locally under a compatible Node 25 runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Offline complete (`MOCK_VERIFIED`); sidecar project linking and deployment remain blocked on human authorization                                       |
| 3     | Versioned inventory/link/fact/query/experiment/change/redirect/technical-SEO manifests, fixtures, schema/policy/contract tests, audit-only fixture workflow, and root `npm run seo-agent:verify` offline gate pass. A SHA-pinned credential-free trusted-branch workflow, versioned runner policy, preflight, redacted provenance recorder, and static regression fixtures now define the self-hosted offline gate. Provenance preserves the command, fixture revision, lock/runtime hashes, elapsed time, and original verifier status even for a failing verifier. The root gate invokes npm through its resolved Node CLI entry point, avoiding a shell-based Windows launcher. Dependency advisories remain separately labelled because they require a reviewed network path. | Offline complete (`MOCK_VERIFIED`); runner host and its firewall proof remain `BLOCKED_MISSING_CREDENTIALS`                                            |
| 4     | Compiled least-privilege agent boundaries, a central versioned AI Gateway model route plus atomic run-scoped tool/token/cost reservation policy, deny-policy evals for every prohibited action, a versioned adversarial fixture for injection/exfiltration/unsafe advice, a draft-publisher gateway fixture, shared run-budget metering, Vercel read allowlist, backend/Git-state scan, redacted security-escalation fixtures, and a protected native-Eve runtime dispatcher with deterministic audit run identity pass.                                                                                                                                                                                                                                                          | Offline complete (`MOCK_VERIFIED`)                                                                                                                     |
| 5     | Typed, feature-flagged AI Gateway catalog, GitHub read/PR inspection, Vercel project/deployment, Search Console, PageSpeed, HTTP fetch, injected web-search, Browserbase, GA4, Business Profile, and Local Falcon adapters now share bounded timeout/retry/rate-limit/response-size controls. Similarweb, Google Trends alpha, and tracing deliberately remain configuration-blocked until documented access is approved. A source-tier policy preserves provenance/access dates, rejects untrusted instructions, prevents competitor sources from supporting Wade/safety/law/manufacturer claims, and escalates conflicting values.                                                                                                                                              | Offline complete (`MOCK_VERIFIED`); all live reads remain `BLOCKED_MISSING_CREDENTIALS`                                                                |
| 6     | A hash-verified public Santa Cruz County research packet and publication-blocked opportunity backlog are committed. The audit path also renders a human-gated, Git-scoped manifest/evidence proposal without writing files, and the human-only rollback procedure has a deterministic drill record. These establish no Wade's business claim, service area, canonical owner, or new URL.                                                                                                                                                                                                                                                                                                                                                                                          | Preparation complete (`LIVE_VERIFIED` public sources; `MOCK_VERIFIED` artifact and rollback contracts); production audit `BLOCKED_MISSING_CREDENTIALS` |
| 7–9   | Sandbox drafting, preview, human merge, and post-merge production audit require the approved Phase 6 production audit and linked Vercel projects. Deterministic gates already reject missing noindex protection, blocked preview evidence, non-read-only production audits, and deployment SHAs that do not match the human merge.                                                                                                                                                                                                                                                                                                                                                                                                                                                | `BLOCKED_MISSING_CREDENTIALS`                                                                                                                          |

## Phase 2 Foundation Refresh (2026-07-30)

- Verified the installed Eve `0.27.13` package requires Node `>=24`; local Node `25.9.0` satisfies it. Current Eve type declarations confirm that Eve owns the Vercel Sandbox image, so the source enforces `networkPolicy: "deny-all"` without an unsupported runtime override.
- Added a sidecar-only `vercel.json` for `automation/seo-agent`. It installs the sidecar lockfile and runs the sidecar build; Eve supplies the Vercel Build Output, Workflow, and native Cron metadata. No Vercel project was linked, no deployment was created, and no public-site project configuration was modified.
- Added independent lint, formatting, typecheck, test, dry-run, build, deployment-configuration verification, and production-readiness preflight commands. The preflight intentionally fails without production credentials.
- Acceptance evidence is recorded in `IMPLEMENTATION_STATUS.md`: isolated install, `ci:offline`, fixture dry-run, root dependency install, root build classification, and credential-safe environment checks.
- Re-verification remains a required Phase 2 acceptance activity: run `npm run verify:all` with the compatible sidecar runtime before each foundation checkpoint, and record any public-site build result separately from the sidecar. A missing public-site Supabase configuration is an external baseline limitation, never a reason to weaken the public build or alter it from this sidecar phase.

## Phase 3 Route-Map Completion (2026-07-30)

- Added `seo/manifests/route-map.json` to the offline repository inventory contract.
- The generator records source paths, action dependencies, route parameters, and a materialization state. Directly declared static paths remain `MOCK_VERIFIED`; dynamic runtime-data patterns are `BLOCKED_MISSING_CREDENTIALS` until approved runtime data is available.
- The verifier rejects a stale or malformed route map, including a dynamic route masquerading as a materialized static URL.

## Phase 4 Core Runtime Completion (2026-07-30)

- The native Eve schedule now uses the verified `defineSchedule({ run })` handoff to the backend-only runtime channel. The runtime channel starts a durable Eve task session and returns after session creation; it never executes a long audit in the Cron HTTP response.
- The public-less runtime exposes only `/api/healthz`, `/api/readyz`, and a protected `GET /api/cron` dispatcher. The external dispatcher uses a timing-safe `CRON_SECRET` Bearer comparison in production. The native Eve/Vercel schedule is platform-authenticated and does not rely on that shared secret.
- Deterministic UTC-date run IDs, SHA-256 idempotency keys, a stable Eve continuation token, active-session lookup, short-lived process lock, and validated Git-backed completed-run history prevent duplicate runs without an application database. Cold-start/provider behavior remains a Vercel runtime prerequisite, so it is not claimed as live proof.
- `observe`, `propose`, `dry-run`, `paused`, and `disabled` are explicit runtime modes. Every currently implemented mode remains audit-only; the mutation kill switch defaults enabled and no GitHub mutation path is wired.
- The bounded orchestrator workflow enforces first-run audit-only behavior, step/time/token/cost/file/URL/PR limits, typed failures, bounded retries, timeout handling, circuit-breaker degradation, and redacted health/readiness responses. It deterministically produces one schema-checked `SEO_AUDIT_WORK_PACKET` for each declared specialist before provider dispatch; aggregate packet limits must fit the run budget and every packet denies content and PR mutation. Offline runtime integration fixtures cover authentication, malformed dispatch, duplicate dispatch/locks, deterministic scheduling, timeout/retry/circuit behavior, first-run protection, kill switch, unavailable-integration degradation, and typed work-packet safety.
- Native Eve evals now run five deterministic, mock-model cases through a temporary local Eve server. They gate direct-main/merge/deploy, prompt-injection and secret-exfiltration, fabricated review/job and unsupported-area/unsafe-advice, duplicate/doorway/stuffing, and audit-only/no-change behavior. The current production model remains on the approved Gateway route; only `SEO_AGENT_EVAL_FIXTURE=true`, set inside the local eval wrapper, selects the test model.
- A Vercel Sandbox smoke adapter is now available but cannot run absent an explicit production-sidecar approval plus Vercel OIDC/scoped token. Its fixture proves the actual SDK call shape requests `node24`, `networkPolicy: "deny-all"`, a benign Node stdout command, and guaranteed cleanup. It truthfully returns `BLOCKED_MISSING_CREDENTIALS` otherwise; a real smoke execution is still a Phase 6 external prerequisite.

## Phase 4 Agent Composition, Policies, and Evals (2026-07-30)

- Completed the authored Eve composition layer: the root orchestrator now coordinates the nine declared bounded specialists and exposes 14 package-form skills for orchestration, search performance, research, local SEO, content strategy, refresh/create/consolidate, internal linking, technical SEO, conversion usefulness, source policy, approval/risk policy, prompt-injection defense, preview audit, and post-deploy audit. The build verifier proves every skill is discoverable.
- Writer and independent QA now use distinct central AI Gateway profiles. Both profile routes and their fallback routes are versioned allowlists; the implementation uses the verified Gateway `providerOptions.gateway.models` fallback surface. Offline evals still use only Eve's mock model and make no provider call.
- The deterministic content gate returns `NO_ACTION` or `REQUEST_FIRST_PARTY_EVIDENCE` for unsafe, unsupported, overlapping, or evidence-free proposals. It explicitly covers fabricated jobs/reviews, prices, locations, guarantees, availability, licenses, warranties, response times, statistics, unsafe advice, copied competitor content, doorway pages, stuffing, hidden links, mass publishing, and date-only freshness.
- Native Eve eval coverage increased from five to 15 deterministic cases. The 10 added fixtures cover good existing-page evidence, generic SEO spam, fake locations, unsupported 24/7 claims, copied content, dangerous DIY instructions, cannibalization, conflicting sources, broken links/over-scoped patches, and valid no-change/evidence-request decisions. Critical bad cases pass only when the agent refuses or returns a non-mutation decision.
- Acceptance: `npm --prefix automation/seo-agent run ci:offline; npm run lint; npm run typecheck; npm run build; git diff --check` with Node `25.9.0` first on `PATH` exited `0` in `110s`. Sidecar lint/format/typecheck, 66 deterministic tests, 9 runtime integration tests, 15 native Eve evals/50 gates, audit fixture, rollback drill, independent review, Eve build, and manifest validation passed. Root lint/typecheck and 28-page build passed. This is `MOCK_VERIFIED`; no provider request, Vercel deployment, GitHub mutation, or content publication occurred.

## Phase 7 Draft-Publication Contract Completion (2026-07-30)

- Added a deterministic Markdown change-set boundary for the reserved future `content/pages`, `content/posts`, and `content/services` source. It permits only canonical LF Markdown files with required front matter, safe relative paths, unique file targets, content hashes, and the configured file/line budgets.
- A content draft PR now requires the validated change set, a packet that references it, an explicit future-Markdown-migration boundary, human approval, a feature branch, an attested staged branch commit, and a returned draft PR. Unsafe paths, secrets, unsafe content, oversized changes, digest tampering, missing staging, non-draft responses, and malformed metadata fail closed.
- The installed GitHub Tools Eve factory exposes the upstream `createPullRequest` input schema without a supported input-schema override. It is therefore deliberately not exposed to the model: it could otherwise accept arbitrary repository/base/draft inputs. A real scoped publication gateway remains a human-approved external setup prerequisite; fixtures exercise only its strict adapter contract.

## Phases 7–9 Controlled Lifecycle Contract (2026-07-30)

- Added a credential-free, injected lifecycle coordinator that starts from a pinned `main` commit, creates a deny-all isolated checkout, gathers evidence, selects exactly one existing-page-first opportunity, validates a bounded Markdown patch, and derives only `eve/seo/YYYY-MM-DD-<slug>` branch names.
- The coordinator is mutation-disabled by default and requires four simultaneous gates before it can invoke an injected gateway: enabled publishing mode, disabled runtime kill switch, an explicit human approval record, and an integration-test flag. It rejects gateways exposing a merge method, direct-main/force commit results, deletion, unsafe content paths, unsupported service areas, and incomplete labels.
- The lifecycle creates a draft-only PR packet with source/evidence, metrics, claims, files, tests/evals, preview, risks, expected outcome, observation window, rollback, and human-review checklist. It waits only for self-hosted CI, comments failed CI/preview findings, marks ready only after a passing non-indexable preview audit, observes rather than performs a human merge, then runs a read-only production audit tied to the human merge SHA and records a monthly operations issue.
- The deterministic fixture covers success, disabled mode, CI retry/failure, preview failure, awaiting human merge, SHA mismatch, unsafe branch/selection/area/merge gateway, deletion, destructive SEO paths, and PR-body completeness. It is `MOCK_VERIFIED`; no live client is wired and all real publication remains blocked on owner-managed least-privilege setup.

## Phase 5 Browser-Integration Truth Boundary (2026-07-30)

- The required browser-research adapter is now explicitly classified as allowlisted HTTP public-document retrieval. Its evidence carries `retrieval_mode: HTTP_DOCUMENT_FETCH`; it cannot be described as browser automation.
- Added a distinct provider-neutral browser-automation adapter contract. It remains `BLOCKED_MISSING_CREDENTIALS` until a human approves and configures an isolated browser provider. A provider must be injected, both initial and redirect domains must be versioned/allowlisted, and every attempted page consumes browser-page and external-request budgets before dispatch.
- The offline probe plan, integration inventory, tests, and setup guide now separately record both capabilities, preventing a fixture or HTTP response from being treated as live browser-automation proof.

## Phase 3 Observability-Contract Completion (2026-07-30)

- Run manifests now require an explicit audit outcome, model/tool latency records, per-tool approval outcomes, denial reason, error classification, and nullable draft-PR/preview URLs in addition to the existing correlation, budget, token, cost, prompt/skill, and source-revision fields.
- The audit-only fixture emits each field deterministically. Contract tests reject unsafe preview URLs and unrecognized approval outcomes, preventing an incomplete or ambiguous trace from being written into Git-backed evidence.
- This is an offline manifest contract only. Eve/Vercel trace export and external observability remain separately `BLOCKED_MISSING_CREDENTIALS`; the contract does not claim a live trace was delivered.

## Phase 3 Publishable-Metadata Integrity (2026-07-29)

- The repository-derived technical audit now rejects literal, publishable metadata/JSON-LD placeholders for Google verification, postal address, postal code, and coordinates. The real repository must have no high or critical technical finding; a temporary isolated fixture proves that the same detector continues to flag both placeholder content and build-error suppressions.
- Removed the existing literal verification token and unverified address/coordinate/area-served JSON-LD instead of replacing them with an unsupported business fact. Job structured data now uses the record's actual creation timestamp rather than a static historical date.
- This is an offline integrity boundary. It does not validate the truth of existing reviews, jobs, location claims, or production data; those require independent evidence and the existing human review path.

## Phase 10 Independent Readiness Review (2026-07-30)

- Added a deterministic, read-only independent review that consumes the generated technical-SEO manifest, integration inventory, and the public site's Next configuration. It reports a structured decision rather than treating a passing fixture suite as completion proof.
- The review deliberately exits successfully when it concludes `NOT_READY`: that result is evidence, not a test failure. It sets `completion_ready: false` until all completion blockers and external prerequisites have been resolved or explicitly accepted by the human final-verification phase.
- The initial independent review identified two public-site quality risks: TypeScript and ESLint build errors were suppressed in `next.config.js`. The Phase 10 remediation removed both suppressions only after root lint, explicit typecheck, and full production build passed. The generated technical-SEO inventory now has no high or critical finding.
- Current required live integrations are also recorded as external prerequisites. Fixture and mock coverage remains valid offline evidence, but it cannot substitute for a configured Eve/AI Gateway/Sandbox, scoped GitHub and Vercel read paths, Search Console, PageSpeed, or browser-research authorization.

## Phase 11 Observability, Security, and Operational Resilience (2026-07-30)

- Acceptance scope: a safe-field operational event contract, correlations across cron/workflow/provider/GitHub/browser/preview/production phases, recursive secret/PII redaction, deterministic metrics and alert classifications, fault recovery plans, tracked executable secret scanning, and a written threat model/incident procedure.
- Acceptance check: `npm --prefix automation/seo-agent run ci:offline && npm --prefix automation/seo-agent run audit:dependencies` with the compatible Node runtime. Fixture output must remain `MOCK_VERIFIED`; telemetry/alert delivery and provider traces remain `BLOCKED_MISSING_CREDENTIALS` until a reviewed sink and redacted live probe exist.
- Dependencies: Phase 4 runtime controls, source policy, rollback drill, and lifecycle preview/production gates. This phase adds no mutation authority, provider request, browser session, GitHub action, deployment, or public route.

## Phase 12 Verification Evidence System (2026-07-30)

- Acceptance scope: `npm run verify:all` generates redacted local artifacts with command logs/reports, byte-level tracked-tree freshness, tool versions, test/eval/build/integration/finding/blocker evidence, and a machine DoD result set.
- Acceptance check: `npm run verify:all && npm run verify:completion` from a clean tracked worktree. Completion rejects missing/stale evidence, failed required checks, high/critical findings, unclassified integrations, and incomplete DoD items; explicit credential blockers remain reported, not treated as live success.
- Stop-hook acceptance: the enabled sentinel has a maximum continuation budget no greater than three. The PowerShell hook may signal continuation only when supplied active-state JSON is active, completion fails, and the supplied count remains below that bound. It never writes state or reruns work.

## Phase Update Template

Append to `IMPLEMENTATION_STATUS.md` after each phase:

```markdown
## Phase <n> — <name> (<YYYY-MM-DD>)

- State: COMPLETE | IN_PROGRESS | BLOCKED_MISSING_CREDENTIALS | REJECTED
- Commit / branch:
- Changed scope:
- Commands: `<command>` — exit `<n>` — duration `<time>`
- Evidence: `<paths or redacted external references>`
- Verification classification: LIVE_VERIFIED | MOCK_VERIFIED | BLOCKED_MISSING_CREDENTIALS | FAILED
- Failures / limitations:
- Next exact action:
```

## Phase 18 — Review Remediation and Canonical Runtime Selection (2026-07-30)

- Acceptance scope: repair accepted Blob/archive review findings, per-finding evidence provenance, and canonical verifier selection of the Eve-compatible runtime without enabling an external integration.
- Acceptance checks: archive fixtures must deny uncommitted/tampered Git content, invalid approval records, raw payload shapes, and partial-write retries; plans must remain blocked rather than claim provider verification; root evidence fixtures must require individual source/tool, timestamp, scope, and truth state; `npm run verify:all` must select Node 24+ for sidecar eval/build checks or fail before recording misleading green evidence.
- Dependencies: no sidecar project, Blob token, Git reader credential, or production approval is introduced by this phase. A live Blob write remains a later, explicitly authorized human verification step.

## Phase 20 — Editorial Reuse, Image Provenance, and Demand-Timed Growth (2026-07-30)

- Acceptance scope: make committed Markdown inventory and contextual internal links the first planning input; enforce posts as the normal net-new URL type; forbid new service pages; require explicit approval for landing pages; constrain service work to refreshes; enforce demand-timed rather than cadence-based blogging; and add rights/relevance/alt-text provenance for proposed imagery.
- Acceptance checks: `npm --prefix automation/seo-agent run format:check && npm --prefix automation/seo-agent run lint && npm --prefix automation/seo-agent run test && npm --prefix automation/seo-agent run test:integration && npm --prefix automation/seo-agent run build`. Regression fixtures must reject new service URLs, generic-page automation, fixed cadence, insufficient holiday lead-time/local relevance, broken contextual links, and unlicensed or irrelevant image plans.
- Dependencies: Phase 19 Markdown-architecture rebase must retain root/sidecar isolation. The injected image-research boundary is fixture-tested and deliberately returns non-publishable `UNVERIFIED` candidates; a live provider still needs separate reviewed credentials, explicit source/asset domain allowlists, and human asset approval. No content changes, provider call, GitHub mutation, deployment, or production configuration is allowed in this phase.

## Phase 21 — Demand-Timed Editorial Selection (2026-07-30)

- Acceptance scope: rank evidence-backed candidates without a fixed publishing cycle; prefer a viable existing-page/link improvement; select a new blog only for a proven distinct unmet intent; and return `NO_ACTION` for past holidays, low confidence, or absent support.
- Acceptance checks: `npm --prefix automation/seo-agent run format:check && npm --prefix automation/seo-agent run lint && npm --prefix automation/seo-agent run test`. Fixtures must prove existing-page preference, a permitted distinct blog fallback, and no-action outcomes without external calls.
- Dependencies: consumes the Phase 20 editorial policy and remains planning-only. A subsequent artifact/experiment phase may serialize the selected plan but must not open a PR or create content.

## Phase 22 — Vercel Services Deployment Topology (2026-07-31)

- Acceptance scope: move from two Vercel projects to one Vercel Services project without merging application runtimes. The public `site` service remains at `/`; `eve_seo_agent` remains backend-only under `/_internal/eve`, with separate root, lockfile install, and build.
- Acceptance checks: root `vercel.json` is the only Vercel configuration; `npm --prefix automation/seo-agent run verify` validates service roots, route prefixes, and Eve-owned output/Cron metadata; architecture checks reject a public-site import of sidecar code or reference to Eve/Gateway environment names; root lint/typecheck/test/build and sidecar offline gates remain independent and green.
- Dependencies: current Vercel Services access/configuration remains `BLOCKED_MISSING_CREDENTIALS` until an owner links the project and records a redacted deployment probe. Services does not isolate static project server variables: use OIDC/Connect where available, never put agent values in `NEXT_PUBLIC_`, and revert to separate projects if a connector cannot be safely constrained.

## Phase 42 Update: GitHub Connect Scope Repair (2026-08-01)

- Acceptance scope: bind every Vercel Connect GitHub app token request to only `byronwade/wadesplumbingandseptic.com` and only the read permissions required by the audit adapter.
- Result: the deployed OIDC exchange and scoped GitHub token are `LIVE_VERIFIED`. The live inspection found the older unscoped provider could inherit the connector installation's wider repository set. The corrective source now requires a repository, passes Vercel Connect `github_app_installation` authorization details, and requests `contents:read` plus `pull_requests:read` only. The repair is locally test-verified and awaits its own deployment.
- Dependencies / next gate: restrict the GitHub App installation itself to only the Wade's repository, merge the corrective PR, and then run the approved audit-only Production workflow. The current Production deployment remains observe-only and has zero Agent Runs.

## Phase 43 — Eve Draft-Proposal via Cron + Connect (updated 2026-08-03)

- Acceptance scope: a durable, protected `proposal` job creates one bounded Markdown blog-post draft and one GitHub draft PR through Vercel Connect after Cron research.
- Required controls: `SEO_AGENT_RUN_MODE=propose`, `SEO_AGENT_MUTATION_MODE=enabled`, `SEO_AGENT_MUTATION_KILL_SWITCH=false`, and `SEO_AGENT_ENABLE_GITHUB=true`. The write token must be issued through Vercel Connect for only `byronwade/wadesplumbingandseptic.com` with fixed contents, pull-request, and issue write scopes.
- Acceptance checks: deterministic tests prove disabled GitHub, disabled mutation, enabled kill switch, rejected independent QA, malformed content, non-draft PR, direct-main path, and force update cannot write. The only allowed sequence is `read main` → `create eve/seo/YYYY-MM-DD-slug branch` → `create one commit` → `open one draft PR`. The job must not merge, deploy, delete, or mark a PR ready.
- Runtime behavior: observe mode keeps the Monday Cron on audit. Propose mode selects the proposal job on that same Cron. The first live proof remains a conservative household-plumbing checklist with one home-page internal link and no Wade-specific location, availability, licensing, warranty, pricing, review, or service claim.
- Dependencies / next gate: merge and deploy Phase 51, set the four Production values in the Vercel UI, run the Eve Cron, and inspect the Connect-created draft content PR. Do not merge the content PR automatically.
